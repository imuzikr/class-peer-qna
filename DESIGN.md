# 자리표 디자인 기준

## 1. 분위기
기존의 따뜻한 크림색 학급 화면과 조밀한 자리 카드를 유지한다.
이 문서는 멋진 순간·확대 창·수업 중 개별 보기·손들기 확인·학생 자리표·참여
전광판·자리 배정 창과 기록 관리의 누가기록·수업 노트 목록에 적용한다. 모둠 보기는 제외한다.

## 2. 색상
기존 `app/globals.css` 토큰을 사용한다: 배경 `--bg-deep`, 테두리
`--border`, 본문 `--text`, 보조 글자 `--text-sub`.
출석은 기존 초록색, 결석은 붉은색, 확인 전은 중립색을 유지한다.

## 3. 글꼴
기존 Pretendard/Malgun Gothic 계열과 제목의 `--font-serif`를 유지한다.
좁은 자리표의 이름 8.5px, 학번 7.5px 및 확대 창의 기존 크기는 변경하지 않는다.

## 4. 간격과 배치
- 한 줄은 같은 너비의 자리 6개이며, 2열씩 세 묶음으로 보인다.
- `--seat-pair-gap`은 묶음 안의 가로 간격 4px이다. 행 간격은 화면별 기존 값을 유지한다.
- `--seat-pair-spacer`는 묶음 사이 빈 트랙의 너비 4px이다.
  트랙 양옆의 간격을 합쳐 묶음 사이는 12px이다. 768px 이하에서는 빈 트랙을
  0px로 줄여 묶음 사이를 8px로 표시하며, 자리 수는 항상 6열이다.
- `--seat-min-width`는 일반 자리표 64px, 좁은 패널 44px, 수업 중 개별 보기
  64px, 참여 전광판 96px, 자리 배정 창 112px이다. 자리 배정 카드는 기존 최소 높이 74px와 3:2 비율을 유지한다. 기존 글자와 배지를 담는 기준이다.
- `SeatGrid`의 스크롤 영역에는 4px 여백을 둔다. 카드의 최소 너비보다 좁으면
  자리표 안에서만 가로 스크롤한다. 키보드로 영역에 접근해 좌우 방향키로 이동할 수
  있으며, 좌석에 포커스가 가면 해당 카드가 보이는 위치로 스크롤한다.
- 오른쪽 보상 패널 안쪽 너비는 312px로, 6개 카드와 묶음 간격·스크롤 영역 여백을
  모두 담는다(필요 너비 308px). 참여 전광판은 760px, 손들기 확인·학생 자리표는
  640px로 넓히며 기본 모달의 640px 제한에 전광판이 눌리지 않게 한다.
- 확대 창 760px, 자리 배정 창 1180px은 유지한다. 자리 배정 창은 1100px 이하에서
  미배치 학생 목록을 아래로 내려 자리표 너비를 확보한다. 화면 자체가 좁아 여섯
  카드를 담을 수 없을 때만 내부 가로 스크롤을 유지한다.
- 기록 관리 목록은 카드 최소 너비 96px, 행 간격 8px이며 기존 모달 최대 너비
  860px을 유지한다. 세로 높이 60vh 제한은 공통 스크롤 영역에 적용한다. 학번순
  명단·필터 결과를 그대로 표시하며, 선생님 보기는 6명씩 줄 순서만 뒤집는다.
- 빈자리도 같은 열을 차지한다. 학생/선생님 보기 모두 2열/2열/2열을 유지한다.
- 모둠 이름·빈 모둠 안내·모둠별 학생 목록에는 이 간격 규칙을 적용하지 않는다.

## 5. 공통 컴포넌트
`SeatGrid`가 스크롤 영역과 공통 열 배치를 담당한다. 기록 목록은 스크롤 영역의
클래스와 접근성 이름만 지정하며, 각 화면의 카드들을 자식으로
받는다. `.seat-pairs-grid`가 붙은 실제 자리표에만 공통 규칙을 적용한다.
`SeatPickGrid`와 `SeatCell`의 데이터와 이벤트를 유지한다. 좁은 패널과 확대 창은
동일한 자리 배열, 클릭, 드래그 및 보기 방향 상태를 공유한다.
기본·출석·결석·빈자리·손들기·과일 배지·hover·키보드 포커스 상태를 유지한다.
빈 간격은 CSS 트랙이며 새 DOM 요소나 포커스 대상을 만들지 않는다.

## 6. 동작
자리 클릭은 해당 학생을 선택하고, 드래그는 기존 자리 인덱스로 이동한다.
선생님 보기는 기존 180도 회전과 카드의 역회전을 유지한다.
간격 변경에 새 애니메이션을 추가하지 않는다.

## 7. 표면
기존 1px 테두리, 10px 자리 카드 모서리, 빈자리의 점선을 유지한다.
새 그림자나 장식은 추가하지 않는다.


## 참여 전광판 별도 창
- 기존 전광판의 카드·색상·보기 탭을 그대로 사용한다. 모달 머리말의 기존
  `btn-ghost` 계열 버튼으로 '새 창으로 열기'를 제공한다.
- 별도 창에서는 배경을 가리는 오버레이를 제거하고 화면 너비를 사용한다.
  기본 창 크기는 1120×800이며 사용자가 이동·크기 변경할 수 있다.
- 별도 창 여백은 기존 모달과 같은 20px, 좁은 화면은 12px이다. 창 안
  전광판과 누가기록 간격은 기존 14px이며, 누가기록 너비는 340px이다.
  1100px 이하에서는 누가기록을 아래에 배치한다. 자리의 최소 너비와
  2열 묶음은 기존 SeatGrid 규칙을 그대로 지킨다.
- 머리말 동작은 줄바꿈을 허용하고 한국어 버튼 글자는 단어 안에서 쪼개지지
  않게 한다. 반 이름과 방송 중·일시정지·대기 상태를 함께 표시한다.
- 팝업 차단은 모달에 읽을 수 있는 오류로 알리고 모달을 유지한다.
  별도 창에는 '원래 화면으로' 동작을 제공한다. 이동 시 보기·자리·작성 중
  누가기록을 보존하며, 창 닫기와 수업 종료 시 창과 이벤트를 정리한다.

## 출석 관리 좌석형
- 기존 출석 관리 탭과 모달을 유지하고 교사에게 좌석형 탭을 추가한다.
- SeatGrid의 6열/2열 묶음, 최소 카드 너비96px, 행 간격8px을 사용한다.
- 카드는 기존 출석 상태의 초록색 배경과 테두리, 기록 없음은 중립 배경을 사용한다.
- 이름14px/굵게, 학번·시간12px, 카드 여백12px, 모서리10px. 긴 이름은 줄바꿈한다.
- 선택일 출석 여부를 글자로도 표시한다. 저장된 빈자리는 자동으로 채우지 않고 점선으로 유지한다. 배치에 없는 학생은 인원수와 관계없이 별도 공통 그리드에 표시한다.
- 날짜별 자리배치가 있으면 우선 사용하고 없으면 기본 자리배치를 사용한다. 배치가 없으면 명단순임을 안내한다.
- 좌석형은 조회 화면이며 드래그나 출석을 변경하는 클릭 동작을 추가하지 않는다.
- 768px 이하에서 출석 관리 도구 줄은 8px 간격으로 줄바꿈하고 버튼 글자는 줄바꿈하지 않는다. 출석 시작/종료 묶음의 높이는 내용에 맞춰 늘어난다.

- 출석 좌석형도 공통 SeatViewToggle/useSeatView의 학생·선생님 보기 설정을 공유한다. 기본 자리 그리드만 기존 seat-flipped로 180도 회전하고 카드는 역회전한다. 미배치 명단은 고정한다. 현재 방향 버튼은 자리배치 안내 아래에 표시한다.

## 참여 전광판 자리표 정리
- 자리표 보기의 모둠 색 테두리는 제거하고 기존 출석 상태 배경과 1px 테두리를 유지한다. 모둠 이름 글자와 모둠별 보기의 섹션 색 띠·중립 카드 테두리는 유지한다.
- 반 이름과 방송 상태는 보기 전환 탭 바로 오른쪽에 둔다. 화면이 좁거나 반 이름이 길면 줄바꿈하여 읽을 수 있게 한다.
- 전광판 모달만 바깥 여백16px, 안쪽 여백20px, 최대 높이 `calc(100dvh - 32px)`를 사용한다. 기본 6열×5행을 일반 데스크톱 높이에서 전부 표시하되 작은 화면에서는 스크롤로 모든 내용에 접근할 수 있게 한다.
- 별도 창의 반·방송 상태도 동일한 도구 줄에 표시하며 기존 화면 크기 변경과 누가기록 패널 동작을 유지한다.
- 제목 아래 간격8px, 도구 줄 아래8px, 범례 아래12px로 위쪽 공간을 줄인다. 자리 카드 크기와 글자 크기는 유지한다.
