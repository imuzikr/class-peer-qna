{
  "name": "class-peer-qna-lint",
  "private": true,
  "type": "module",
  "description": "ESLint — 루트와 분리해 둡니다(Vercel 빌드가 이 패키지를 설치하지 않게). 루트에서 npm run lint",
  "scripts": {
    "lint": "cd ../.. && node tools/lint/node_modules/eslint/bin/eslint.js app components lib"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^17.12.0"
  }
}
