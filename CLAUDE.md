# class-peer-qna — 프로젝트 컨텍스트

수업 중 학생 질문·답변을 실시간으로 공유하는 교실용 Q&A 웹앱.

## 기술 스택

- **프레임워크**: Next.js 15.5 App Router, React 18 (Client Components)
- **DB**: Firebase Firestore (실서비스) / 브라우저 인메모리 Mock (데모 모드)
- **스타일**: 단일 CSS 파일 `app/globals.css` (CSS Variables + Flexbox)
- **배포**: Vercel

## 실행

```bash
npm run dev        # 개발 서버 (http://localhost:3000)
npm run build      # 프로덕션 빌드
npm run test:rules # Firestore 보안 규칙 테스트 (에뮬레이터, Java 필요)
npm run test:unit  # lib/ 순수 함수 시험 (설치 없음 — node --test)
npm run lint       # ESLint 훅 규칙 (보고만, 최초 1회 npm ci --prefix tools/lint)
```

규칙 테스트는 `tests/rules/`에 있고 최초 1회 `cd tests/rules && npm install`이
필요합니다. 루트와 분리한 이유·작성 시 주의점은 `tests/rules/README.md` 참고.
**`firestore.rules`를 고치면 반드시 이 테스트를 돌리고 배포하세요.**

**단위 시험**(`tests/unit/`)은 앱 코드를 **그대로 import**합니다 —
`tests/unit/resolve.mjs`가 `@/` 별칭과 확장자 없는 import를 풀어 줍니다.
시험 도구를 설치하지 않은 것은 규칙 시험을 떼어 둔 것과 같은 까닭입니다.
**JSX는 못 읽으니** 컴포넌트 안의 셈을 시험하려면 lib/의 함수로 빼내세요
(`RewardTiming` → `lib/lessonSessions.js`의 `rewardTimingStat`).
Firebase를 import하는 `lib/store.js`·`lib/data/*`도 못 읽습니다 — 순수 함수를
거기 새로 두지 말고 제 파일에 두세요(날짜는 `lib/dates.js`, store.js가 같은
이름으로 다시 내보냅니다). `tests/ui/`의 둘(popover · 과일 축포)은 React
훅을 가짜로 바꿔 끼우는 시험이라 소스를 잘라 쓰는 방식을 일부러 둡니다.

**날짜 셈은 `lib/dates.js` 한 곳입니다** — `dateKeyOf`(Date → `2026-09-25`) ·
`todayDateKey` · `dateKeyFromParts`(달력 칸) · `shiftDateKey`(± 며칠) ·
`dateKeyLabel`(`9월 25일 (금)`) · `dateKeyDots`(`2026.09.25`). 같은 셈이
열 파일에 열다섯 벌 흩어져 있던 것을 모았습니다(`ymd` · `toYMD` · `getToday`
· `toDateKey` · `shiftDate` · `fmtDate` · `formatDateLabel` …). 날짜 열쇠가
곧 문서 ID(`uid_날짜`)라 한 글자만 달라져도 지난 기록을 못 찾으므로,
**컴포넌트에 새로 짜지 말고 여기서 가져오세요.** 옛 구현을 그대로 옮겨 적은
참조본과 두 해치 날짜를 견주는 시험이 `tests/unit/dates.test.mjs`에 있습니다.

**린트**는 설치물만 `tools/lint/`에, 설정은 루트 `eslint.config.mjs`에
있습니다(ESLint는 설정 파일 폴더 바깥을 무시합니다). `next build`는
린트를 건너뜁니다(`eslint.ignoreDuringBuilds`). 지금은 경고 28개 —
의존성 빠짐 21 등 — 를 **보고만** 합니다(쓸모없어진 끄기 주석 5개는 지웠습니다).

**CI**(`.github/workflows/ci.yml`)가 main과 `claude/**` 푸시, PR에서 위
셋과 빌드를 돌립니다. 배포를 막지는 않으니(Vercel은 따로 배포합니다)
**main에 올리기 전에** 작업 브랜치의 결과를 보세요.

## 배포 (Firestore 규칙 · Cloud Functions)

```bash
npx firebase-tools login   # 최초 1회, 브라우저로 Google 계정 로그인
npm run deploy             # 규칙 테스트 → firestore:rules + functions 배포
```

`npm run deploy -- --rules-only` / `--functions-only` / `--skip-tests` 옵션으로
범위를 좁힐 수 있습니다 (`scripts/deploy.sh` 참고). `firebase-tools`는 npx로
그때그때 받아 쓰고 루트 devDependency로는 넣지 않았습니다 — Vercel 빌드가
이 패키지를 전혀 쓰지 않는데 설치 시간만 늘기 때문입니다(`tests/rules/`,
`functions/`를 별도 워크스페이스로 분리한 것과 같은 이유). 앱 코드(Next.js)는
`main` 푸시 시 Vercel이 자동 배포하며, 이 스크립트와는 무관합니다.

Firebase 미설정 시 자동으로 **데모 모드**로 동작 (새로고침 시 데이터 초기화).
실서비스 전환: `lib/firebase.js`의 `firebaseConfig`에 Firebase 콘솔 값 입력.

### 함수 요금 — 천장은 두고 바닥은 두지 않습니다

`functions/index.js`의 `setGlobalOptions`에 **`maxInstances: 10`**이 있습니다.
인스턴스는 함수를 돌리는 일꾼 한 명이고, 이 값은 **평소 쓰는 양을 재는 것이
아니라 사고를 막는 천장**입니다 — 한 반 스물여섯 명이 일제히 눌러도 요청은
1~2초에 걸쳐 줄지어 들어오고 함수 하나가 Firestore에 두어 번 쓰고 끝나므로,
같은 순간에 필요한 일꾼은 두세 명입니다. 천장에 닿아도 요청은 **거부되지 않고
줄을 서서** 처리되고, 여기 함수는 답변 수 집계와 알림이라 학생이 결과를
기다리는 종류가 아닙니다.

**`minInstances`는 일부러 비워 둡니다**(기본 0). 그것은 요청이 없어도 일꾼을
켜 두어 콜드 스타트를 없애는 설정인데, **아무도 안 쓰는 시간에도 요금이
붙습니다.** 하루에 몇 번 도는 이 앱에서는 몇 초를 아끼자고 낼 값이 아닙니다.
누가 '함수가 느리다'고 하면 이 값부터 켜고 싶어지는데, **켜기 전에 콘솔에서
실제 실행 시간을 먼저 보세요.**

함수가 깨어나는 때는 셋뿐입니다 — 답변이 달릴 때 · 답변이 지워질 때 ·
**질문 문서가 고쳐질 때**. 마지막이 `onAnswerUnderstood`인데 걸린 조건이
'질문 문서 업데이트'라, 학생이 '나도 궁금해요'를 누르거나 회고를 써도 한 번
깨어났다가 채택이 아니면 첫 줄에서 돌아 나옵니다. **무한 루프는 없습니다** —
답변 수를 올리는 쓰기가 이 함수를 깨우지만 그 함수는 질문 문서에 다시 쓰지
않아 고리가 닫히지 않습니다. 나머지 다섯(역할 부여 · 계정/반 삭제 · 반 공지)은
교사가 가끔 누르는 것입니다.

이 앱에서 요금이 **먼저** 문제가 될 곳은 함수가 아니라 Firestore 읽기입니다
(맨 아래 '질문 읽기 범위' 절). 금액 자체는 Google Cloud 콘솔의 **예산 알림**으로
지켜보세요 — 코드로 막을 수 있는 것은 천장까지입니다.

## 주요 페이지

| 경로 | 설명 |
|------|------|
| `/` | 랜딩 — 수업 코드 입력 |
| `/board` | 질문 게시판 (3단 레이아웃: 키워드·피드·공지) |
| `/study` | 공부방 (프로젝트 대시보드 → 프로젝트 → 개인 카드 + KWL 패널) |
| `/admin` | 관리자 대시보드 |
| `/report` | 학생 학습 리포트 |

## 핵심 파일

| 파일 | 역할 |
|------|------|
| `lib/store.js` | 데이터 함수의 **입구** — `lib/data/*`의 공개 이름을 다시 내보내기만 함 |
| `lib/data/*.js` | Firestore CRUD + Mock + 구독 — 영역별(questions · classes · study · books · kwl · cornell …) |
| `lib/data/shared.js` | 데모 모드 자료(`mock`) · 구독자 목록 · `nextMockSeq` · 영역끼리 나눠 쓰는 도우미 |
| `lib/roster.js` · `lib/useClassRoster.js` | 교사 화면의 반 명단(소속 × 실명 × 과일 누적, **학번순**) — 셈과 구독 훅 |
| `lib/user.js` | `getCurrentUser()`, `isAdmin()` — 세션 기반 사용자 |
| `lib/firebase.js` | Firebase 초기화. `isFirebaseConfigured` 플래그로 모드 분기 |
| `app/globals.css` | 전체 스타일. 모바일 반응형은 파일 하단 `@media (max-width: 768px)` |
| `components/QuestionModal.jsx` | 질문 상세 모달 (2열 데스크톱 / 단일스크롤 모바일) |
| `components/RichTextEditor.jsx` | 서식 입력 에디터 (variant: full/chat) |
| `components/KwlPanel.jsx` | KWL 사이드 패널 (오늘 탭 + 기록 탭) |
| `components/ActivityHeatmap.jsx` | 52주 잔디 히트맵 + ActivityOverview 통합 패널 |
| `components/ActivityOverview.jsx` | 오각형 레이더 차트 + 막대 요약 (학습 균형) |
| `components/PythonRunner.jsx` | Python 코드 실행기 (코드 복사 버튼 포함) |
| `lib/pyRun.js` | Pyodide 워커 한 벌 — 실행기와 수업 노트 서랍의 활동 칸이 나눠 씀 |
| `lib/pyCells.js` | 파이썬 연계 활동의 **셀**(글 셀 · 코드 셀+결과) ↔ 카드 HTML 풀기·적기 — 옛 카드도 읽음 |
| `components/PyCellEditor.jsx` | 파이썬 연계 프로젝트의 서랍 활동 칸 — 글 셀 · 코드 셀(▶는 그 셀만 · 결과는 저절로 붙고 코드를 고치면 걷힘) |
| `lib/projectTask.js` | 카드의 '파이썬 실행기' 단추 → 수업 노트 서랍에 '프로젝트 활동'을 여는 창 이벤트 |
| `components/StudyProjectDashboard.jsx` | 공부방 첫 화면 — 프로젝트 카드 그리드 (교사·학생 공통) |
| `components/StudyProjectView.jsx` | 프로젝트 상세 — 개인 카드 그리드 + 교사 도구 |
| `components/StudyMyActivityCard.jsx` | 개인 카드 상세 — 활동 칸 + 누구의 카드인지 칩 · 옆 학생 넘기기 |
| `components/StudyProjectForm.jsx` | 프로젝트 만들기 모달 (제목·안내·활동 목록) — **원본**을 만듭니다 |
| `components/StudyTemplateList.jsx` | 프로젝트 원본 목록 (교사) — '수업 관리' 창의 프로젝트 탭 · 우리 반에 가져오기 · 편집 · 삭제 |
| `components/StudyProjectImportModal.jsx` | 프로젝트 가져오기 (교사) — 공부방 머리줄, 원본 골라 이 반에 열기 · 아래 '＋ 프로젝트 만들기'(만들면 곧바로 이 반에) |
| `components/StudyTemplateEditModal.jsx` | 프로젝트 원본 편집 모달 — 제목·안내·활동·연계 (교사), 다음에 가져오는 반부터 적용 |
| `components/StudyProjectEditModal.jsx` | 프로젝트 편집 모달 — 제목·활동 안내만 (교사) |
| `components/ClassNotesTools.jsx` | 기록 관리·수업 메모 버튼 + 모달 묶음 (교사 전용) |
| `components/CornellNoteDrawer.jsx` | 수업 노트 서랍 (학생 전용) — 오른쪽 손잡이 → 코넬 세 칸 · 오늘의 활동과 날개로 나란히 |
| `components/LessonAnswerPanel.jsx` | 활동보기 — 수업 중 자리표의 첫 탭, 내보낸 활동의 학생 답을 한 명씩 |
| `components/CornellNoteSheet.jsx` | 수업 노트 한 장 — **읽기 전용 코넬 2단**(리포트·교사 열람 공용) |
| `components/CornellNoteViewerModal.jsx` | 내 노트 크게 보기 (학생) — 수업 노트 · KWLS 두 탭, **지난 노트를 고치는 유일한 자리** |
| `components/CornellNoteEditSheet.jsx` | 수업 노트 한 장 — **고치는** 코넬 2단 (위 창에서만) |
| `components/MyKwlsNotes.jsx` | 위 창의 'KWLS 노트' 탭 — 내 KWLS 넘겨 보기 |
| `components/KwlsNoteSheet.jsx` | KWLS 한 장 — 읽기 전용 K·W·L·S 2×2 |
| `components/KwlDateCalendar.jsx` | KWLS 날짜 달력 (교사 전체 화면 · 학생 KWLS 탭 공용) |
| `components/CornellNotesPanel.jsx` | '기록 관리'의 수업 노트 탭 — 날짜 하나로 반 전체 |
| `components/CornellNoteReadModal.jsx` | 한 학생의 지난 노트 넘겨 보기 + 피드백 (교사 전용) |

책방(`/books`)은 **활동 목록 → 활동** 두 단계입니다(URL은 `?activity=`).
예전에는 '활동 종류 그리드 → 그 종류의 목록 → 활동' 세 단계였는데, 가운데
화면이 하는 일이 종류를 고르는 것뿐이라 만들기 창의 종류 고르기와 겹쳤습니다.
지금은 반의 활동을 종류 섞어 한 목록에 **만든 차례**(오래된 것이 앞)로
늘어놓고 카드마다 종류를 적습니다. 옛 주소의 `?kind=`는 무시됩니다.
한때 최신순이었는데, 활동을 하나 만들 때마다 목록이 통째로 한 칸씩 밀려
어제 열던 활동이 매번 다른 자리에 있었습니다. 그래서 '가장 최근 활동'이
필요한 곳(`BookActivityStats`의 기본 선택)은 목록의 **끝**을 봅니다.

활동을 연 화면(다섯 보드)의 머리말 둘째 줄에도 **반 고르개**를 답니다
(`classPicker` prop — 페이지에서 **한 번 만들어** 내려 줍니다). 예전에는 반
이름 배지뿐이라 옆 반을 보려면 '← 활동 목록'으로 나갔다 다시 골라야 했습니다.
**반을 바꾸면 그 반의 활동 목록으로 갑니다** — 지금 연 활동은 이 반의 것이라
반만 바꾸고 화면을 두면 남의 반 활동을 보게 됩니다. 고를 반이 하나뿐이면
지금까지처럼 이름 배지입니다. 높이는 옆 배지(27px)에 맞추는데 세로 여백이
4px인 것은 `<select>`가 `line-height`를 듣지 않아서입니다(실측).

'＋ 독서 활동 만들기'는 제목 줄이 아니라 **그 아래 오른쪽 끝**에 따로 둡니다
(`.books-head-make`) — 공부방의 '＋ 프로젝트 가져오기'와 같은 자리입니다. 제목
줄에 함께 두었을 때는 반 고르개·KWLS 차트·기록 관리 사이에 섞여 '보는 단추'와
'새로 만드는 단추'가 구분되지 않았습니다.

`ClassNotesTools`는 공부방 제목 줄, 책방 목록 머리말(반 고르는 줄 오른쪽),
책방 모아보기 화면들의 제목 끝에 같은 것을 끼워 씁니다. 책방 쪽은
`app/books/page.js`에서 **한 번 만들어** `classTools` prop으로 각 보드에
내려 줍니다 — 보드마다 명단·반 정보를 따로 구독하지 않으려고요.

왼쪽 **'📌 오늘' 패널**(오늘 이 반의 출석·카드·성찰·과일)도 공부방과 책방이
같은 컴포넌트를 씁니다 — `StudyActivityPanel`에 `board={null}`을 주면 그
모습이 됩니다. 책방은 `.books-body`(가로 flex)로 감싸 왼쪽에 놓았고,
`studyBoards`·출석은 책방 페이지에서 따로 구독합니다. 첫 줄 안내는
`todayNote`로 바꿉니다(책방에서는 이 자리가 활동 관리로 바뀌지 않으므로).
접힘 상태는 두 화면이 같은 localStorage 키를 공유합니다.

**'KWLS 차트' 패널**도 두 화면이 같습니다 — 머리말의 단추로 왼쪽 끝에서 폭을
벌리며 열리고(`.study-kwl-slot`), 교사는 `TeacherKwlPanel`('반이 어디까지
썼나'), 학생은 `KwlPanel`('내가 쓰는 곳')입니다. 책방 KWLS 활동도 공부방과
같은 `kwl` 컬렉션에 쌓이므로, 오늘 현황을 보러 화면을 옮길 이유가 없습니다.
책방에서는 **활동 목록 화면에서만** 답니다 — 활동을 열면 그 화면이 가로를 다
쓰는 데다 여는 단추(머리말)가 사라져 닫을 길이 없어집니다(연 상태는 기억).

## 첫 화면 — '아직 모름'과 '없음'을 가릅니다

로그인 직후 공부방·책방은 학생에게 **입장 코드 화면이 번쩍 스쳤습니다.**
`memberships`가 `[]`로 시작하는데 그 값만으로 '소속 없음'을 판정했기 때문입니다
— 서버 답이 오기까지 몇백 밀리초 동안은 '소속이 없는' 것이 아니라 '아직
모르는' 것입니다. 공부방을 첫 화면으로 두면 학생이 접속할 때마다 겪습니다.

두 페이지 모두 `membershipsLoaded`(첫 답이 왔는가)를 따로 들고,
**그 전에는 입장 화면도 본문도 그리지 않고** AuthGate와 같은 모습의 스피너
(`.auth-gate.auth-gate--inline`)를 둡니다. 학생 눈에는 로그인 스피너가
이어지다 제 화면이 뜨는 것으로 보입니다. 교사·비로그인은 물어볼 것이 없어
곧바로 `true`로 둡니다.

`--inline`이 따로 있는 이유: `.auth-gate`는 `min-height: 100vh`라 상단바
아래에 놓으면 56px만큼 넘쳐 스크롤 막대가 생깁니다.

**같은 함정을 조심하세요** — 구독 결과의 빈 배열은 '없다'가 아니라 '아직
안 왔다'일 수 있습니다. 빈 값으로 화면을 통째로 갈아 끼우는 자리라면
'답이 왔는가'를 따로 들어야 합니다.

**상단바의 `activeClassId`도 같은 함정입니다**(`TopNav`). 이 값은 학생
소속(`memberships`)에서 나오는데, 그 배열은 `[]`로 시작하고 **구독을 다시 걸
때도 잠깐 빕니다**. 그때 `null`로 떨어뜨리면 이 값에 걸린 것들이 함께 빠지고,
특히 **수업 노트 서랍이 언마운트되어** 학생이 쓰던 화면이 없어졌다 다시 나타나며
자동 저장(2초) 전의 글자가 날아갑니다.

- 그래서 **마지막으로 알던 반을 ref에 붙들어 둡니다**. 답을 기다리는 동안은
  그것을 씁니다.
- **놓는 조건은 '이번 구독의 첫 답이 왔는데도 비어 있을 때'**입니다
  (`membershipsLoaded` — 구독을 다시 걸 때마다 거짓으로 되돌립니다).
  붙들기만 하면 교사가 반 편성에서 뺀 학생이 없는 반을 계속 가리켜, 규칙에
  막힌 읽기가 조용히 실패합니다.
- **`membershipsLoaded` 하나만으로는 못 막습니다** — 중간에 다시 구독할 때는
  그 값이 이미 참입니다. 둘을 함께 봐야 합니다.

## 모바일 레이아웃 핵심 패턴

### QuestionModal (모바일)
- `modal-wide`: `height: 100dvh`, `overflow: hidden`, flex-column
- `qa-grid`: 단일 스크롤 컨테이너 (`overflow-y: auto`)
- `qa-mobile-header`: `position: sticky; top: 0; z-index: 2` — 스크롤해도 제목 탭 고정
- `qa-right`: `display: contents` → 자식(chat-head/chat-scroll/chat-compose)이 qa-grid 플렉스 아이템으로 편입
- `chat-scroll`: `flex: none; overflow-y: visible; min-height: 50vh` — 독립 스크롤 없음
- 자동 스크롤: `scrollRef`(qa-grid, 모바일) + `chatScrollRef`(chat-scroll, 데스크톱) 이중 처리

### 공부방
- 프로젝트 그리드(`study-project-grid`)·개인 카드 그리드(`study-project-cards`)는
  `repeat(auto-fill, minmax(…, 1fr))` → 768px 이하에서 `1fr`(한 줄에 한 장)
- KWL 모바일: FAB 버튼(`kwl-fab`) → `kwl-panel--open` 클래스로 오버레이 패널

## 역할 구분

- **교사(isTeacher)**: 프로젝트 만들기, 공지 작성, 전체 학생 카드 열람, 프로젝트 설정, 정렬
- **학생**: 질문 1개 작성, 프로젝트당 개인 카드 1개 작성, KWL 작성
- **관리자(isAdmin)**: 실명 확인, 답변 이해 표시, 회고 현황 확인

역할 전환 (개발용): `RoleSwitcher` 컴포넌트 (`role-switch` CSS 클래스, 모바일에서 숨김)

## 데이터 모델 (Firestore 컬렉션)

- `questions` — 질문 (keyword, authorId, resolved, meTooIds[], reflection) — 익명 닉네임만(authorName/authorEmoji)
- `answers` — 답변 (questionId, authorId, understood)
- `studyBoards` — 공부방 **프로젝트** (classId, type, viewMode, editMode, keywords[],
  activityType, activities[], activityLocks[]) — 화면 용어는 '프로젝트',
  컬렉션 이름은 예전 그대로(기존 데이터 유지)
  - `type: 'notice'` 하나는 반마다 자동 생성되는 '수업 자료'(선생님 보드)
  - **만들 때도 복제할 때도 첫 활동만 열립니다**(`activityLocks`가
    `[false, true, true, …]`). 책방의 곁텍스트 읽기와 **같은 모양**입니다 —
    거기도 만들면 1단계만 열립니다. 두 방이 같은 '한 칸씩 열어 주는' 진행인데
    시작하는 모습이 달라, 교사가 화면마다 다시 익혀야 했습니다.
    - 한때 **만들기는 전부 열림 · 복제는 전부 잠김**이라 같은 프로젝트가 어느
      길로 왔느냐에 따라 다르게 도착했습니다.
    - **이미 있는 프로젝트는 안 건드립니다** — 지금 학생이 쓰고 있는 활동이
      갑자기 잠깁니다. 바뀌는 것은 앞으로 만드는 것뿐입니다.
    - 수업 도중 '활동 설정'에서 더하는 활동은 지금까지대로 잠긴 채로
      시작합니다(`nextActivityLocks`). 파이썬 실행기에서 더하는 활동만
      예외로 열린 채입니다 — 지금 받으려고 만드는 자리라서요.
- `studyTemplates` — **프로젝트 원본** (ownerId, ownerName, title, description,
  keywords[], activityType, activities[], pyLinked, createdAt) — 반에 안 묶인 선생님의 것
  - **연계 두 단추 — 키워드 · 파이썬 실행기**(`pyLinked`). 만들기 창의
    '연계하기' 칸(안내 아래 · 활동 위, 머리 모양은 '활동' 칸과 같음)에 **두 누름
    단추가 한 줄에 반씩** 서고, 편집 창에도 같은 조각이 섭니다
    (`components/ProjectLinkOptions.jsx` — 두 창이 **같은 조각**이라 모양이
    한 벌입니다). 원본에서 복사본으로(`startStudyTemplateInClass`), 다른 반으로
    (`duplicateStudyBoard`), 옛 프로젝트 묶기로(`migrateLegacyGroup`) 함께 갑니다.
    - **꺼짐은 연한 살구(`--primary-light`), 켜짐은 살구(`--primary`, 흰 글자)**
      입니다(`.project-link-btn.on`, `aria-pressed`). 단추 하나가 곧 기능
      하나입니다 — 키워드 단추를 켜면 그 아래에 키워드 칩이 펼쳐집니다.
    - 한때 제목 줄의 '연계하기' **토글을 먼저 켜야** 체크 상자 두 줄이
      나타났는데 걷었습니다 — 켜는 스위치가 두 겹이라 '연계하기를 켰는데
      아무것도 안 된다'가 되었습니다. **마스터 토글을 되살리지 마세요.**
    - 480px 아래에서는 한 줄에 하나씩 섭니다.
    - **파이썬 실행기와 연계하기**를 켜면 학생 카드의 활동 칸 오른쪽 위
      (`.study-mycard-py`)와 '크게 보기' 창의 머리 줄에 '파이썬 실행기' 단추가
      섭니다. 누르면 **실행기가 아니라 오른쪽 수업 노트 서랍**에 그 활동 칸이
      '프로젝트 활동'으로 열립니다 — 아래 '수업 밖에서 — 프로젝트 활동' 절.
      그 칸은 **셀 편집기**(글 셀 · 코드 셀 — 아래 '파이썬 연계 프로젝트의 활동
      칸은 셀입니다' 절)이고, 쓴 것은 그 카드에 곧바로 저장됩니다. 이
      프로젝트에서는 **글·코드를 쓰는 곳이 서랍 하나**이고 카드는 보기와 첨부만
      합니다. 이름은 그대로 '파이썬 실행기'입니다(학생에게 '여기서 파이썬을
      돌린다'가 그 이름으로 익어 있습니다).
      - 한때 이 단추는 실행기를 2단으로 열어 보낼 곳을 잡아 두었고, 학생이
        실행기에서 '활동으로 보내기'를 눌렀습니다. 그 보내기에서 탈이 줄줄이
        났습니다(엉뚱한 칸에 들어가기 · 카드가 늦게 들여오기 · 잠긴 활동으로
        보내기). 학생이 **그 칸에서 곧바로 쓰면** 보낼 일 자체가 없습니다.
        그래서 **학생 실행기는 1단 연습장**이고(2단은 교사만), 길게 짠 코드는
        복사해 서랍의 코드 블록에 붙입니다.
    - 칸의 단추는 **크게 보기 창을 열지 않고** 곧바로 서랍을 엽니다(칸을
      여는 클릭까지 번지지 않게 막음). 실행기가 떠 있으면 접습니다 — 둘 다
      오른쪽에서 나와 겹칩니다. 창 안의 단추는 **창을 닫고** 서랍을 엽니다
      (보기만 하는 창이라 저장할 것이 없습니다).
    - **서는 곳은 내 카드뿐**입니다(`StudyProjectView`가 거름): 모둠 프로젝트는
      아직 안 씁니다(규칙이 학생의 카드 생성을 막음. 만들기·편집 창에서도 꺼
      두고 까닭을 적습니다), 교사·남의 카드도 아닙니다. 잠긴 활동은 칸 자체가
      읽기 전용이라 단추가 안 섭니다.
    - 실행기의 '＋ 새 프로젝트'로 만든 프로젝트는 연계를 **켠 채로** 만듭니다.
    - 그 폼은 **활동을 한 번에 여럿** 받습니다('활동 1 이름 (예: …)' · '활동 2
      이름' … + 목록 아래 '＋ 활동 추가', 둘째 칸부터 ✕, 빈 칸은 건너뜀) — 만들기
      창과 같은 목록입니다. 새로 만든 프로젝트라 **첫 활동만 열리고** 나머지는
      잠긴 채 들어옵니다.
    - **교사가 실행기 2단에서 활동을 고르면 그 활동을 엽니다**
      (`PyProjectPanel`의 `openIfLocked` — 프로젝트를 고를 때는 활동 1,
      활동을 고를 때는 그 활동). 실행기 안에는 활동을 여는 칩이 없어, 안
      열면 학생 쪽에 '잠긴 활동'만 뜨고 선생님은 공부방으로 건너가야 했습니다.
      수업 모드의 '내보내기'가 잠긴 활동을 함께 여는 것과 같은 까닭입니다 —
      공부방에서 **일부러 잠가 둔 활동도** 여기서 고르면 열립니다(선생님
      확인함).
    - 수업 중에 한 칸씩 더하는 '＋ 활동 추가'(맨 뒤에 붙고 열린 채 시작)는
      그대로입니다 — 맨 뒤에만 붙으므로 학생이 앞 활동에 쓴 글은 자리가 안
      바뀝니다.
    - 보낸 뒤 알림은 따로 없습니다(실행기 칸의 짧은 안내만) — 선생님 요청.
    - **규칙을 안 건드립니다** — `studyTemplates`·`studyBoards` 둘 다 필드
      목록을 못 박아 두지 않습니다(확인함).
  - **프로젝트는 선생님의 것이고 반의 것이 아닙니다.** 반에서 쓸 때
    **'우리 반에 가져오기'**가 그 반에 복사본(`studyBoards` 문서 +
    `templateId` = 원본 id)을 하나 엽니다(`startStudyTemplateInClass`).
    수업 자료(`lessons`)가 반에 안 묶이고 '수업 시작'으로 비로소 반과 만나는
    것과 짝입니다.
  - **공부방 머리줄의 단추는 '＋ 프로젝트 가져오기'입니다**
    (`StudyProjectImportModal`). 한때 '＋ 프로젝트 만들기'였는데, 반 화면에서
    눌렀는데 반에는 아무것도 안 생기고(원본만) 수업 관리 창이 떠서 '만들었는데
    왜 없지?'가 되었습니다(실제 신고). 이제 반 화면의 단추는 **이 반에 꺼내
    오는 일**만 합니다.
    - 창은 **고르기만** 합니다 — 줄마다 '우리 반에 가져오기', 이 반에 이미
      있으면 프로젝트 탭과 같은 **'연결된 프로젝트' + 열기**. 편집·삭제는
      없습니다(프로젝트 탭의 일). 같은 목록이 두 곳에 보여도 한쪽은 꺼내 쓰는
      곳, 다른 쪽은 관리하는 곳이라 할 일이 갈립니다.
    - **아래에 '＋ 프로젝트 만들기'가 늘 섭니다**(원본이 있어도 — 목록에 없는
      새 프로젝트를 만들 수 있게). 원본이 하나도 없으면 안내와 함께 그 단추만
      섭니다. 이 길로 만들면 **원본을 보관하고 곧바로 이 반에 엽니다**
      (`StudyProjectForm`의 `openInClass` → `createStudyProjectInClass`) —
      새로 만들어 이 반에서 쓰는 일이 누름 한 번이고, 만든 뒤 그 프로젝트로
      들어갑니다. 만들기 창의 안내 줄도 그 사실을 말합니다.
    - 만들지 않고 닫으면 **가져오기 창으로 돌아갑니다**(`creatingProject`가
      `"import"`/`"tab"`로 어디서 열었는지 듭니다).
    - **수업 관리 프로젝트 탭의 '＋ 새 프로젝트 만들기'는 원본만** 만듭니다 —
      반을 정하지 않고 미리 설계해 두는 자리라서요. 반에 곧바로 여는 길은
      가져오기 창 · 수업 편집 · 파이썬 실행기 셋입니다.
  - **왜**: 예전에는 프로젝트가 처음부터 반에 묶여, 같은 프로젝트를 세 반에서
    쓰면 문서가 셋이었고 수업 준비의 '가져오기' 목록에 같은 이름이 반 수만큼
    늘어섰습니다(실제 신고). 지금은 원본 하나가 **한 줄**입니다.
  - **원본을 보는 자리는 '수업 관리' 창의 프로젝트 탭 하나**입니다
    (`LessonManagerModal`의 `tab` · `projectsPane` → `StudyTemplateList`).
    수업 자료와 원본은 둘 다 반이 아니라 선생님에게 붙은 설계도라 한 창에
    탭으로 나란히 둡니다. 줄 모양도 수업 줄(`.lesson-row`)을 그대로 씁니다.
    - 한때 공부방 머리줄에 '📚 프로젝트 원본' 단추와 따로 뜨는 창
      (`StudyTemplateModal`)이 있었는데 걷었습니다 — 보는 자리가 둘이면
      어디서 봐야 하는지 헷갈립니다. 머리줄에는 '＋ 프로젝트 가져오기'
      하나만 둡니다(위 절 — 그 창은 고르기 전용이라 관리 자리와 겹치지 않음).
    - **두 탭은 같은 크기**입니다 — 탭 단추는 격자 한 줄에 `1fr`씩이라 탭이
      늘어도 같은 폭으로 서고(`.lesson-mgr-tabs`), 목록 칸은 높이를 못 박아
      (`clamp(240px, 56vh, 520px)`) 탭을 바꿔도 창이 늘었다 줄었다 하지
      않습니다. 창 폭은 720px(줄마다 단추가 셋이라 620px에서는 제목이 일찍
      접혔습니다). 실측: 1400×900에서 창 720×666 · 목록 504px · 탭 92×30
      둘 다 같음 · 400px 폭에서도 가로 넘침 0.
    - 탭은 **주소로** 듭니다(`?panel=lessons&tab=projects&new=<원본 id>`) —
      '뒤로 가기'가 탭 단위로 돌고, `new`가 방금 만든 줄을 짚습니다.
    - 탭의 '＋ 새 프로젝트 만들기'는 이 창을 **닫고** 만들기 창을 엽니다
      (창 둘이 겹치지 않게). 만들지 않고 닫으면 그 탭으로 돌아갑니다
      (`creatingProject`가 `"tab"`/`"import"`로 어디서 열었는지 듭니다).
  - 탭에서 연 만들기 창은 원본을 만들고 곧바로 프로젝트 탭을 열어 그 줄을
    짚어 둡니다(`.is-new`).
    **안내 카드는 원본에 깔지 않습니다** — 카드는 반에 붙는 것이라 복사본이
    생길 때 함께 깔립니다(만들 때와 같은 모양으로 도착: 첫 활동만 열림 ·
    비공개 · 안내 카드 한 장).
  - **수업 편집의 '수업 프로젝트' 고르개 하나가 두 묶음(optgroup)입니다** —
    `우리 반 프로젝트`(고르면 곧바로 이 수업에 연결) · `외부 프로젝트`
    (아직 이 반에 없는 내 원본 + 원본 없는 옛 프로젝트).
    - **우리 반 프로젝트 중 수업 자료에 연결돼 있는 것은 줄 끝에
      `(연결됨)`**입니다(`linkedBoardIds` — `app/study/page.js`가 이미
      구독해 둔 수업 자료 목록으로 셉니다, 읽기 0). 판정은 LessonMode의
      `boardId`와 같습니다(이 반 키가 있으면 그 값, 없으면 옛 `boardId`가
      이 반 것일 때만). **지금 연결돼 있는가**를 보는 것이라, 연결했다가
      '연결 안 함'으로 비운 프로젝트는 표시가 사라집니다(연결 이력은
      남기지 않습니다).
      - **묶음을 셋으로 나누지 마세요.** 연결하는 순간 그 줄이 다른 묶음으로
        옮겨 가 방금 고른 것이 자리를 바꿉니다. 고른 뒤의 동작도 같습니다 —
        한 프로젝트를 여러 수업이 함께 가리킬 수 있고, 여기서 연결해도 다른
        수업의 연결은 그대로입니다.
    - 한때 둘째 묶음이 '프로젝트 가져오기' 단추 뒤의 **따로 선 고르개**였는데
      걷었습니다 — 두 목록이 같은 자리에 번갈아 서서 무엇이 어디 있는지 두 번
      찾아야 했고, 이 반에 복사본이 있는 원본은 두 목록에 같은 이름으로
      함께 섰습니다.
    - **둘째 묶음은 고르기만 하고, 아래 확인 줄의 '가져오기'를 눌러야
      만듭니다**(`importFrom` → `handleImport`). 닫힌 고르개에서 방향키를
      누르면 브라우저가 한 칸마다 change를 보내, 고르는 즉시 가져오면
      훑어 내려가기만 해도 이 반에 프로젝트가 줄줄이 생깁니다. 확인 줄이
      서 있는 동안은 '+ 새 프로젝트' 자리를 가져오기·취소가 차지하고, 그
      아래 한 줄이 무엇이 따라오는지 적습니다(`.lesson-board-import-note`).
    - **이 반에 복사본이 있는 원본, 이 반 프로젝트와 이름이 같은 원본·옛
      프로젝트는 둘째 묶음에 안 섭니다** — 첫째 묶음의 그것이 곧 그것이고,
      가져오면 같은 이름이 이 반에 둘이 됩니다. 이름은 `projectNameKey`로
      견줍니다(만들기 판정과 같은 기준).
  - **연결할 사본에 활동이 하나도 없으면 원본의 활동을 채웁니다**
    (`LessonMode`의 `pickBoard` — 첫째 묶음에서 원본의 복사본을 고를 때).
    원본에는 활동이 있는데 연결하면 '활동 없음'인 일이 실제로 있었습니다
    (사본이 어떻게 비었는지는 확인하지 못함). **빈 사본에만** 하므로 이
    반에서 활동을 고쳐 둔 사본은 안 건드리고, 잠금은 새로 시작할 때처럼
    첫 활동만 엽니다.
  - **한 반에 같은 원본을 두 번 열지 않습니다.** 프로젝트 탭은 이 반에 복사본이
    있으면 '연결된 프로젝트 · 열기'를 세우고, 수업 편집의 고르개는 그
    원본을 가져올 묶음에서 빼고 이 반 묶음의 복사본만 보여 줍니다. 판정은
    이 반 보드의 `templateId`. 휴지통에 든 복사본은 구독에서 빠지므로 그때는
    새로 열 수 있습니다.
  - **원본이 바뀌어도 이미 나간 복사본은 그대로**입니다. 학생 카드가 활동을
    자리로 읽고 쓰므로, 원본의 활동 순서가 이미 나간 반에 번지면 쓴 글이
    엉뚱한 칸으로 밀립니다. **원본을 지워도 복사본과 학생 카드는 남습니다** —
    되묻는 창이 그것을 밝힙니다.
  - **반 복사본에서 활동을 고치면 원본에도 적습니다**
    (`syncTemplateActivities` — 활동 목록만). 활동은 대개 반 복사본에서
    고치는데(수업 편집의 활동 칸 · 프로젝트의 활동 패널 · 순서 바꾸기 · 파이썬
    실행기의 '＋ 활동 추가'), 원본에 안 적으면
    수업 중에 이름만으로 만든 원본은 영영 활동이 비어 **다른 반에서 불러오면
    빈 프로젝트**가 옵니다. 바뀐 원본은 **다음에 불러오는 반부터** 적용되고,
    다른 반 복사본은 위 까닭으로 안 건드립니다. 원본 쓰기가 실패해도(남의
    원본·지워진 원본) 반 복사본 저장은 그대로입니다. 제목·안내는 안 맞춥니다.
    **활동을 고치는 자리를 새로 만들면 이 함수를 함께 부르세요.**
  - **원본을 곧바로 고치는 창**도 있습니다 — 프로젝트 탭의 원본 줄에서
    휴지통 왼쪽 **'편집'**(`StudyTemplateEditModal`). 제목 · 안내 · 활동 목록 ·
    연계(키워드 · 파이썬 실행기)를 고칩니다. 유형(개별/모둠)은 복사본 편집
    창과 같은 선으로 안 둡니다.
    - **왜**: 활동과 연계는 복사본을 거쳐 원본에 닿았지만 **제목·안내는 닿는
      길이 아예 없었고**, 아직 어느 반에도 안 가져온 원본은 거쳐 갈 복사본이
      없어 지우는 것 말고는 손댈 수 없었습니다.
    - **이미 가져간 반에는 번지지 않습니다**(위 '이미 나간 복사본은 그대로'와
      같은 까닭). 창 맨 위 한 줄(`.tpl-edit-scope`)이 이미 가져간 반 이름을
      적고 '다음에 가져오는 반부터'를 밝힙니다 — 이 반 복사본(`instanceOf`)과
      다른 반(`usedIn`)을 합쳐 셉니다(읽기 0).
    - **이름은 원본끼리 달라야 합니다** — 다른 원본과 `projectNameKey`가 같으면
      저장을 막고 칸 아래에 적습니다. 만드는 세 자리가 같은 이름을 막는 것과
      같은 까닭입니다.
    - 활동 칸·연계 칸은 만들기 창과 **같은 클래스·같은 조각**입니다.
    - **body에 포털로** 띄우고 z-index 150입니다(`.tpl-edit-backdrop`) — '수업
      관리' 창(100) 위, 되묻는 창(200) 아래. 배경을 눌러 닫아도 '수업 관리'
      창은 그대로 남습니다(실측).
    - **규칙을 안 건드립니다** — 원본은 만든 교사가 고칠 수 있고 `ownerId`만
      못 바꾸는데, 이 창은 그 칸을 안 보냅니다(`updateStudyTemplate`).
  - **앞으로 만드는 것부터**입니다. 이전에 만든 프로젝트에는 `templateId`가
    없고 자료를 옮기지 않았습니다. 가져오기 목록에서 그것들은 원본 뒤에
    **원본과 같은 모양으로 이름만** 섭니다(고르면 예전처럼 통째로 복제).
    원본을 지운 복사본도 그 자리로 갑니다(가리킬 원본이 없으므로).
    - 한때 `원본 없는 프로젝트 · 반이름`으로 반별로 묶었는데 걷었습니다 —
      앞으로는 다 원본이라 그 구분은 고를 때 쓸모가 없었고, 같은 프로젝트가
      반 수만큼 되풀이됐습니다(실제 신고).
    - **같은 이름은 한 줄로 접습니다**(`importOld`). 반마다 복제해 쓰던 것이라
      이름이 같으면 대개 같은 프로젝트입니다. 활동이 가장 많은 것(같으면
      최근 것)을 가져옵니다 — 학생 카드는 안 따라오므로 가져오는 것은 활동
      목록과 안내뿐입니다. 원본과 이름이 같은 옛 프로젝트도 뺍니다.
    - **이 반에 같은 이름이 이미 있는 옛 프로젝트도 뺍니다** — 그것은 같은
      고르개의 '우리 반 프로젝트' 묶음에서 연결하면 되고, 가져오면 같은
      이름이 이 반에 하나 더 생길 뿐입니다(두 목록에 같은 이름이 보이던
      실제 신고).
    - **옛 프로젝트를 원본으로 묶기** — 프로젝트 탭 목록 칸의 **첫 줄**
      (`LegacyProjectMigrate`, 묶을 것이 있을 때만 섭니다)에서 미리보기 창을
      열어, **이름이 같은 것끼리** 원본 하나에 잇습니다
      (`lib/projectNames.js`의 `groupLegacyProjects` · `migrateLegacyGroup`).
      - **지우거나 옮기는 것은 없습니다** — 각 반 프로젝트에 `templateId`
        한 칸만 적습니다. 학생 카드·반마다의 활동 목록·잠금은 그대로입니다.
      - 원본의 활동은 **활동이 가장 많은 사본**(같으면 최근 것) — 가져오기
        목록이 같은 이름을 접을 때와 같은 기준입니다. 반마다 활동 목록이
        다르면 창이 그 사실을 적습니다(이름만 같은 다른 프로젝트일 수 있음).
      - **묶음마다 체크**가 있어 뺄 수 있고, 뺀 것은 알림 줄에 남습니다(한쪽
        이름을 바꾼 뒤 다시 묶으면 됩니다). 한 반에만 있는 옛 프로젝트도 제
        원본을 하나씩 얻습니다.
      - 같은 이름의 원본이 이미 있으면 **새로 만들지 않고 그것에 잇습니다**
        (비어 있으면 활동을 채움) — 이름은 서로 달라야 합니다.
      - 대상은 **내가 만든, 보관 안 된 반**뿐입니다. 최고 관리자도 남의 반
        프로젝트를 제 원본에 묶지 않고, 보관된 반은 규칙(`ownsClassEditable`)이
        쓰기를 막습니다. 데모 모드만 화면에 보이는 반을 씁니다(접속마다 uid가
        새로 나와 '내가 만든 반'이 없으므로).
      - 묶음은 **한 개씩 차례로** 씁니다. 중간에 멈춰도 앞의 것은 이어진 채
        남고, 다시 열면 남은 것만 섭니다(이어진 것은 목록에서 빠지므로).
      - **규칙을 안 건드립니다** — 원본 만들기와 보드 고치기가 이미 담당
        교사에게 열려 있습니다. 읽는 문서도 안 늡니다(받아 둔 보드·원본).
  - **수업 중에 만드는 자리도 원본을 만듭니다** — 수업 편집의 '+ 새 프로젝트'와
    파이썬 실행기의 '＋ 새 프로젝트'가 `createStudyProjectInClass`로 **원본을
    만들고 곧바로 이 반에 불러옵니다**(누름은 지금까지처럼 한 번). 한때 이 두
    자리만 반에 곧바로 만들어, 가져오기 목록에 '원본 없는 프로젝트'가 계속
    생겨났습니다 — '새 프로젝트는 무조건 원본'이라는 규칙과 어긋났습니다.
    - **프로젝트 이름은 서로 달라야 합니다.** 만드는 세 자리(공부방 만들기
      창 · 수업 편집 · 파이썬 실행기)가 모두 만들기 **전에**
      `lib/projectNames.js`의 `findSameNameProject`로 같은 이름을 찾고, 있으면
      만들지 않고 `ProjectNameDupModal`을 띄웁니다 — '동일한 이름의
      프로젝트가 있어요 … 이전 프로젝트를 불러올까요?'.
      - 찾는 차례: 이 반의 프로젝트 → 내 원본 → 다른 반의 원본 없는 옛
        프로젝트. 이름은 앞뒤 공백을 걷고 안쪽 공백을 하나로 봅니다.
      - 단추는 **취소**(창만 닫고 이름 칸으로 — 초점·선택이 이름 칸에 돌아감)와
        **이전 프로젝트 불러오기**(`loadSameNameProject` — 이 반 것은 그대로,
        원본은 이 반에서 시작, 옛 것은 복제) 둘뿐입니다. 한때 수업 편집에만
        노란 안내 줄과 '그래도 만들기'가 있었는데 걷었습니다 — 같은 이름의
        원본이 둘이면 목록에서 가를 수 없습니다.
      - 두 단추는 **같은 크기 · 같은 색**(`btn-primary` 둘, 격자 `1fr 1fr`).
        처음 초점은 단추가 아니라 **창 자체**입니다 — 이름 칸에서 Enter로
        열리면 초점이 간 단추에만 테두리가 생겨 둘이 달라 보였습니다(실측).
      - 창은 **body에 포털로** 띄우고 z-index 3020입니다 — 수업 편집(2900)
        위에 떠야 하고, 실행기 서랍은 transform이 있어 그 안의 fixed가 서랍
        기준으로 바뀝니다. 배경이 `.modal-backdrop`이라 실행기의 '바깥 누르면
        닫기'도 비켜 갑니다. Esc는 캡처 단계에서 받아 뒤 화면에 안 넘깁니다.
      - **세 자리가 같은 판정을 써야 합니다** — 한 자리만 막으면 다른 자리로
        같은 이름이 새어 들어옵니다. 실행기는 그래서 `templates`를
        `PythonRunner`를 거쳐 받습니다.
    - 활동이 없는 원본을 불러올 때는 **안내 카드를 안 깝니다** — 빈 '안내'
      카드만 격자 맨 앞에 섭니다(예전 '+ 새 프로젝트'도 그랬습니다).
    - 컴포넌트에서 `addStudyBoard`를 직접 부르는 자리는 이제 없습니다.
    `duplicateStudyBoard`(다른 반으로 복제)는 `templateId`를 함께 옮깁니다 —
    같은 원본의 복사본이라는 사실은 반이 바뀌어도 그대로라서요.
  - 읽기는 `subscribeMyStudyTemplates`(`ownerId` 등호 하나 + 화면 정렬 —
    **만든 차례**, 오래된 것이 앞). 프로젝트 탭과 가져오기 목록이
    **같은 구독**을 나눠 씁니다. '다른 반에서 쓰는 중'은 이미 받아 둔 보드
    목록으로 셉니다(읽기 0).
  - 규칙은 수업 자료와 같습니다 — 만든 교사만 읽고 고치고 지웁니다(관리자는
    읽기·고치기·지우기). 목록은 `where ownerId == 나`여야 통과하고,
    `ownerId`는 못 바꿉니다. `studyBoards` 규칙은 **안 건드렸습니다**(필드
    목록을 못 박아 두지 않아 `templateId`가 그대로 통과). 시험은
    `tests/rules/studyTemplates.test.mjs`(17개).
  - **코드보다 규칙을 먼저 배포하세요**(`npm run deploy -- --rules-only`).
    순서가 뒤집히면 프로젝트 만들기가 권한 오류로 실패합니다.
  - 책방 독서 활동도 같은 길로 따라올 예정입니다(아직 반에 묶여 있음).
- `studyBoards/{boardId}/cards` — 학생 **개인 카드** 서브컬렉션 (boardId, authorId, authorName, authorEmoji)
  - 문서 ID = 작성자 uid → 프로젝트당 카드 1개 보장. 전체 조회는 `collectionGroup("cards")` 사용
  - 카드 문서는 **학생이 처음 저장할 때** 생깁니다(규칙이 `authorId == 본인`을
    요구해 교사가 대신 못 만듦). 화면에서는 명단 기준으로 '빈 자리'를 미리
    깔아 두어(`StudyProjectView`의 seats) 카드가 이미 있는 것처럼 보입니다.
  - (데모 모드 mock은 평면 배열 `mock.studyCards`로 흉내 — Firebase는 서브컬렉션)
  - **교사 '안내' 카드** — 프로젝트를 만들 때 한 장 같이 깔립니다
    (`StudyProjectForm`이 `buildActivityTemplate(acts)`로 활동 틀을 채워 둡니다).
    격자 맨 앞자리에 서서 예시·자료 노릇을 하고, 교사는 설정(⚙)의
    '＋ 카드 추가'로 언제든 더 만들 수 있습니다. 교사 카드만 **자동 ID**라
    한 프로젝트에 여러 장이 될 수 있습니다(학생은 문서 ID = uid로 한 장).
    - **판정은 `lib/activities.js`의 `isTeacherAuthoredCard` 한 곳입니다.**
      `authorId`가 `teacher_`로 시작하거나 `authorName === "선생님"`인지를
      봅니다 — 앞의 것은 **데모 모드의 uid 규칙**이라, 실서비스 교사 uid는
      평범한 Firebase uid입니다. 그래서 **접두만 보면 실서비스에서 통째로
      샙니다.** 이름 쪽이 실제로 걸리는 조건이고, 교사 표시 이름은
      `lib/auth.js`가 프로필을 '선생님'으로 교정해 늘 이 값입니다.
    - **같은 식을 다른 파일에 베껴 쓰지 마세요.** 실제로 다섯 군데에 복제돼
      있었고 그중 넷이 접두만 봐서 실서비스에서 안내 카드가 학생으로
      세어졌습니다 — `LessonMode`(활동 편집 차단) · `/admin`의
      `classParticipantIds`·`classCards` · `StudyRoomStats`의 제출률.
    - 그중 `LessonMode`는 **눈에 보이는 고장**이었습니다: 활동을 바꾸기 전에
      '학생이 이미 쓴 내용'이 있는지 보는데, 안내 카드 본문이 활동 이름을
      담고 있어 늘 걸렸습니다. 그래서 **아무도 안 썼는데도 교사가 활동
      목록을 영영 못 고쳤습니다.**
    - KWLS 기록은 `userId`만 있고 작성자 이름이 없어 그쪽은 접두로만 봅니다
      (`StudyRoomStats`의 `isStudent`) — 교사는 KWLS를 안 써서 걸릴 일이
      없는 자리입니다.
    - 화면에는 **'안내' 배지**(`.study-card-guide`)를 답니다. 집계에서 빠지는
      것과 **화면에서 그렇게 보이는 것은 별개**라, 이름('선생님')만 있으면
      학생 카드 격자에서 '선생님이 카드를 냈다'로 읽힙니다(실제 오류 신고).
    - **복제할 때도 함께 갑니다**(`duplicateStudyBoard` → `copyGuideCards`).
      초기화되는 것은 '학생 기록'이지 교사가 만든 것이 아닙니다 — 활동 목록과
      공개 범위를 그대로 가져가는 것과 같은 갈래입니다. 예전에는 카드 서브
      컬렉션을 통째로 건너뛰어 **복제한 반에만 안내 카드가 없었습니다**(실제
      신고 — `activityLocks`에서 겪은 '어느 길로 왔느냐에 따라 다르게 도착'과
      같은 문제).
      - **작성자를 복제하는 교사로 다시 적습니다.** 규칙이
        `request.resource.data.authorId == uid()`를 **교사·관리자에게도 예외
        없이** 요구해, 원본을 그대로 옮기면 다른 교사가 만든 안내 카드일 때
        거부됩니다. 표시 이름은 늘 '선생님'이라 화면은 그대로입니다.
      - **글만 옮기고 첨부는 안 옮깁니다.** 보드를 지우면 그 카드의 첨부를
        함께 지우므로(`purgeClass`), 참조만 복사해 두면 **원본 반을 지우는
        순간 새 반의 링크가 깨집니다.** 보드의 학습 자료도 복제에서 함께 안
        옮기고 있어 결이 같습니다.
      - 원본에 교사 카드가 없으면(안내 카드가 생기기 전에 만든 프로젝트,
        또는 교사가 지운 경우) **새로 한 장** 깝니다 — 만들 때와 같은 모습으로
        도착시킵니다.
- `kwl` — **KWLS 기록** (classId, userId, date, answers{know,want,learned,still}
  + 옛 K/W/L/S 필드) — 문서 ID 고정 upsert (append 아님)
  - 공부방 하루 성찰: ID = `uid_classId_date`
  - 책방 KWLS 활동: ID = `uid_classId_act_활동id` + `activityId`·`topic`.
    책방 원본은 `bookActivities/{id}/entries/{uid}` 에 그대로 두고 여기에도
    적습니다 — 관찰 화면(히트맵·리포트·교사 패널)이 전부 이 컬렉션을 보므로
    이것만으로 두 곳의 KWLS가 한 흐름이 됩니다. 과거분은
    `npm run kwls:backfill` 로 옮깁니다(기본은 미리보기, `--apply` 로 실제 쓰기)
  - **종합 격자**(`KwlClassGrid`, `/admin` 「종합 분석」 맨 아래) — 세로 학생 ·
    가로 'KWLS를 쓴 날'. 칸 색은 그날 채운 칸 수(1~4)이고, 램프는 학기
    히트맵의 밝은 바탕용(`.kwls-hm--light`)과 **같은 값**입니다 — 같은 것을
    세는 두 격자가 다른 색을 쓰면 한쪽을 보고 다른 쪽을 오해합니다.
    - 가로축을 '수업일 전부'가 아니라 **기록이 있는 날**로 세웁니다. 아무도
      안 쓴 날은 빈 열일 뿐인데 격자만 가로로 길어집니다(출석부 내보내기가
      '기록이 있는 날짜만 열로' 하는 것과 같은 생각).
    - 세는 단위는 건수가 아니라 **칸**입니다. 한 학생이 같은 날 두 건을 쓸 수
      있어(공부방 하루 성찰 + 책방 KWLS 활동) 건수로 세면 두 곳에 쓴 학생이
      네 칸을 다 채운 학생보다 진해집니다. 그날 것을 합쳐 K·W·L·S 중 몇 칸이
      찼나로 셉니다.
    - **읽기가 늘지 않습니다** — 그 화면이 이미 들고 있는 `classKwl` 배열을
      그대로 받습니다(`KwlOutcome`·`ParticipationBreadth`와 같은 자료).
    - 칸을 누르면 `KwlFullscreenModal`이 그 날짜·그 학생으로 열립니다. 그
      창의 학생 드롭다운(`initialUid`·`roster`)은 **날짜를 넘겨도 유지**되어,
      좌우 화살표로 한 학생의 흐름을 이어 볼 수 있습니다. 그 날 안 썼으면
      '누가 언제' 안 썼는지 적힌 빈 상태가 뜹니다.
    - 격자가 이미 센 '어느 날에 누가 썼나'를 `dateIndex`
      (`{ counts, byUid }`)로 함께 넘깁니다 — 그 창의 달력·화살표가 같은
      질의를 다시 하지 않게. 셈은 **격자가 칸을 칠하는 기준과 같아야**
      합니다(한 칸이라도 채운 날만) — 다르면 색이 없는 칸으로 화살표가
      내려앉습니다.

  **크게 보기 창의 좌우 화살표는 '기록이 있는 다음 날'로 건너뜁니다**
  (`KwlFullscreenModal`). 하루씩 옮기면 주말·수업이 없던 날이 그대로 걸려
  빈 화면을 몇 번씩 지나야 했습니다(한 주 쉬면 일곱 번). 달력에서 초록 칸만
  골라 누르는 것과 같은 움직임입니다.
  - **한 학생만 보는 중이면 그 학생이 쓴 날로** 갑니다. 반 전체 날짜로
    옮기면 다른 학생이 쓴 날에 내려앉아 여전히 빈 화면입니다.
  - 더 갈 곳이 없으면 화살표가 꺼지고, 툴팁이 **갈 날짜를 미리** 적습니다.
    아직 목록을 안 읽었을 때만은 꺼 두지 않습니다 — 누르는 것이 곧 읽어
    오는 일이라, 미리 꺼 두면 고장으로 보입니다.
  - 그 목록은 달력이 쓰는 요약과 **같은 읽기 한 번**에서 나옵니다
    (`fetchKwlDays`·`fetchKwlUserDays`가 한 캐시를 나눠 쓰고, 같은 순간에
    둘을 물어도 질의는 하나입니다 — 진행 중인 약속을 붙잡아 둡니다).
    달력을 열거나 화살표를 처음 누를 때 비로소 읽습니다.
  - 그 요약이 세는 것도 **'문서가 있나'가 아니라 '한 칸이라도 썼나'**입니다.
    빈 문서가 남은 날을 세면 달력이 초록인데 열어 보면 아무것도 없습니다.
- `bookActivities/{id}.sectionLocks` — **곁텍스트 읽기의 단계별 열기**
  `{ [단계 key]: true }`(true = 잠김). 교사가 수업 진도에 맞춰 한 단계씩
  열어 줍니다 — 공부방의 `activityLocks`와 같은 생각입니다. 공부방은 활동
  이름이 바뀌고 순서가 움직여 배열이지만, 여기 여덟 단계는 `lib/paratext.js`에
  못 박혀 있어 key 맵입니다(답도 key로 저장하므로 나중에 단계를 끼워 넣어도
  어긋나지 않습니다).
  - **키가 없으면 잠김.** 만들 때 1단계만 열어 둡니다(`initialSectionLocks`).
  - **규칙에는 넣지 않습니다.** 여덟 단계의 답이 `entries/{uid}` 한 문서 한
    맵에 다 들어 있어, 규칙이 단계별로 판정하려면 필드 이름 15개를 규칙
    파일에 베껴 두고 손으로 맞춰야 합니다. 화면에서만 막습니다(공부방도
    같습니다). 활동 전체를 잠그는 `locked`는 그대로 규칙이 막습니다 —
    '수업 끝, 이제 아무도 못 고침'은 여전히 규칙이 보장합니다.
  - 헬퍼는 전부 `lib/paratext.js`에 있습니다(`isSectionLocked` ·
    `sectionLocksWith` · `sectionLocksUpTo`).
- `bookActivities/{id}.deleted` · `.deletedAt` · `.deletedBy` — **휴지통**
  활동 삭제는 곧바로 지우지 않고 이 표시만 찍습니다(`deleteBookActivity`).
  목록·학생 화면·통계에서 사라지지만 자료는 그대로라 `restoreBookActivity`로
  돌아옵니다. 진짜로 없애는 것은 `purgeBookActivity` — 휴지통 안에서 한 번 더
  확인하고 부릅니다.
  - **휴지통 비우기**는 머리줄 오른쪽 끝(`.book-trash-empty`). 되묻는 창에
    개수와 '무엇이 함께 지워지는가'를 밝히고, 동의하면 `purgeBookActivity`를
    **한 건씩 차례로** 부릅니다(`Promise.all` 아님) — 활동 하나가 하위
    컬렉션까지 훑는 일이라 스무 개를 한꺼번에 던지면 쓰기가 한 순간에
    몰립니다. 중간에 멈추면 앞의 것은 지워진 채이므로 '몇 개까지 지웠는지'를
    알림에 적습니다.
  - 접혀 있어도 그 단추는 보입니다 — 되묻는 창이 몇 개를 지우는지 먼저
    말하므로, 안 펴 봤다고 잘못 지워지지 않습니다.
  - **공부방 휴지통**(`StudyProjectDashboard`)에도 같은 단추가 있습니다 —
    같은 모양·같은 창·같은 '한 건씩 차례로'. 다만 **펼쳤을 때만** 답니다:
    그 휴지통은 열 때 비로소 읽어 오므로(`fetchTrashedStudyBoards`) 접혀
    있는 동안은 몇 개인지 몰라, 되묻는 창이 개수를 말해 줄 수 없습니다.
  - 예전에는 여기서 낱말·모둠·기록을 곧바로 지웠는데, 손이 미끄러져 한 반의
    활동이 통째로 날아간 일이 있었습니다. 앱 안에 되돌릴 길이 없어 Firestore
    시점 복구까지 갔지만 PITR이 꺼져 있어 보존 창이 1시간뿐이었고, 그 사이에
    지난 뒤라 못 살렸습니다. (그 뒤 PITR을 켜 두어 지금은 7일입니다.
    급할 때 쓰는 도구: `npm run books:rescue` — `functions/scripts/rescue-activity.mjs`)
  - **mock은 활동 문서를 갈아 끼웁니다**(제자리에서 고치지 않고 —
    `updateBookActivity`·`deleteBookActivity`·`restoreBookActivity`).
    Firestore는 읽을 때마다 새 객체를 주는데 mock이 같은 객체를 고쳐 두면,
    그 문서를 들고 있는 쪽이 늘 같은 참조를 받아 React가 다시 그리지
    않습니다. 데모 모드에서 교사가 곁텍스트 단계를 열어도 학생 서랍이 잠긴
    채로 있던 일이 실제로 있었습니다(반 문서에서 겪은 그 함정과 같습니다).
  - **판정은 `deleted`(참·거짓)로 하세요.** `deletedAt`은 '언제'만 적어 둔
    것입니다 — `serverTimestamp()`는 서버가 답하기 전까지 화면에 null로 와서,
    그것만 보고 거르면 지운 활동이 한 박자 되살아났다가 사라집니다.
  - 거르는 자리는 화면입니다(질의에 조건을 붙이면 복합 색인이 필요).
    `app/books/page.js`와 `components/BookActivityStats.jsx` 두 곳.
- `bookActivities/{id}/groups/{gId}/words` — 책방 닿소리 낱말
  - `groupMode: 'solo'`(개별 활동)는 **구성원이 한 명뿐인 판**을 학생마다 하나씩
    둡니다. 새 컬렉션을 만들지 않아 학생 화면·교사 전체 보기·낱말 권한
    (`isMyBookGroup`)이 그대로 쓰입니다.
  - **활동을 시작한 뒤 들어온 학생에게는 판이 없습니다.** 판은 만들 때의
    명단으로 한 번에 찍히기 때문입니다(`createBookActivity`의 `soloRows`).
    교사 화면 왼쪽 목록 맨 위에 `판이 없는 학생 n명`이 뜨고, 눌러서 만듭니다
    (`.book-rail-missing`).
    - **저절로 만들지 않습니다.** 교사가 화면을 열 때마다 쓰기가 일어나고,
      반 편성을 손보는 동안 빈 판이 조용히 쌓입니다. 드문 일이라 교사가 보고
      누르는 편이 '언제 무엇이 생겼는지' 분명합니다.
    - **`composeBookGroups`를 쓰지 마세요 — `addSoloBookBoards`가 따로 있습니다.**
      그 함수는 넘긴 목록이 곧 전부라 목록에 없는 판을 물리고(retired), 문서
      id를 **`group_{index}`로 가정해** 씁니다. 그런데 `npm run books:to-solo`로
      옮긴 활동의 판은 id가 **`solo_<uid>`**입니다 — 거기서 그 함수를 부르면
      빈 `group_N`들이 새로 생기고 멀쩡한 `solo_*` 판이 전부 물러나, 학생들의
      낱말이 화면에서 통째로 사라집니다(문서가 지워지는 것은 아니지만 앱
      안에 되돌릴 길이 없습니다). 실제로 그렇게 짰다가 고쳤습니다.
    - `addSoloBookBoards`는 **있는 판을 건드리지 않고 없는 것만 더합니다** —
      물릴 일도, 남의 id를 넘겨짚을 일도 없습니다. 새 판 id는 `solo_<uid>`라
      `group_N`과도 서로와도 안 부딪히고, 그 id가 이미 있으면(반에서 뺐다가
      다시 넣은 학생) **되살립니다** — 새로 만들면 그 학생이 전에 넣은 낱말이
      묻힙니다. 번호는 **물린 판까지 세어** 매깁니다(살아 있는 것만 세면 물린
      판과 같은 번호가 나와 되살릴 때 겹칩니다).
      실측: `solo_*` 활동에서 판 4 → 6개, 낱말이 있던 두 판은 그대로.
    - 판이 **하나도 없을 때도** 같은 줄이 뜹니다 — 활동을 만든 뒤 반이 통째로
      들어온 경우에는 목록 자체가 안 그려지는데 그때가 가장 필요합니다.
    - **규칙은 안 건드립니다** — `groups`의 create가 이미 담당 교사에게
      열려 있습니다(`ownsBookActivityEditable`).
  - 이미 모둠으로 진행한 활동을 개별로 바꾸려면 `npm run books:to-solo`
    (인자 없이 실행하면 활동 목록, `<활동id>`로 미리보기, `--apply`로 실제).
    낱말 생성 규칙이 `authorId == 본인`이라 교사가 남의 낱말을 옮겨 적을 수
    없어서, 앱이 아니라 admin SDK 스크립트로 처리합니다.
  - **모둠원 이름 칩 줄 맨 앞에 낱말 입력칸 하나**(`.consonant-quick`,
    학생만). 적으면 **첫 글자의 초성**으로 갈 칸을 스스로 골라 넣습니다 —
    열네 칸을 눈으로 훑어 ＋를 찾는 일이 낱말마다 되풀이되던 것이 한 번으로
    줄어듭니다.
    - **제 줄을 두지 마세요.** 판(`.consonant-grid`)이 남는 높이를 다 쓰므로
      줄이 하나 늘면 그만큼 판이 낮아집니다. 이미 있는 이름 칩 줄
      (`.canvas-legend`)에 얹습니다. 개별 활동은 이름 칩이 없는데, 그때도
      이 줄이 뜨도록 렌더 조건이 `legend.length > 0 || canWrite`입니다.
    - 입력칸 너비는 **판의 한 칸과 같습니다** — `.consonant-quick`에
      `calc((100% - 32px) / 5 + 91px)`. 판이 5칸 gap 8px이고 이 줄은 판과
      폭이 같으므로 한 칸이 `(100% - 32px)/5`이고(이 묶음이 flex 아이템이라
      100%가 줄 폭으로 풀립니다), 91px은 입력칸이 아닌 부분입니다
      (→ 슬롯 30 + gap 6·6 + '넣기' 49 — 실측). 창 크기가 달라져도 아래
      첫 칸과 좌우 끝이 딱 맞습니다(1400/1100/900px에서 오차 0~1px).
      단추 글자나 여백을 고치면 이 91px도 함께 재세요.
    - 못 넣은 까닭은 줄 **아래에 떠서**(absolute) 잠깐 머물다 사라집니다.
      줄 안에 두면 글자가 길어 이름 칩이 다음 줄로 밀리고, 그만큼 판이
      내려앉습니다(`--canvas-lead-h`가 곧 판이 시작하는 자리입니다).
    - 셈은 `lib/korean.js`의 `initialJamoOf`(음절 코드 ÷ 588 = 초성 차례,
      조사 고르기와 같은 셈) → `lib/consonants.js`의 `cellIndexOfWord`
      (자모 → 칸). **쌍자음은 제 칸이 없고 홑자음 칸에** 담깁니다
      (까치→ㄱ · 땅→ㄷ · 쓰레기→ㅅ · 짜장→ㅈ) — 격자가 원래 그렇습니다.
    - **칸마다 있는 ＋를 없애지 마세요.** 한글로 시작하지 않는 낱말
      (AI·CO₂)과 '고른 칸이 마음에 안 들 때'는 그 길로만 넣습니다.
    - 넣기 전에 `→ ㄱ 칸`을 미리 보여 주고, 넣은 뒤에는 그 칸이 잠깐
      밝아집니다(`.consonant-cell.flash`). 판이 3×5라 낱말이 어디에
      떨어졌는지 눈으로 좇기 어렵습니다. 같은 칸에 연달아 넣어도 다시
      밝아지도록 `{ i, n }`으로 번호를 함께 듭니다(같은 값이면 상태가
      안 바뀌어 타이머가 다시 안 걸립니다).
    - 한글로 시작하지 않으면 **넣지 않고** 까닭을 적습니다 — 엉뚱한 칸에
      넣는 것보다 낫습니다. 같은 낱말을 두 번 넣는 것도 여기서만 막습니다
      (빠르게 적는 칸이라 실제로 잦고, 칸을 눌러 넣던 때보다 알아채기
      어렵습니다).
  - 개별 활동은 **주제어를 판(group)마다 따로** 둡니다(`groups/{gId}.topic`).
    읽는 책이 저마다 다를 수 있어 교사는 활동의 `topic`을 비워 두고, 학생이
    자기 판 한가운데를 두 번 눌러 적습니다. 화면은 `group.topic || activity.topic`
    순으로 씁니다. 규칙은 개별 활동에 한해 그 판의 구성원에게 `topic` **한
    필드만** 허용합니다(`changedOnly(['topic'])` · 40자 · 잠기면 불가).
  - **한 학생만 보기는 판 위의 모둠원 칩**(`ConsonantCanvas`의 범례)으로
    고릅니다. 누르면 그 학생 낱말만 남고('내 판'의 거르기와 같은 방식),
    위의 '모둠 전체 보기'로 돌아옵니다. 한 사람만 보는 중에는 **칩 줄을 통째로
    감춥니다** — 무엇을 보는 중인지는 위 한 줄이 말합니다.
    한때 왼쪽 모둠 카드 안에도 이름 단추를 뒀는데, 모둠을 고르는 것인지 학생을
    고르는 것인지 구분이 어려웠습니다. 이름표가 이미 판 위에 색과 함께 있으니
    그것을 그대로 단추로 씁니다(교사 화면에서만 — 학생에게는 이름표 그대로).
    고름은 캔버스 안 state이고, 부모에는 `onFocusChange`로만 알립니다
    (오른쪽 진행 패널이 그 학생 줄을 함께 짚는 데 씁니다).
  - **'모둠원별 진행'의 14칸은 진하기를 쓰지 않습니다** — 채웠으면 사람 색,
    안 채웠으면 회색 하나뿐입니다. 낱말 수를 진하기로 나타내면 한 개 넣은
    칸이 옅어 안 채운 칸과 헷갈립니다. 14칸을 다 채우면 마지막 칸에 붉은
    점(`.dash-heat-cell.is-done`) — 손들기 표시(`.question-signal-dot`)와 같은
    색·크기라 '여기 봐 달라'는 신호가 화면이 달라도 하나로 읽힙니다.
    **모둠별 진행 대시보드(`ConsonantDashboard`)는 그대로 진하기를 씁니다** —
    거기서는 '어디에 얼마나 모였나'가 그 패널의 주제라 뜻이 다릅니다.
  - 개별 활동의 판은 한 사람짜리라 '모둠 안 자리'로 색을 매기면 모두 첫 색
    하나가 됩니다. 그래서 낱말에는 사람 색을 입히지 않고(범례도 없음),
    '학생별 진행' 패널만 줄 번호로 색을 줍니다(`lib/bookColors.js`의
    `ROW_COLORS` 10색 되풀이 — 붙어 있는 줄끼리만 다르면 충분).
  - 학생이 채우는 판(`.canvas-main:not(.dash-root)` / `.canvas-embed` 안의
    `.consonant-*`)은 연한 초록. 전체 보기 집계(ConsonantDashboard)도 뿌리가
    `.canvas-main`이라 `:not(.dash-root)`로 빼 둡니다 — 거기는 낱말마다 '넣은
    사람 색'이 붙는 화면이고, 끼워 넣은 형태(`.dash-embed`)와 색이 갈리면 같은
    화면이 두 얼굴이 됩니다. 중계 화면(`.cast-grid`)도 그대로. 판 색을 건드릴
    땐 **반드시 이 두 뿌리 아래로 한정**할 것.
  - **'전체 보기' 격자는 칸마다 낱말 다섯 개만** 보여 줍니다(`TopWords`).
    예전에는 모든 낱말을 나온 횟수만큼 반복해 늘어놓아 '줄 길이 = 언급 횟수'로
    읽혔는데, 낱말이 500개가 되자 폭 62px 고정 때문에 글자가 잘리고 칸마다
    안쪽 스크롤이 생겨 대부분이 안 보였습니다(칠판에서는 굴릴 수도 없습니다).
    지금은 반복 대신 `×3` 배지를 쓰고, 순서는 **많이 나온 순 → 같으면 먼저
    채운 순**입니다(가나다순은 수업 흐름과 무관해 씁니다). 나머지는 '+29개'로
    접고, 칸을 누르면 뜨는 크게 보기에 모둠 색과 함께 전부 있습니다.
    중계 화면(`PresentationOverlay`의 `CastTopWords`)도 같은 모습으로 맞춥니다 —
    한쪽만 바꾸면 교사 화면과 학생 화면이 달라집니다.
  - **'전체 보기'는 잔디 / 격자 / 낱말 구름 세 얼굴**입니다(`.dash-view-tabs`).
    탭이 선 차례는 **수업 중에 자주 여는 것부터**입니다 — 잔디('누가 어디까지
    채웠나')가 맨 앞이고, 낱말을 보는 둘이 뒤에 섭니다. 차례와 **처음 여는
    얼굴은 별개**입니다(`view`의 첫 값은 그대로 `grid`).
    `.dash-view-tab`에는 자리로 거는 규칙(`:first-child` 따위)이 없어 —
    탭마다 제 알약입니다 — 차례를 바꿀 때 CSS는 안 건드립니다.
    격자는 첫 글자로 나뉜 자리라 '어떤 낱말이 많이 나왔나'를 보려면 칸을
    하나씩 짚어야 합니다. 구름은 칸을 지우고 낱말만 남겨 그것을 한 화면에
    보여 줍니다. **이미 만든 `merged`를 다시 펴는 것뿐이라 읽는 문서가 하나도
    안 늡니다.**
    - 자리 잡기는 `lib/wordCloud.js`(순수 계산) — 큰 낱말부터 가운데에 놓고
      나선으로 밀려 나가며 빈틈을 찾습니다. 라이브러리를 쓰지 않습니다.
      판 넓이에 맞춰 글자 크기를 통째로 정하고, 하나라도 자리를 못 찾으면
      조금 줄여 다시 합니다 — 그래서 **판이 넓을수록 글자가 커집니다**.
    - **크기는 나온 횟수로만** 정합니다(넓이가 횟수에 비례하도록 제곱근).
      순위로 정하면 다 한 번씩 나온 낱말 사이에도 크기 차이가 생겨, 없는
      차이를 있는 것처럼 보여 줍니다. 다 같이 한 번씩이면 다 같은 크기가
      맞습니다.
    - **상위 70개**(`CLOUD_TOP_N`). 500개를 다 그리면 대부분이 읽을 수 없는
      점이 됩니다. 고르는 순서는 격자와 **같아야 합니다** — 다르면 격자에
      큰 낱말이 구름에 없는 일이 생깁니다.
    - **색은 위 다섯에만**(`accentCount`), 나머지는 먹빛으로 크기에 따라
      짙기를 줍니다(`restGray`). 낱말마다 모둠 색을 입혀 봤더니 일흔 개가
      저마다 다른 색이라 '어느 색이 많은가'를 눈이 먼저 세게 되어, 정작
      '무엇이 많이 나왔나'가 묻혔습니다. 모둠은 격자와 크게 보기가 색으로
      말해 줍니다. 다섯을 그냥 앞에서 세지 않고 **6위보다 확실히 많이 나온
      낱말에만** 칠합니다 — 다 한 번씩 나온 판에서 아무 낱말 다섯에 색이
      붙으면 없는 사실을 말하게 됩니다(다 같으면 하나도 안 칠함).
    - 글자 굵기는 `WordCloud.jsx`의 `WEIGHT`와 `.cloud-word`의 `font-weight`가
      **반드시 같아야** 합니다 — 폭을 재는 canvas와 실제 글자가 어긋나면
      낱말끼리 겹칩니다(`letter-spacing`은 음수만 — 좁아지는 쪽은 안전).
    - 중계할 때는 **고른 70개를 그대로 실어 보냅니다**(`cloud`·`cloudRest`).
      학생 쪽에서 다시 고르게 하면, 정렬에 쓰는 `firstAt`이 방송 문서에 없어
      다른 70개가 뽑힙니다. `view`로 격자/구름을 함께 바꿉니다.
    - 낱말이 들어올 때마다 다시 짜면 화면이 쉬지 않고 뒤척입니다 — 처음
      한 번은 곧바로, 그 뒤로는 0.9초 모았다가 한 번만(자리 이동은 CSS
      transition으로 미끄러지게).
  - **잔디**(`ConsonantGrassView`)는 앞의 둘과 묻는 것이 다릅니다 — 격자·구름이
    '무슨 낱말이 나왔나'라면 여기는 **'누가 어디까지 채웠나'**입니다.
    한 줄이 닿소리 하나, 한 칸이 학생 한 명입니다(세로 14줄 × 가로 반 전체).
    - **전광판의 격자를 그대로 씁니다**(`.progress-grass`·`.grass-*`) — 공부방
      전광판 · 책방 전광판과 같은 모양이라 교사가 세 번 익히지 않습니다.
      다만 **모달이 아니라 그 자리에** 그립니다: 낱말을 넣는 학생 화면과
      나란히 두고 수업 내내 보는 자리라, 열고 닫는 동작이 끼면 그때마다
      격자가 사라집니다.
    - **칸 색은 둘뿐 — 진하기를 쓰지 않습니다.** 채웠으면 초록, 안 채웠으면
      회색입니다. 낱말 수를 진하기로 나타내면 한 개 넣은 칸이 옅어 안 채운
      칸과 헷갈립니다('모둠원별 진행'의 14칸에서 이미 같은 이유로 걷어 냈습니다).
      몇 개 넣었는지는 툴팁이 말합니다. 같은 화면의 '모둠별 진행'이 진하기를
      그대로 쓰는 것과 어긋나지 않습니다 — 거기서 묻는 것은 '어디에 얼마나
      모였나'라 뜻이 다릅니다.
    - **14칸을 다 채운 학생은 이름 위에 붉은 점**(`.grass-name.is-done`).
      다 채운 줄은 초록이 열넷이라 옆줄과 잘 안 갈려, 세로 줄을 하나씩 눈으로
      훑어야 '누가 끝냈나'를 알 수 있었습니다. 색은 손들기 표시
      (`.question-signal-dot`) · '모둠원별 진행'의 완료 점
      (`.dash-heat-cell.is-done`)과 **같은 값**입니다 — 화면이 달라도
      '여기 봐 달라'는 신호가 하나로 읽혀야 합니다.
      - **자리는 이 화면의 모든 이름이 비워 둡니다**(`.dash-grass .grass-name`의
        `padding-top`). 점이 붙는 이름에만 여백을 주면 그 이름만 내려앉아
        스물몇 개가 늘어선 줄이 들쭉날쭉해집니다.
      - **`.grass-name`에 직접 주지 마세요** — 책방 전광판
        (`BookProgressBoard`)이 같은 클래스를 씁니다. 거기는 '다 채움'이라는
        개념이 없어(줄마다 제 상태가 따로) 점이 영영 안 붙는데 이름만
        내려앉고 글자 자리가 줄어듭니다.
      - **흰 테두리를 두르지 않습니다.** 저 둘은 색칠한 칸 위에 얹히는 점이라
        흰 고리가 '띄운 자리'로 읽히지만, 여기는 크림색 바탕 위라 고리가
        그대로 흰 무리로 보입니다.
      - **`position: relative`를 덧대지 마세요.** `.grass-name`은 `sticky`라
        그것만으로 이미 점의 기준입니다. 덧대면 sticky가 풀려 세로로 구를 때
        이름 줄이 위에 안 붙습니다.
      - 세는 기준은 머리말의 '다 채운 학생 n/N'과 **같은 칸 기준**입니다
        (낱말 수가 아니라 '몇 칸을 채웠나').
    - **학생 화면에 중계하지 않습니다.** 방송 꾸러미에서 `view`가 `grass`이면
      `grid`로 바꿔 보냅니다 — 그 격자는 '누가 아직 안 했나'를 이름과 함께
      내거는 것이라 칠판에 띄우면 못 따라온 학생이 그대로 드러납니다.
    - 차례는 **학번순**입니다(자리표·출석부와 같은 기준). 모둠끼리 붙여
      세우면 색 띠는 고와지지만, 여기서 찾는 것은 '누가 안 했나'라 이름으로
      짚는 편이 빠릅니다.
    - **읽는 문서가 안 늡니다** — 이미 구독해 둔 모둠·낱말로 셉니다. 머리말의
      '다 채운 학생 n/N'도 **같은 `students` 배열에서** 셉니다(따로 세면
      한쪽만 고쳤을 때 격자는 다 찼는데 머리말은 덜 찼다고 말합니다).
    - 뿌리 클래스는 **`.dash-grass`**입니다. `.consonant-progress`·
      `.book-progress`로 짓지 마세요 — 앞의 것은 활동 만들기 창들과 학생
      카드의 진행 배지가, 뒤의 것은 `-legend`·`-done`·`-sum`이 이미 쓰고
      있습니다(책방 전광판이 `.bookboard`로 이름을 바꾼 것과 같은 까닭).

- `bookActivities/{id}.grouped` — **곁텍스트·RAFT를 모둠으로 진행**
  - 닿소리만 모둠 활동이던 것을 곁텍스트 읽기·RAFT 글쓰기로 넓혔습니다.
    **글은 모둠으로 묶여도 학생마다 한 장 그대로**(`entries/{uid}`)이고,
    모둠이 정하는 것은 '누구와 함께 보는가' — 화면의 흐름과 동료 평가의
    범위입니다. 그래서 저장 위치도 규칙(entries)도 안 건드립니다.
  - **판정은 `grouped === true`로**(`lib/bookGroups.js`의 `isGroupedActivity`).
    `groupMode`로 판정하면 예전에 만든 개인 활동이 전부 모둠 활동으로
    둔갑합니다 — 그때도 방식과 상관없이 'teacher'가 적혀 있었습니다.
    닿소리는 원래 모둠 활동이라 이 표시를 안 씁니다(`groupMode !== 'solo'`).
  - 교사 화면은 `GroupFilterRow`(한 줄)로 카드를 모둠으로 좁힙니다. 닿소리처럼
    왼쪽 세로 목록을 두지 않은 이유는, 이 화면들은 카드 격자가 주인공이라
    왼쪽에 판을 두면 카드가 좁아져 학생 이름이 잘리기 때문입니다.
  - **모둠을 다시 짜는 길은 머리말의 '활동 모둠'**입니다(`ParatextBoard` ·
    `RaftBoard` · `BookGroupBoard` 셋 다 — `GroupComposer`를 **같은 인자로**
    부릅니다). 곁텍스트·RAFT는 짜는 길이 만들기 창에만 있어, 한 번 만들고
    나면 교사가 모둠을 고칠 수 없었습니다(전학·결석·모둠 갈등 어느 것도
    반영할 수 없었습니다).
    - 자리는 `.books-head-main`의 **맨 앞**, 말은 `활동 모둠`, 그림은
      `IconPeople` — 닿소리와 같아야 합니다. 네 화면의 머리말이 같은 차례인
      까닭과 같습니다.
    - 조건은 **`grouped && !freeMode`**입니다. '학생이 고르기'는 학생이 스스로
      드는 방식이라 교사가 짤 것이 없고, 개인 활동은 모둠 자체가 없습니다.
      닿소리의 같은 단추와 판정이 **같아야** 합니다.
    - 두 보드는 `app/books/page.js`에서 **`admin`일 때만** 그려지므로
      `isTeacher` prop이 없습니다 — 조건에 넣지 마세요(늘 참입니다).
      대신 `baseGroupAssignment`·`onToast`를 페이지에서 내려 줍니다.
    - **저장되는 자리도 규칙도 안 건드립니다.** 글은 모둠으로 묶여도 학생마다
      한 장이라(`entries/{uid}`), 여기서 바뀌는 것은 '누구와 함께 보는가'와
      동료 평가의 범위뿐입니다.
    - `composeBookGroups`는 **문서 id가 `group_{index}`라고 가정하고**, 넘긴
      목록에 없는 모둠은 물러난 것으로 표시합니다(`retired`). 그 index는
      **1부터**입니다 — 0부터 세면 첫 모둠이 물러나고 이름이 한 칸씩 밀립니다.
  - 학생 화면은 `GroupMatesRow`(우리 모둠 명단 칩) — 자기가 쓰는 칸 바로 위.
    '자유 구성'인데 아직 모둠이 없으면 `GroupJoinRow`가 대신 뜹니다(고르기
    전에도 글은 쓸 수 있습니다 — 어차피 각자 한 장이라서).
  - **만들기 창은 모둠을 세 번에 나눠 묻습니다**(`BookActivityForm`).
    한 줄에 다섯 갈래를 늘어놓았더니 '기본 모둠'과 '활동 모둠'이 나란히 있어
    무엇이 무엇인지 알기 어려웠습니다. 이제 묻는 차례대로 갈라 둡니다.
    ① 진행 방식 — 모둠 활동 / 개별 활동
    ② 모둠 구성 — 기본 모둠 그대로 / 이 활동만의 모둠
    ③ 모둠 짜는 방법 — 교사 배정 / 무작위 / 학생이 고르기 (②가 '이 활동만'일 때)
    저장되는 `groupMode`는 지금까지와 같은 다섯(solo·base·teacher·random·free)
    이라 **자료도 규칙도 그대로**입니다 — 묻는 방법만 바뀝니다.
  - **만들기 창은 가로로 넓은 두 칸**입니다(`.book-form-cols`, 900px).
    왼쪽은 '무엇을 할까'(활동 종류 다섯을 세로로), 오른쪽은 '어떻게 할까'
    (이름·주제어·주소·모둠). 480px 한 줄에 전부 쌓았더니 세로로 길어져
    안쪽에 스크롤 막대가 생겼고, 내리면 위에서 고른 활동 종류가 화면 밖으로
    나가 무엇을 만드는 중인지 안 보였습니다. 오른쪽은 물음을 짝지어 한 줄에
    둘씩 놓습니다(진행 방식 ｜ 모둠 구성 · 모둠 짜는 방법 ｜ 모둠 수).
    **한 줄에 셋을 넣지 마세요** — 버튼 줄이 두 줄로 접힙니다(‘모둠당 최대’를
    ‘모둠 짜는 방법’ 줄에 끼웠다가 그렇게 됐고, 지금은 ‘모둠 이름’ 옆입니다).
    768px 이하에서는 두 칸이 위아래로 풀리고 짝짓기도 한 줄에 하나씩 됩니다.
  - **고르는 도중에 창 크기가 변하지 않습니다.** 모둠 설정의 아랫부분
    (`.book-group-detail`)은 고른 갈래에 따라 내용이 통째로 바뀌는데
    ('이 활동만의 모둠'은 줄 둘 + 안내, 나머지는 안내문 한 덩이), 가장 큰
    갈래에 맞춰 `min-height: 166px`로 자리를 미리 잡아 둡니다. 166인 것은
    '학생이 고르기'의 고르개가 글자 칸보다 1.3px 높아서입니다 — 164로 두면
    그 갈래에서만 창이 자랍니다. 여기에 안내문을 더 길게 쓰면 그 갈래만
    커지므로, 고칠 때는 네 갈래의 높이를 함께 재 보세요. 768px 이하에서는
    이 자리 잡기를 풉니다(줄이 세로로 풀려 어차피 넘고, 짧은 갈래에는 빈
    자리만 크게 남습니다).
  - **기본값은 셋 다 '모둠 활동 + 기본 모둠'**입니다(닿소리·곁텍스트·RAFT).
    수업이 대체로 늘 같은 모둠으로 돌아가는데 활동마다 다시 짜면 같은 일을
    되풀이하게 됩니다. 반에 기본 모둠이 없으면 만들기를 막고 그 자리에서
    안내합니다(빈 모둠만 생겨 '이 활동만의 모둠'과 같아지므로).
  - **'이 활동에 판이 있는가'는 종류로 판정하지 마세요.** 곁텍스트·RAFT가
    모둠이 될 수 있게 되면서, 종류만 보는 곳은 전부 어긋납니다(활동 카드가
    모둠 RAFT를 '개인 활동'으로 적던 실제 버그). 판정은
    `type === 'consonant' || isGroupedActivity(activity)` — 닿소리는 늘 판이
    있고(개별 활동이면 1인 판), 곁텍스트·RAFT는 `grouped`일 때만입니다.
    `BookActivityStats`(닿소리 통계)처럼 **낱말을 세는 화면은 종류로 직접**
    거릅니다(`type === 'consonant'`) — '모둠이 있는 활동'으로 거르면 낱말이
    없는 모둠 RAFT까지 들어와 빈 격자가 뜹니다.
- `bookActivities/{id}/peerReviews/{받는사람_쓴사람}` — **동료 평가**
  (activityId, groupId, toUid, toName, fromUid, fromName, html, updatedAt)
  - 모둠 친구의 발표를 들으며 남기는 한 마디(감상·질문·평가). 문서 ID가
    `받는사람_쓴사람`이라 **한 친구에게 한 장**이고 그 장을 고쳐 씁니다 —
    여러 장이 쌓이면 무엇이 마지막 생각인지 알 수 없습니다.
  - **읽기는 받은 학생·쓴 학생·담당 교사뿐.** 반 전체가 서로 다 보게 하면
    먼저 쓴 코멘트를 보고 따라 씁니다. 대신 쓴 사람 이름은 남깁니다 —
    익명이면 함부로 씁니다.
  - **쓰기는 같은 모둠에만.** 규칙은 모둠을 뒤져 찾을 수 없어서 문서에 적힌
    `groupId` 한 번의 get으로 판정합니다 — 그 모둠 명단에 **쓴 사람과 받는
    사람이 둘 다** 있어야 통과하므로, 남의 모둠 id를 적어도 소용없습니다.
  - **잠금은 `peerReviewLocked` — 활동 전체 잠금(`locked`)과 별개.**
    '이제 그만 쓰고 이야기하자'로 닫는 자리라 글쓰기까지 함께 잠기면 안 됩니다.
  - **표시가 없으면 잠김입니다**(기본 잠김). 활동을 만들자마자 열려 있으면
    아직 아무도 발표하지 않았는데 코멘트가 쌓입니다 — 동료 평가는 '발표를
    듣는 시간'에만 여는 것이라, 교사가 머리말 단추로 한 번 열어야
    (`peerReviewLocked: false`) 시작합니다. 판정은 `lib/bookGroups.js`의
    `isPeerReviewOpen` 한 곳과 규칙의 `peerReviewOpen()` **둘이 같아야**
    합니다 — 한쪽만 고치면 화면은 쓸 수 있다고 하는데 저장이 거부됩니다.
  - 받은 한 마디를 보는 자리는 **둘**입니다: 학생은 자기 RAFT 글 아래
    (`PeerReviewList`, 앵커 `#peer-received` — 모둠 줄의 '💬 받은 한 마디'가
    거기로 데려다 줍니다), 교사는 학생 카드를 열었을 때 그 학생의 글 아래.
  - 학생 쪽 질의는 `where('fromUid','==',나)`와 `where('toUid','==',나)` 둘로
    나눕니다. 조건 없이 통째로 받으면 남의 평가까지 읽는 일이라 규칙이 막습니다
    (그 사실을 `tests/rules/peerReviews.test.mjs`에 시험으로 박아 두었습니다).
- `classes/{classId}/cornellNotes/{uid}_{날짜}` — **학생 수업 노트(코넬)**
  (classId, uid, date, lessonTitle, cue, notes, summary, feedback)
  - 학생이 수업 중에 오른쪽 서랍(`CornellNoteDrawer`)에서 적는 필기입니다.
    같은 부모의 `lessonMemos`는 **교사의** 수업 메모라 주인이 반대입니다 —
    이름을 `lessonNotes`로 지으면 반드시 헷갈려서 `cornellNotes`로 뒀습니다.
  - 칸은 코넬 노트 그대로: `lessonTitle`(제목) · `cue`(단서·핵심 질문) ·
    `notes`(필기, 서식 HTML) · `summary`(내 말로 요약). 하루 한 장 —
    복습 단위가 차시라서입니다.
  - **단서와 필기는 '덩어리'로 짝지어 둡니다**(`blocks`, `lib/cornell.js`).
    덩어리 하나 = 단서 한 칸 + 필기 한 칸이고, 읽는 화면에서는 그것이
    표의 **한 행**이 되어 왼쪽 물음이 제 오른쪽 필기와 나란히 섭니다.
    - **왜**: 학생이 실제로 적는 자리(서랍)는 폭이 380px이라 세 칸이 세로로
      쌓여 있어, 쓰는 동안에는 '이 단서를 저 필기 옆에 둔다'는 감각이
      없습니다. 2단은 나중에 크게 보기·리포트·인쇄에서만 나타나고, 그때 가서
      물음과 필기가 어긋나 있었습니다.
    - **수업 중에 미리 나누지 않습니다.** 코넬 노트의 본래 쓰임이 '수업 중엔
      필기만, 끝나고 단서를 단다'라, 처음에는 덩어리가 하나뿐이라 지금까지처럼
      죽 쓰고 주제가 바뀔 때 학생이 `＋ 다음 핵심 질문`으로 한 줄을 더합니다
      ('덩어리'는 코드 안에서만 쓰는 말입니다 — 학생 화면에는 '핵심 질문').
      칸은 글만큼 늘어나 분량이 갇히지 않습니다(최대 `CORNELL_BLOCK_MAX`=20).
    - **읽는 자리는 모두 `blocksOf`를 거칩니다.** `blocks`가 없는 옛 노트는
      `cue`/`notes`를 **한 행짜리**로 읽어 지금까지와 똑같이 보입니다 —
      자료를 옮기는 일이 없습니다.
    - **`cue`/`notes`는 거울로 함께 씁니다**(`flattenBlocks`). 규칙이 그 두
      칸의 길이를 검사하고(그래서 **규칙을 안 건드립니다** — 시험으로 박아
      두었습니다), 아직 덩어리를 모르는 셈(`ActivityHeatmap`의 '세 칸 채움')이
      그대로 굴러갑니다. **진짜 값은 `blocks`입니다.**
    - 빈 덩어리는 저장하지 않습니다(`usedBlocks`) — '＋'를 눌러 두고 안 쓴
      줄이 남으면 다음에 펴 볼 때 빈 행이 늘어서 있습니다.
    - 서랍의 필기 에디터는 비제어라 `initialHtml`을 **마운트 때 한 번만**
      봅니다. 그래서 열쇠에 `loadSeq`(서버 값을 받아들인 횟수)를 섞습니다 —
      안 그러면 서버에서 온 글이 화면에 안 들어옵니다.
    - 이름표(단서·필기)는 표 **맨 위에 한 번만**. 행마다 붙이면 행이 셋만
      돼도 이름표가 여섯 개라 정작 글이 묻힙니다. 한 칸으로 눕는 좁은
      화면에서는 머리 줄을 감추고 칸마다 `::before`로 붙입니다.
    - 크게 보기에서는 **격자 하나가** 구릅니다 — 칸마다 따로 구르면 짝지어
      둔 왼쪽과 오른쪽이 서로 어긋나 흐릅니다(그러라고 행으로 묶은 것이
      아닙니다).
    - 한 장(`.cornell-sheet`)에는 **양쪽 끝 세로선이 없습니다**(테두리가
      위아래만, 모서리도 각짐). 남는 선이 행을 가르는 가로 줄과 단서·필기를
      가르는 세로선 하나뿐이라 상자가 아니라 '줄 친 노트'로 읽힙니다.
      옆이 없는 채로 모서리만 굴리면 선이 허공에서 끊기므로 radius도 함께
      뺐습니다. (인쇄물의 표는 그대로 사방에 선이 있습니다 — 종이에서는
      칸의 경계가 보여야 손으로 짚어 가며 복습합니다.)
  - **수업 모드에서는 제목이 프로젝트 이름으로, 자료가 함께 걸립니다.**
    `LessonMode`가 방송에 `boardId`·`boardTitle`을 함께 싣고(자료 목록 자체는
    안 싣습니다 — 방송은 슬라이드를 넘길 때마다 덮어써져 매번 실어 나르면
    쓰기가 커집니다), 서랍이 `fetchBoardHandouts(boardId)`로 그 프로젝트의
    학습 자료를 **문서 1건** 읽어 노트의 `materials`에 이름+링크로 걸어 둡니다.
    - `studyBoards` 읽기는 로그인 사용자에게 열려 있어 **규칙을 안 건드립니다**
      (자료는 원래도 학생 활동 화면에 보였습니다 — 여기서 얻는 것은 권한이
      아니라 '두 달 뒤 그 노트에서 바로 닿는다'입니다). 규칙 시험으로 확인.
    - **파일을 복제하지 않습니다.** 학생 수만큼 같은 파일이 쌓입니다. 대신
      이름을 남겨, 교사가 나중에 그 파일을 지워 링크가 깨져도 '무엇이었는지'는
      압니다.
    - 이미 저장된 노트에 자료가 있으면 그것을 그대로 둡니다 — 교사가 프로젝트
      자료를 바꿔도 그날 노트에 걸린 것은 흔들리지 않습니다.
  - **`lessonTitle`은 학생이 적는 제목입니다.** 원래 방송 문서의 수업 제목을
    받아 적어 두던 자리인데, 코넬 노트에 제목 줄이 없으니 나중에 펴 봤을 때
    단서·필기만 있어 무슨 수업인지 알 수 없었습니다. 지금은 **방송 제목을
    기본값으로만** 채우고(아직 아무것도 안 적었을 때만), 학생이 한 번이라도
    손대면 다시 건드리지 않습니다 — 제목의 주인은 학생입니다. 새 필드를
    늘리지 않아 규칙도 그대로입니다.
  - **읽기는 본인과 담당 교사. 본문 쓰기는 학생 본인만.** 남의 필기를 고칠 수
    있으면 그건 그 학생의 기록이 아닙니다. 교사는 `feedback` 한 칸만 씁니다
    (`changedOnly(['feedback','feedbackAt','feedbackBy'])`).
  - 학생이 이어서 저장해도 피드백이 지워지지 않게, update 규칙이 feedback이
    그대로일 것을 요구합니다(앱은 merge로 써서 자연히 남습니다).
  - 서랍은 `TopNav`에서 **발표 오버레이와 형제로, 방송 조건 바깥에** 그립니다.
    오버레이 안에 넣으면 (ㄱ) 슬라이드를 넘길 때마다 다시 그려져 입력이
    끊기고, (ㄴ) `LessonMode`가 **일시정지에도** 방송 문서를 지우므로 한
    차시에 몇 번씩 서랍이 사라집니다. 서랍이 열리면
    `.broadcast-overlay--noted`로 발표 화면이 그만큼 좁아집니다(덮지 않음).
  - **손잡이는 열려 있을 때도 남습니다** — 서랍 왼쪽 가장자리
    (`.cornell-handle.open { right: var(--cornell-w) }`)로 옮겨 붙어 그대로
    '닫기'가 됩니다. 여닫는 자리가 늘 같은 곳이라야 손이 기억합니다(예전에는
    열리는 순간 사라져 닫는 길이 머리말 ×와 Esc뿐이었습니다). 바깥을 눌러
    닫지는 **않습니다** — 수업 중 슬라이드를 한 번 볼 때마다 닫혀 쓰던 흐름이
    끊깁니다(글은 자동 저장이라 날아가진 않지만). 손잡이가 세로쓰기라 그 안의
    숫자 배지·화살표는 `writing-mode: horizontal-tb`로 되돌립니다.
  - 서랍은 폭이 380px라 세 칸을 **세로로** 쌓습니다. 진짜 코넬 2단(왼쪽 좁은
    단서 · 오른쪽 넓은 필기 · 아래 요약)은 `CornellNoteSheet` 한 곳에만 있고,
    학생 리포트와 교사 열람이 그것을 같이 씁니다 — 한쪽만 고치면 같은 노트가
    두 얼굴이 됩니다.
  - **고치는 자리는 둘뿐입니다 — 오늘 것은 서랍, 지난 것은 크게 보기 창.**
    서랍의 날짜는 마운트 때 오늘로 못 박혀 있어(`todayDateKey()`), 날이
    바뀌면 어제 노트는 목록으로 내려가고 서랍에서는 못 고칩니다. 지난 노트를
    고치는 길은 `CornellNoteViewerModal`의 '✏ 고치기' **하나**입니다.
    - **서랍이 날짜를 갈아 끼우게 하지 마세요.** 한 번 그렇게 짰다가
      되돌렸습니다 — 서랍은 '오늘 쓰는 자리'인데 지난 날짜에 머무를 수
      있으면, 닫았다 다시 연 학생이 어제 칸에 오늘 필기를 적습니다.
      고치는 자리가 창 하나면 어느 날짜인지가 늘 화면 한가운데 있습니다.
    - **규칙은 안 건드립니다.** `isMyCornellNote`는 문서 id(`uid_날짜`)와 칸
      길이만 보고 **날짜가 오늘인지는 보지 않아**, 지난 노트 수정은 원래
      허용돼 있었습니다. 없던 것은 화면뿐이었습니다.
    - 고치는 한 장은 `CornellNoteEditSheet` — 읽기 전용 시트와 **같은
      껍데기·같은 격자**(`.cornell-sheet--edit`)라 '고치기'를 눌러도 보던
      배치가 안 바뀝니다. **읽기 전용 `CornellNoteSheet`를 고쳐 쓰지
      마세요** — 그 한 장을 다섯 곳이 함께 씁니다.
      - 두 칸은 격자의 **직계 자식**이어야 합니다(`Fragment`). 한 겹 싸면
        200px 칸에 둘 다 들어가 2단이 무너집니다.
      - 선생님 한 마디는 고치는 중에도 **읽기 전용으로 그대로** 답니다.
        고치러 오는 까닭의 태반이 그 글이라, 사라지면 무엇을 고치라고
        했는지 보려고 창을 나갔다 와야 합니다.
      - 고치는 동안에는 **하이라이트를 안 그립니다** — 글자가 바뀌는 중이라
        자리가 매 글자 어긋나고, 덮개는 입력칸 안으로 못 따라 들어갑니다.
        대신 표시가 있는 노트에만 위에 한 줄 답니다(`.cornell-edit-warn`):
        '하이라이트가 있는 경우 내용을 수정하면 하이라이트가 사라질 수도
        있습니다. 단, 표시만 사라질 뿐 피드백 내용은 그대로 유지되니
        걱정하지 마세요.' 표시가 없는 노트에 띄우면 무슨 소린지 알 수 없는
        경고가 됩니다.
    - **넘길 때는 저장하고 넘어갑니다.** ‹ › · 날짜 고르개 · 창 닫기(×·Esc·
      배경) · KWLS 탭으로 옮기기가 모두 저장한 뒤 움직입니다. 이 앱에는
      '제출'이 없고 글은 늘 저절로 저장되므로, 넘기다 고치던 것을 잃는
      쪽이 더 놀랍습니다.
      - **고치는 중에는 방향키를 안 듣습니다** — 글자 칸 안에서 ← →는 커서를
        옮기는 키입니다. 가로채면 글 가운데로 커서를 못 옮깁니다.
      - 저장할 날짜를 **`editDate`로 따로 듭니다.** 구독으로 목록이 갈려
        보던 노트가 사라지면 `id`가 맨 앞으로 옮겨 가는데, 날짜를 `note`에서
        그때그때 읽으면 고치던 글이 **엉뚱한 날짜에** 쓰입니다.
      - `materials`·`feedback`·`feedbackMarks`는 **안 보냅니다**(merge라 그대로
        남고, 규칙도 학생이 뒤의 둘을 바꾸는 것을 막습니다).
      - 저장 뒤 **서랍의 14일 캐시를 버립니다**(`invalidateMyRecentCornellNotes`).
        안 버리면 방금 고친 노트가 서랍 목록에 옛 내용으로 남습니다(5분).
    - **삭제는 없습니다.** 규칙은 본인 삭제를 허용하지만
      (`resource.data.uid == uid()`) `lib/store.js`에 함수를 두지 않았습니다 —
      노트를 지우면 선생님 한 마디까지 함께 사라집니다.
  - **읽는 자리는 넷**입니다: 서랍(오늘 것 쓰기 + 아래에 최근 14일 목록) ·
    `CornellNoteViewerModal`(한가운데 큰 코넬 2단, ‹ › 와 ← → 로 연속
    넘기기 — 서랍의 '노트 전체 보기'와 **공부방 머리말의 '📓 수업 노트'**
    가 엽니다) · `/report`의 '📓 수업 노트'(앵커 `#cornell-notes`) ·
    '기록 관리' 모달의 수업 노트 탭(교사).
    공부방 단추는 학생 줄의 맨 끝('KWLS 차트' 오른쪽)입니다 — 지난 노트를
    넘겨 보고 내려받는 길이 서랍 안에만 있어 찾기 어려웠습니다.
    - 크게 보기는 **화면을 옮기지 않습니다.** 예전에는 `/report`로 밀어냈는데,
      수업 중에는 방송이 떠 있고 쓰던 노트도 두고 가야 해서 부담이 큽니다.
    - 그 창의 배경은 `z-index: 3010` — 보통 모달 배경(100)으로 두면 서랍
      (3001) 밑에 깔려 아무것도 안 보입니다. 서랍 **안이 아니라 형제로**
      그려, 서랍이 다시 그려져도 창이 흔들리지 않습니다.
    - Esc가 겹칩니다: 같은 `window`에 걸린 리스너끼리는 `stopPropagation`이
      안 통해, 창이 떠 있는 동안 서랍의 Esc 처리를 아예 비켜 줍니다
      (`if (!open || viewerOpen) return`). 안 그러면 한 번에 둘 다 닫힙니다.
    - **탭이 둘입니다 — 수업 노트 · KWLS 노트**(`.dash-view-tabs`).
      **화면에는 '코넬'이라 적지 않습니다** — 교사 화면이 '수업 노트'라,
      학생 화면만 다른 말을 쓰면 같은 것을 두 이름으로 익혀야 합니다.
      코드 주석의 '코넬'은 그대로 둡니다(코넬 2단·코넬 노트의 복습 방법 —
      화면 글자가 아니라 필기법 이름이라, 바꾸면 '수업 2단'이 됩니다).
      학생이 남기는 '내가 쓴 것'이 그 둘이라, 지난 기록을 펴 보는 자리를
      여기 하나로 모읍니다(예전에는 KWLS를 보려면 공부방 사이드 패널의
      기록 탭까지 찾아 들어가야 했습니다).
      - KWLS 탭은 `MyKwlsNotes`가 통째로 맡습니다 — 구독(`subscribeMyAllKwl`,
        내 것만·등호 둘)도, 넘기기도, 달력도. **한 번 열면 감춰만 둡니다**
        (`hidden`) — 탭을 오갈 때마다 지웠다 만들면 그때마다 다시 읽습니다.
      - 한 장은 `KwlsNoteSheet` — 껍데기(`.cornell-sheet`)와 제목 줄은 코넬
        한 장과 **같은 것**이고 안쪽만 K·W·L·S 2×2입니다(위가 읽기 전,
        아래가 읽은 뒤 — 학생이 쓸 때 본 배치 그대로). 같은 창에서 탭만
        바꿔 넘나드는 자리라 두 장이 다른 모양이면 다른 공책처럼 보입니다.
      - **인쇄가 없습니다.** 코넬 노트는 종이로 뽑아 복습하는 자리지만
        KWLS는 화면에서 견주어 보는 기록입니다.
      - **빈 기록은 한 장으로 세지 않습니다**(한 칸이라도 썼나) — 교사
        화면의 달력·화살표와 같은 기준입니다.
      - 방향키(← →)를 두 탭이 함께 들으면 한 번 눌러 두 장이 넘어갑니다.
        코넬 쪽은 `tab === 'cornell'`일 때만, KWLS 쪽은 `active`일 때만.
      - 날짜 달력은 `KwlDateCalendar` — 교사의 KWLS 전체 화면과 **같은
        것**입니다(그쪽은 '그날 쓴 사람 수', 여기는 '내 기록 건수').
        학생 쪽만 기록이 없는 날을 못 누르게 합니다(`disableEmpty`) —
        볼 것이 없는 날이라서요. 교사 화면은 빈 날도 확인하러 가는
        자리라 그대로 열어 둡니다.
      - 팝업은 **오른쪽에 맞춰** 답니다(`.cornell-read-calwrap .kwlfs-cal`).
        단추가 줄 오른쪽 끝이라 왼쪽 기준이면 340px이 창 밖으로 나가는데,
        이 창은 `overflow: hidden`이라 그대로 잘립니다(실측).
  - **교사 화면은 날짜 하나로 좁혀 읽습니다**(`subscribeClassCornellNotesOn`).
    반 전체를 기간 제한 없이 받으면 학생 수 × 수업 일수(한 학기면 수천 건)라,
    '오늘 누가 썼나'를 학생 수만큼으로 줄입니다. 한 학생의 흐름은 카드를 눌러
    들어갔을 때만 그 학생 것을 받습니다(`subscribeStudentCornellNotes`).
    학생 리포트는 자기 반마다 리스너 하나(`subscribeMyCornellNotes`) —
    `collectionGroup`을 안 쓰는 이유는 규칙에 그룹 문을 새로 열지 않으려고요.
    셋 다 `where` 하나만 걸고 정렬은 화면에서 합니다(복합 색인 회피).
  - **선생님 한 마디 도착 알림** — 서랍이 뜰 때 `fetchMyRecentCornellNotes`로
    **최근 14일치**를 훑어 안 읽은 것을 셉니다(손잡이 숫자 배지 + 서랍 맨 위
    목록). 선생님은 수업이 끝난 뒤에 쓰므로 오늘 노트만 보면 어제 것에 달린
    한 마디를 영영 못 봅니다.
    - 읽는 방법은 **날짜마다 한 건씩 `getDoc`**입니다(문서 ID가 `uid_날짜`).
      질의로 좁히려면 uid 등호 + 날짜 범위라 복합 색인이 필요하고, 등호만
      걸면 그 반의 내 노트가 전부 옵니다(한 학기 쉰 장). 이 방식은 노트가
      쌓여도 **늘 14건 고정**입니다. 화면을 옮길 때마다 다시 읽지 않게
      **5분 캐시**를 둡니다(`invalidateMyRecentCornellNotes`).
    - **`documentId() in [...]` 한 방 질의는 규칙이 거부합니다**(실측).
      ID 앞부분이 uid라는 건 우리 약속일 뿐이라 규칙이 알 수 없습니다.
      실측 기록이 `tests/rules/cornellNotes.test.mjs`에 있으니 지우지 마세요.
    - 읽음 표시는 노트 문서의 `feedbackSeenAt`입니다(`markCornellFeedbackSeen`).
      localStorage로 하면 폰에서 읽은 것을 노트북이 몰라 배지가 되살아납니다.
      학생 update 규칙이 변경 키 화이트리스트가 아니라 '본인 것 + 칸 길이 +
      피드백 불변'만 보므로 **규칙을 고치지 않고** 이 필드가 통과합니다.
    - 판정은 `isCornellFeedbackUnread` 한 곳에서. `feedbackAt`이 없는 옛
      기록은 '봤다'로 봅니다 — 안 그러면 배지가 영영 안 꺼집니다.
  - **읽기 규칙은 `get`/`list`로 나눠 두었습니다.** 문서가 없으면 `resource`가
    비어 `resource.data.uid == uid()`가 오류로 끝나 **본인조차 거부**됩니다
    (아직 안 쓴 날짜를 열어 보는 경우 — 새 날 첫 접속이 여기 걸립니다).
    그래서 `get`에는 `nId.split('_')[0] == uid()` 갈래를 함께 뒀습니다. 쓰기
    규칙이 ID를 `uid_날짜`로 못 박고 있어 권한이 넓어지지 않습니다.
    `list`는 문서가 반드시 있으므로 지금까지대로 `resource.data.uid`로 봅니다.
  - **PDF로 내려받기**(`lib/exportCornell.js`) — 크게 보기 창의 '🖨 이 노트 /
    전체 n장', `/report` 수업 노트 머리말의 '🖨 PDF로 저장'.
    - 만드는 법은 **브라우저 인쇄**입니다(숨은 iframe에 인쇄용 문서를 띄우고
      `print()` — '대상: PDF로 저장'). jsPDF·html2canvas를 쓰지 않는 이유:
      그림으로 굽지 않아 **글자가 글자로 남고**(복사·검색·확대), 한글 글꼴을
      실어 나르지 않아도 됩니다. 공부방 '활동 자료 다운로드'의 PDF와 같은
      방식이라(`printHtmlDoc`) 이 앱의 인쇄물이 한 가지로 만들어집니다.
    - **레이아웃은 이 파일 한 곳에서만** 정합니다. 한 장에 노트 하나
      (`break-before: page`), 코넬 2단은 **표(table)로** 짭니다 — grid·flex는
      내용이 한 쪽을 넘칠 때 잘리는데 표는 다음 쪽으로 이어집니다. 고정 높이·
      overflow를 두지 마세요(잘린 노트는 노트가 아닙니다).
    - **내용이 적어도 한 쪽을 채웁니다** — 표에 `height: 175mm`. 표의 height는
      최솟값이라(표는 내용보다 작아지지 않습니다) 길게 쓴 날은 그대로 늘어나
      다음 쪽으로 이어집니다. 내용에 맞춰 줄어들게 두었더니 한 줄짜리 필기가
      종이 맨 위에 손바닥만 하게 찍혀 노트가 아니라 쪽지처럼 보였습니다.
      175인 까닭: 인쇄 높이 269mm(1016px)에서 제목·요약·수업 자료·선생님
      한 마디가 **다 붙는 가장 두꺼운 노트**가 947px입니다(185mm면 985px라
      피드백 두어 줄에 둘째 쪽으로 넘어갑니다 — 실측). 값을 키울 때는 그
      경우로 다시 재 보세요.
    - 필기에 남는 태그는 `lib/html.js`의 `ALLOWED_TAGS`가 전부입니다 —
      링크·표는 저장 단계에서 걷히므로 인쇄 CSS에 규칙을 두지 않습니다.
    - 인쇄물의 이름은 `printableName`에서 한 번만 만듭니다(학교 계정
      '20512김하윤' → '20512 김하윤'). 두 자리가 다른 이름을 찍으면 같은
      학생의 노트가 두 얼굴이 됩니다.
  - **교사가 학생 글을 드래그해 짚으면 그 대목이 학생 화면에도 칠해집니다.**
    끈 자리는 형광펜처럼 덮이고, 같은 대목이 아래 '선생님 한 마디'에
    `「인용」 / → ` 한 덩이로 붙어 거기서부터 씁니다. 글 전체에 대한 한 마디
    밖에 못 쓰던 자리가 '이 문장에 대한 한 마디'가 되고, **학생은 선생님 말이
    제 글의 어느 문장에 대한 것인지 인용문을 대조하지 않고 그대로 봅니다** —
    그것이 이 기능의 목적입니다.
    - 짚은 자리는 노트 문서의 **`feedbackMarks`**(`{ start, end, text }` 목록).
      셈과 헬퍼는 `lib/cornellMarks.js` 한 곳, 덮개를 그리는 것은
      **`CornellNoteSheet`** 한 곳입니다.
    - **덮개를 시트 안에 둔 까닭**: 한 장을 쓰는 곳이 다섯입니다(교사 열람 ·
      노트 크게 보기 · `/report` · 서랍의 피드백 알림 · 서랍의 지난 노트).
      교사가 짚은 자리는 **그 다섯 곳에서 다 같이 보여야** 하므로, 껍데기를
      거기 두면 부르는 쪽은 아무것도 안 해도 따라옵니다. 교사 창만
      `onMarkAdd`·`onMarkRemove`를 함께 주어 끌어서 만들고 눌러서 지웁니다 —
      안 주면 읽기 전용이라 손 모양도 hover 색도 없습니다(`.is-editable`).
    - **규칙을 열었습니다**(`feedbackMarks`) — 교사의 `changedOnly`에 넣고,
      학생 update에는 **피드백과 같이 '그대로일 것'**을 요구합니다. 학생이 그
      대목을 고쳐 **표시가 안 그려지는 것**과 표시를 **지워 버리는 것**은 다른
      일입니다. 목록 안 항목의 모양까지는 규칙이 못 보므로(반복문이 없습니다)
      개수 천장 40만 둡니다 — `MARK_MAX`가 그 값과 **같아야** 합니다.
      시험은 `tests/rules/cornellNotes.test.mjs`(여덟 개).
    - **코드보다 규칙을 먼저 배포하세요.** 순서가 뒤집히면 앱이 그 칸을
      보내는데 규칙이 거부해 **피드백 저장이 통째로 실패합니다.**
    - **Range를 들고 있으면 안 됩니다.** 본문은 `dangerouslySetInnerHTML`로
      그리는 자리라, 부모가 한 번 다시 그릴 때마다(피드백 칸에 글자 하나만
      쳐도) 그 아래가 통째로 새 노드로 갈립니다 — 들고 있던 Range는 문서에서
      떨어져 나가 줄 상자가 0개가 됩니다(실측: 한 글자 치면 rects 1 → 0 ·
      `isConnected` false). 그래서 **글자 자리로 매어 두고 잴 때마다 Range를
      새로 만듭니다**(`offsetsOf`·`rangeAt`).
    - **학생이 고치면 표시가 사라집니다.** 짚을 때의 글자(`text`)를 함께 두고
      그릴 때마다 그 자리의 지금 글자와 견주어, 다르면 안 그립니다
      (`markStillFits`). 자리만 보고 칠하면 학생이 앞에 한 줄만 더 써도 엉뚱한
      대목을 덮습니다. 덤이 있습니다 — **어긋나면 '사라지는' 쪽으로만 무너지고,
      엉뚱한 데를 칠하는 일은 구조적으로 안 생깁니다.**
      - **지우는 것이 아니라 '지금은 안 그리는' 것입니다.** 저장된 자리는
        그대로 남아 있어, 학생이 고친 것을 되돌리면 표시도 다시 나타납니다.
      - **밀린 자리를 다시 찾아 따라가지 않습니다**(일부러). 규칙이 하나라
        예측하기 쉽고, 사라져도 인용은 피드백 글에 남습니다.
    - **자리를 재는 범위는 '학생이 쓴 글'뿐입니다.** 이름표('단서 · 핵심
      질문')·제목 줄·수업 자료·선생님 한 마디에는 **`data-nomark`**를 달아
      건너뜁니다 — 그것까지 세면 나중에 **이름표 한 글자만 고쳐도 저장해 둔
      자리가 전부 밀립니다.** 새 칸을 시트에 더할 때 그것이 학생 글이 아니면
      이 표시를 함께 다세요.
    - **글자를 `<mark>`로 감싸지 않습니다.** 본문은 위와 같은 이유로, 단서·
      요약은 React가 직접 그리는 자리라 텍스트 노드를 갈아 끼우면 다음 렌더에서
      글이 안 바뀌거나 지우다 오류가 납니다. `getClientRects()`가 주는 줄
      상자를 **위에 덧그립니다**(`.cornell-hl`) — 여러 줄·단서와 필기에 걸친
      선택도 자연히 맞습니다. 덮개는 `mix-blend-mode: multiply`라 글자가 그대로
      읽힙니다(반투명으로 덮으면 짚은 대목이 오히려 흐려집니다).
    - **렌더마다 다시 잽니다**(`useLayoutEffect`에 의존성 배열 없음). 값이
      그대로면 상태를 안 바꾸므로(`sameRects`) 렌더가 더 돌지 않습니다 —
      **이 비교를 빼면 렌더가 끝없이 돕니다.**
    - **안쪽이 구르는 화면이 있습니다** — 노트 크게 보기는 시트가 아니라 그
      안의 격자가 구릅니다. 그때 감싼 칸은 제자리인데 글자만 올라가므로
      `scroll`을 **capture로** 듣고 프레임마다 한 번 다시 잽니다(scroll은
      위로 안 올라옵니다). 창이 통째로 구르는 화면은 둘이 같이 움직여 값이
      그대로라 `sameRects`가 걸러 냅니다.
    - **감싼 칸이 flex 항목 자리를 가로챕니다.** 노트 크게 보기는
      `.cornell-sheet`에 `flex: 1`을 걸어 안쪽 격자를 굴리는데, 그냥 두면 그
      값이 한 겹 안쪽으로 밀려 안 먹고 스크롤이 통째로 풀립니다.
      `.modal-cornell-viewer .cornell-hl-wrap`에 같은 값을 함께 줍니다 —
      **시트를 flex/grid 항목으로 쓰는 화면을 새로 만들면 여기도 함께.**
    - 표시를 누르면 걷힙니다(교사만). 그때 **아직 아무것도 안 쓴 인용이면 그
      줄도 함께** 걷고, `→` 뒤에 쓴 글이 있으면 건드리지 않습니다. 짓는
      모양(`quoteBlock`)과 걷는 모양(`dropQuote`)을 **함께 고치세요** — 다음
      인용을 붙일 때 앞 덩이의 꼬리 공백을 지우므로 걷을 때는 `'→ '`가 아니라
      `'→'`까지만 보고 찾습니다.
    - **표시만 걷어도 '저장 안 한 것'입니다**(`dirty`). 표시가 이제 문서에
      남는 값이라, 안 세우면 저장 단추가 꺼진 채여서 지운 것이 안 반영됩니다.
    - **드래그 판정을 `setMarks` 안에서 하지 마세요** — StrictMode가 갱신
      함수를 두 번 부르므로 인용이 두 번 붙습니다.
    - 키보드 길은 머리말의 '표시 n개 지우기'(덮개는 눈으로 보고 누르는 것이라
      `aria-hidden`). 안내 한 줄도 함께 — 드래그로 되는 일은 화면에 적혀
      있지 않으면 아무도 찾지 못합니다.
    - 한 글자 선택·같은 자리 두 번 끌기는 무시. 인용은 60자(`QUOTE_MAX`),
      자리 확인용 글자는 300자(`MARK_TEXT_MAX`)에서 자르고 **앞에서 그만큼만**
      견줍니다.
    - **mock은 노트 문서를 갈아 끼웁니다**(제자리에서 고치지 않고 —
      `saveCornellNote`·`saveCornellFeedback`). 같은 객체를 고쳐 두면 그 문서를
      들고 있는 쪽이 늘 같은 참조를 받아 React가 다시 그리지 않아, 데모 모드
      에서 **학생이 필기를 고쳐도 표시가 밀린 자리에 그대로 남았습니다**(실측).
    - 실측: 교사가 끌면 교사 화면에만 → 저장하면 **학생 화면에도**(누를 수는
      없음) · 교사 창을 다시 열면 저장된 표시가 돌아옴 · 학생이 앞에 한 줄
      덧붙이면 **양쪽 다 사라지고 피드백 글은 그대로** · 크게 보기에서 안쪽
      격자를 40px 굴려도 글자와 어긋남 0.0px.
  - 탈퇴 정리는 `purgeStudentData`의 `collectionGroup("cornellNotes")` 한 줄.
    반 아래라 반 삭제는 `purgeClass`가 알아서 정리합니다.
    - **`collectionGroup` 질의에는 색인을 손수 선언해야 합니다.** Firestore가
      저절로 만들어 주는 단일 필드 색인은 **컬렉션 범위뿐**이고, 컬렉션 그룹
      범위는 `firestore.indexes.json`의 `fieldOverrides`에 적어야 생깁니다.
      이 줄을 더하면서 짝이 되는 색인을 안 적어, 탈퇴 처리가 **그 항목만**
      `수업 노트: 9 FAILED_PRECONDITION`으로 실패했습니다(나머지는 정상
      삭제 — 실패한 것만 `warnings`로 돌려주는 구조라 조용히 지나갔습니다).
      **`collectionGroup(...).where(...)`를 새로 쓸 때는 색인을 함께 적고,
      코드보다 먼저 배포하세요**(만드는 데 몇 분 걸립니다).
    - **컬렉션 범위 색인을 함께 적어 두어야 합니다.** 단일 필드 예외는 그
      필드의 자동 색인을 **통째로 갈아 끼웁니다** — 컬렉션 그룹만 적으면
      컬렉션 범위 색인이 사라져 `subscribeMyCornellNotes`·
      `subscribeStudentCornellNotes`(둘 다 `where("uid","==")`를 반 안에서
      겁니다)가 함께 깨집니다. 그래서 이 항목만 `COLLECTION` 오름차순·
      내림차순 + `COLLECTION_GROUP` 오름차순 **셋**입니다(다른 넷은 컬렉션
      범위에서 그 필드로 거르는 질의가 없어 그룹 하나뿐입니다).
      콘솔에서 에러 링크(`create_exemption=…`)로 만들면 이것을 알아서
      맞춰 주지만, **'단일 필드' 탭에서 손으로 만들면 빠뜨리기 쉽습니다.**
    - 색인이 없던 동안 남은 노트는 `npm run notes:purge-orphans`로 훑어
      지웁니다(`--apply` 없이는 찾아만 봅니다 · `--uid=xxx`로 한 명만).
      **프로필(`users/{uid}`)이 없는 노트만** 주인 없는 것으로 봅니다 —
      탈퇴 정리가 프로필을 맨 마지막에 지우므로, 반에서만 빠진 학생의
      노트는 건드리지 않습니다. 노트에서 받아 오는 칸은 `uid` 하나뿐이라
      (`mask.fieldPaths`) 필기 본문을 읽지 않습니다.
- `classes/{classId}/rewardEvents` — **과일 지급 이력** (uid, delta, count, byUid, at)
  - **어느 화면에서 줬는지는 안 남습니다.** 출처가 필요한 자리(기록 관리의
    수업 노트 탭)는 이력을 뒤지지 않고 **그 대상 문서에 도장을 찍습니다**
    (`cornellNotes`의 `rewardedAt` — 위 '수업 노트' 절). 여기에 `source`를
    더하는 길도 있었지만, 그러면 '그 학생의 그날 이력'을 넓게 훑어야 해서
    화면 하나 칠하자고 읽기가 늡니다.
  - `rewards`는 누적 총계뿐이라 '언제 몇 개 받았나'가 없습니다. 참여의 변화를
    보려면 시계열이 필요해 지급할 때마다 한 건 적습니다(`setStudentReward`가
    총계와 **한 트랜잭션**으로 씁니다 — 동시 지급 유실도 이때 함께 막힙니다).
  - 이력이라 수정·삭제 불가. 반 삭제 시 `purgeClass`의 recursiveDelete가 정리.
  - **＋1·−1 단추는 반드시 `addStudentReward`(델타)로.** `setStudentReward`는
    '몇 개로 맞춰라'는 절대값이라, 화면에 보이는 개수로 `count + 1`을 만들어
    보내면 **빨리 두 번 누를 때 두 번째가 묻힙니다** — 첫 번째 결과가 구독으로
    돌아오기 전이라 두 번 다 같은 값을 보내고, 서버는 `before === safe`로 보아
    아무 일도 안 합니다(이력도 안 남습니다). 실제로 이 증상이 있었습니다.
    지금은 누르는 자리가 `onAward(uid, 개수, delta)`로 델타를 함께 넘기고,
    페이지의 `awardReward`가 델타가 오면 `addStudentReward`로 보냅니다
    (트랜잭션 안에서 읽은 값에 더하므로 한 번도 안 묻힙니다).
    절대값 경로는 '개수를 직접 맞추는' 자리에만 남겨 둡니다.
- `classes/{classId}.pyTarget` — **교사 실행기 2단에서 고른 활동** `{ boardId, actIndex }`
  다음에 2단을 열어도 그 자리이게 적어 둡니다. 예전에는 그 반 학생의
  '활동으로 보내기'가 전부 여기로 갔지만, 학생은 이제 서랍에서 곧바로 쓰므로
  **학생 화면은 이 값을 읽지 않습니다**. 자세한 것은 아래 'PythonRunner 2단
  서랍' 절.
- `users` — 사용자 프로필 (uid, email, displayName(익명), realName, studentId, role)
  - **식별 정보(실명·이메일·학번)는 여기에만** 저장. 게시물·카드엔 익명 정보만 넣음.
  - 읽기 규칙: 본인+교사. 교사 화면은 `subscribeUserDirectory`로 uid→실명/학번 조회.
- `groupMemos` — **모둠 메모** (classId, pairKey, fromUid/fromName,
  toUid/toName, html, replyToId/replyToText/replyToName, read, createdAt)
  - 학생이 같은 기본 모둠 친구에게 남기는 쪽지. 자세한 것은 아래
    '우리 모둠 · 모둠 메모' 절.
  - `pairKey`는 두 uid를 **정렬해 이은 값**(`a__b`)이라 누가 보내든 같습니다.
    `lib/store.js`의 `memoPairKey`와 규칙의 `pairKeyOf`가 **같아야** 합니다.
- `users/{uid}/notifications` — 개인 알림함 (답변 알림 · 반 공지)
  - 규칙은 **본인에게만** 열려 있습니다(`userId == uid()`). 교사도 못 읽고 못
    지웁니다 — 반 공지 알림에 교사 실명과 공지 본문이 들어 있어서, 여기를
    넓히면 교사가 학생 알림을 들여다볼 수 있게 됩니다.
  - 그래서 **탈퇴 처리 때 이 알림함은 서버 함수만 지울 수 있습니다**
    (`purgeStudentData`의 `recursiveDelete`, admin SDK라 규칙 우회).
    `lib/store.js`의 폴백 경로는 규칙에 막혀 지우지 못합니다.
  - Firestore는 문서를 지워도 하위 컬렉션을 함께 지우지 않습니다. 한때
    `purgeStudentData`에 알림함이 빠져 있어, 프로필만 사라지고 알림이 남은
    계정이 생겼습니다(콘솔에 기울임꼴 '존재하지 않는 문서'로 보입니다).
    원인은 막았고, 남은 것은 `npm run books:purge-orphans`로 훑어 지웁니다
    (`--apply` 없이는 찾아만 봅니다).

## 시각화 컴포넌트 구조 (admin/report 공통)

### 차트 레이아웃 (2행)
- **1행**: `.admin-charts { display: grid; grid-template-columns: repeat(4, 1fr) }` — 도넛/막대 차트 4개
- **2행**: `<ActivityHeatmap>` — 52주 히트맵 + 레이더 오버뷰 풀 폭

### ActivityHeatmap 레이아웃 패턴 (중요)
- `.heatmap-outer`: flex row, `gap: 0` — 히트맵 영역 + ActivityOverview 나란히
- `.heatmap-body`: `flex: 1; overflow-x: auto` — 히트맵 전체 폭 차지
- `.heatmap-day-col`: 요일 레이블 컬럼 (고정 폭)
- `.heatmap-right`: `flex: 1; min-width: 0; display: flex; flex-direction: column` — 내부 column flex
- `.heatmap-week`: **`flex: 1; min-width: 13px`** — 주 컬럼이 가용 폭을 균등 분할 (고정 폭 아님)
- `.heatmap-cell`: **width 없음** — 부모 week 폭에 맞게 자동 확장 (고정 폭이면 그리드가 패널 폭을 못 채움)
- `.heatmap-legend-swatch`: 범례 스와치는 **별도 클래스**, `width: 15px` 고정 (heatmap-cell과 혼용 금지)
- `.activity-overview`: `flex-shrink: 0; width: 350px; border-left; margin-left: 20px; padding-left: 20px` — 구분선 양쪽 20px 대칭

### ActivityHeatmap 잔디 — '건수'가 아니라 '몇 갈래로 참여했나'
- 예전에는 질문·답변 **건수**를 그대로 셌습니다. 활동이 늘면서 이 방식이
  무너집니다 — 한 번에 여러 건이 나오는 활동을 한 날이 성실히 참여한 여러
  날보다 진해집니다(활동마다 '한 건'의 무게가 다름).
- 지금의 하루 점수: 질문 1건 = 1(**하루 최대 2**) · 답변 1건 = 1(최대 2) ·
  KWLS 읽기 전(K·W) 1 + 읽은 뒤(L·S) 1 · 수업 노트 한 장 1.
  **끝까지 마친 것에만 +1**(KWLS 네 칸 · 노트 세 칸) — 개수를 더 세는 대신
  마무리에 색을 주면 '많이 쓴 사람'이 아니라 '끝낸 사람'이 짙어집니다.
  단계는 그대로 0 / 1 / 2 / 3~4 / 5+.
- 그래서 **범례가 '적음–많음'이 아니라 '한 가지–여러 가지'**이고, 칸 툴팁에
  그날 한 갈래를 적습니다(`9/3 · 질문 2 · KWLS 완성`). 색만으로는 왜 진한지
  알 수 없습니다.
- **책방 활동(닿소리·곁텍스트·RAFT)은 넣지 않습니다.** 가짓수는 많지만 수시로
  되풀이하는 활동이 아니라, 넣으면 그 활동을 한 날만 도드라지고 나머지 날의
  차이가 묻힙니다. 읽기 비용도 그렇습니다 — 여기 드는 넷은 `/report`가 **이미
  받아 둔 것**이라 새로 읽는 문서가 하나도 없습니다(책방은 새 구독·복합 색인).
- `/admin`은 고른 학생의 질문·답변·KWLS만 넘깁니다(수업 노트는 그 화면이 안
  받습니다). 그만큼 옅게 나오는데, 없는 것을 있는 척하는 것보다 낫습니다.

### ActivityOverview 값 계산
- **관리자**: 클래스 내 최대값 기준으로 정규화 (`Math.min(value / classMax, 1)`)
- **학생 리포트**: 고정 기준 (질문·답변 10개=100%, 공감 15개=100%)

### 자리 배치 — 학생이 자기 자리를 찾는 창

- 공부방 머리줄 '출석부 보기' **왼쪽**의 `자리 배치`(학생 전용)가
  `components/MySeatModal.jsx`를 엽니다. 학생에게는 자리표를 보는 길이
  아예 없어(교사 화면 셋은 모두 교사 전용) 자리 바꾼 날 물어서 찾았습니다.
- **출석과 무관합니다.** `attStateOf`(출석/결석/확인 전)를 쓰지 않습니다 —
  급우가 오늘 왔는지는 학생이 알 일이 아니고, 이 창이 답하는 물음은 '내
  자리가 어디인가' 하나뿐입니다. 색은 둘 — 내 자리 한 칸만
  `.attend-seat--me`(초록·테두리 2px), 나머지는 늘 `--unchecked`(회색).
  **`--present`를 돌려쓰지 마세요**: 뜻이 다른 둘에 같은 클래스를 쓰면
  출석 색을 고칠 때 이 창이 함께 움직입니다.
- 자리 칸은 `<button>`이 아니라 `<div>`입니다 — 누를 것도(과일·누가기록은
  교사의 일), 끌 것도(자리는 선생님이 정함) 없습니다. '선생님 보기'
  뒤집기도 없습니다(학생은 늘 앉은 자리에서 봅니다).
- **읽는 문서는 자리표 둘뿐**입니다 — `default`와 오늘 것(`daily_날짜`).
  순서도 교사 화면과 같습니다(오늘 것이 있으면 그것). 한쪽만 보면 선생님이
  오늘 자리를 흔든 날 학생 화면만 옛 자리를 가리켜 한 교실에 자리표가 둘이
  됩니다. 이름·학번은 페이지가 이미 갖고 있는 `studentClassRoster`를 그대로
  받아 새로 읽지 않습니다.
- **규칙을 한 줄 열었습니다** — `seatLayouts`의 read에 `isClassMember(cId)`.
  새로 드러나는 것은 '누가 어디 앉는가'뿐입니다(자리표에는 uid만 있고
  이름·학번은 users에서 읽는데 그쪽은 이미 급우끼리 열려 있습니다). 쓰기는
  그대로 교사만. `tests/rules/seatGroups.test.mjs`에 소속 학생 읽기 허용 ·
  쓰기 거부 · 다른 반 학생 거부 · 보관된 반 거부 · **없는 daily 문서 읽기**를
  함께 박아 두었습니다.

### 반 관리하기의 입장 코드 — 카드 한 장에 두 줄

- 반 카드는 **머리줄과 코드 줄 둘**입니다. 머리줄이
  `⠿ 반 이름 …… [✏][🔄 재발급][보관]`, 아래 줄이
  `입장 코드 · ABC123 · 유효기간 …… [전체 화면]`.
  예전에는 코드 칸이 이름표·코드·안내·유효기간·재발급 단추를 **세로로
  쌓아** 카드 하나가 232px이었습니다 — 반이 여섯이면 목록만 1,423px이라
  아래 반을 보려면 모달을 한참 굴려야 했습니다(실측 → 98px · 618px).
- **재발급은 코드 줄이 아니라 머리줄의 편집(✏) 옆**입니다. 반에 하는 일
  (이름 고치기 · 코드 새로 내기 · 보관)이 한자리에 모이고, 아래 줄은
  보기만 하는 줄이 됩니다.
  - 그래서 **재발급 상태는 `ClassManagerModal`이 듭니다**
    (`regeneratingCodeIds` · `handleRegenerateCode`). 단추가 머리줄과 전체
    화면 **두 곳**에 있어, 한쪽에서 누르는 동안 다른 쪽도 함께 잠겨야
    합니다. 보관 단추도 이 값을 봅니다(재발급 도중에 보관하면 코드가
    어긋납니다). `ClassJoinCode`는 이제 `onRegenerate`·`regenerating`만
    받아 그리기만 합니다.
  - 실패 안내도 모달 위쪽 `form-error` 한 곳으로 모읍니다 — 이름 바꾸기·
    보관 실패가 이미 서는 자리입니다.
- **`공부방 입장 화면에서 이 코드를 입력하세요`는 목록 위에 한 번만**
  (`.class-mgr-section-hint`). 카드마다 적으면 반 수만큼 되풀이되는데,
  한 번 읽으면 그만인 말입니다. **전체 화면에는 그대로 둡니다** — 거기는
  칠판에 띄워 학생이 보는 화면이라 그 한 줄이 곧 할 일 안내입니다.
- **쌓인 배치는 전체 화면에 그대로 남습니다.** 두 화면이 `.joincode-*`
  클래스를 함께 쓰므로, 한 줄로 만드는 규칙은 **전부 `.class-joincode`
  아래로 한정**했습니다(그 껍데기는 카드 안에만 있습니다). 같은 클래스에
  직접 손대면 칠판 화면의 큰 글씨가 함께 무너집니다.
- **유효기간은 말줄임으로 자르지 않습니다** — 줄이 모자라면 제 줄로
  내려갑니다(`flex-wrap: wrap`). 한때 `text-overflow: ellipsis`로 두었다가
  480~640px에서 `⚠️ 만료된 코드예요 — 재발급해 주세요`가 **'⚠️ 만료된
  코드…'로 잘렸습니다**(실측) — 잘린 경고는 경고가 아닙니다.
- `.joincode-expand`('전체 화면')는 `position: absolute`를 걷고 **줄의 항목**
  이 되어 `margin-left: auto`로 오른쪽 끝에 섭니다.
- 480px 아래에서는 재발급 단추의 **글자만 접습니다**
  (`.class-mgr-regen-label`) — 단추가 셋이 되며 반 이름이 밀려 잘렸습니다
  (실측 340px 모달에서 '인공지능 기초 A'가 접힘 → 고친 뒤 온전함).
  `aria-label`은 그대로라 읽어 주는 기기에서는 달라지지 않습니다.
- 실측(모달 720px): 카드 98px · 코드 줄 43px · '전체 화면'이 줄 끝에서
  12px · 가로 넘침 0. 400px 모달에서는 유효기간이 제 줄로 내려가 149px.

### 반 편성 고치기 — 학생 프로필에서 (교사)

- `/admin` 학생 프로필(`StudentEditModal`) 아래 **'반 편성'** 줄. 내가 개설한
  반이 칩으로 서고, 누르면 그 반에 **바로** 넣고 뺍니다.
- **왜 필요했나**: 학기 초에 학생이 입장 코드를 잘못 눌러 옆 반에 들어가는
  일이 잦은데 고칠 길이 아예 없었습니다. 규칙이 소속 생성을 '본인 + 유효한
  입장 코드'로만 열어 두어 교사는 넣지도 빼지도 못했고(학생이 스스로 나가는
  `leaveClass`가 있었지만 어느 화면에서도 안 불러 지웠습니다), 학생에게 코드를 다시 알려 주고 직접 들어오게
  해야 했습니다.
- **위 프로필 칸들과 달리 누르는 즉시 저장됩니다.** 소속은 `users`가 아니라
  `memberships` 문서라 '저장' 한 번에 함께 담을 수 없고, 담는 척하면 '취소'를
  눌러도 이미 바뀐 것이 안 돌아옵니다. 그래서 선으로 갈라 두고 곧바로 씁니다
  (아래 '탈퇴 처리'와 같은 모양 — 한 창 안에서 '지금 일어나는 일'끼리 같게).
- **빼도 그 학생의 글은 안 지워집니다.** 명단에서만 빠지고 카드·노트·과일은
  그대로라, 잘못 뺐으면 다시 넣으면 돌아옵니다. 창에도 그렇게 적어 둡니다.
- 소속 조회는 **반마다 문서 1건씩**입니다(`fetchStudentClassIds`).
  `where('uid','==',학생)` 한 방은 남의 반 소속까지 집어 오는 일이라 규칙이
  통째로 거부합니다 — 교사는 자기 반 소속만 읽을 수 있습니다.
- **규칙 둘을 열었습니다.**
  - `memberships` create에 교사 갈래 — `ownsClassEditable(classId)`이고 문서
    ID가 `uid_classId`일 때만. 남의 반·보관된 반은 그대로 막힙니다.
  - `users` update에 **classIds를 줄이는** 교사 갈래. `classIds`는 급우 프로필
    읽기 판정(`isClassmateOf`)의 기준이라, 소속을 지워도 여기 남아 있으면 그 반
    급우의 이름·학번을 계속 읽을 수 있습니다(append만 되는 값이라 지금까지
    지울 길이 없었습니다). **늘리는 쪽은 막습니다** — 늘릴 수 있으면 교사가
    학생에게 남의 반 명단을 읽을 권한을 얹어 줄 수 있습니다. 줄이는 것은 늘
    권한을 좁히는 쪽이라, 잘못 눌러도 학생이 그 반에 다시 들어가면 스스로
    채워집니다(`ensureClassIdSynced`).
- **넣을 때는 `classIds`를 안 건드립니다** — 교사에게 열린 것이 '줄이는 쪽'
  뿐이라서요. 학생이 다음에 공부방을 열 때 스스로 채웁니다. 그 전까지 늦는
  것은 급우 이름표 하나뿐입니다.
- 시험은 `tests/rules/memberships.test.mjs`의 '반 편성 고치기(교사)'.

### 우리 모둠 · 모둠 메모 (`groupMemos`)

- 공부방 머리줄 '자리 배치' **오른쪽**의 `우리 모둠`(학생 전용)이
  `components/GroupMemoModal.jsx`를 엽니다. **왼쪽에 대화 목록, 오른쪽에
  그 대화**입니다 — 창을 여는 이유의 태반은 '새로 쓰려고'가 아니라 '방금 온
  메모를 보려고'라, 열자마자 최근 대화가 서 있어야 합니다. 오른쪽 대화의
  자리 배치는 누가기록 모달과 같습니다(위에 쓰는 칸, 아래에 오간 말) — 다만
  쓰는 칸이 서식 에디터이고, 아래는 목록이 아니라 **대화**입니다.
- 쓰는 칸은 `RichTextEditor variant="chat"`에 **`toolbarTop`**을 함께 줍니다 —
  꾸미개가 먼저, 쓰는 칸이 그다음. `chat`은 원래 툴바가 아래인데(메신저 모양),
  서식을 실제로 쓰는 자리라 질문·공지 작성 창과 순서를 맞췄습니다. 전송
  버튼은 그대로 툴바 안입니다. 가르는 선도 `.rte-chat.rte-toolbar-top`에서
  아래로 넘어갑니다 — 안 옮기면 툴바 위에 선이 그어져 창 머리와 붙습니다.
- **대화는 테두리 하나 안에서 좌우로 오갑니다**(`.gmemo-chat` +
  `.gmemo-msg--mine`). 글마다 상자를 두르면 한 대화가 여러 덩이로 쪼개져,
  왼쪽 목록에서 스레드로 묶어 놓은 것이 여기서 도로 흩어집니다. 그래서
  바깥 상자 하나만 두고 안에서 말이 오갑니다(SNS 대화와 같은 모양).
  - 누가 한 말인지는 **왼쪽/오른쪽**이 말하므로 **줄마다 이름을 안 적습니다**
    — 1:1 대화라 위 머리줄이 이미 상대를 말합니다.
  - 시각·답장·거두기는 말풍선 **밖 아래**에 옅게. 안에 넣으면 짧은 말도
    상자가 두 줄이 되어 대화가 성기어집니다.
  - 말풍선 폭은 **78%**. 100%면 좌우로 갈라 둔 뜻이 없어지고(어느 쪽이든
    줄을 다 씁니다), 너무 좁으면 서식 글이 자꾸 접힙니다.
  - 구르는 것은 이 상자가 아니라 **오른쪽 칸**입니다 — 스크롤이 둘로 겹치면
    대화를 내리다 안쪽 끝에서 멈춘 것처럼 보입니다.
  - 순서는 **오래된 것부터**(대화라 최신순이면 답과 물음이 거꾸로 놓입니다).
  - **`createdAt`이 없으면 0이 아니라 '지금'입니다**(`memoTime`). 방금 보낸
    메모는 서버가 답하기 전까지 이 값이 null로 옵니다(`serverTimestamp()`) —
    0으로 치면 그 글이 **대화 맨 위로 튀었다가** 서버 시각이 도착하는 순간
    맨 아래로 내려앉습니다(실제로 보이던 증상). `lib/store.js`의 정렬도
    같은 `memoTime`을 씁니다 — 두 곳이 다르면 왼쪽 목록과 오른쪽 대화의
    차례가 어긋납니다. (`deletedAt`을 판정에 쓰지 말라는 것과 같은 함정입니다)
- **왼쪽 목록은 스레드로 묶습니다**(`lib/memoThreads.js`의 `buildMemoThreads`,
  최근 다섯). 답장이 오간 것은 한 줄이고, 그 줄에는 **첫 메모**만 섭니다
  (마지막 말은 자꾸 바뀌어 이름표로 못 씁니다). 이렇게 묶어야 목록을
  **최근순**으로 세워도 대화가 흩어지지 않습니다.
  - **세우는 기준은 뿌리가 아니라 마지막 글**입니다. 어제 시작한 대화에 방금
    답이 오면 그것이 지금 볼 것인데, 뿌리 기준이면 아래에 묻힙니다.
  - 스레드는 **저장하지 않고 화면에서 이어 붙입니다** — 문서에 `threadId`를
    두면 답장마다 그 값을 옳게 넣어야 하고, 한 번 어긋나면 대화가 둘로
    갈라진 채 되돌릴 길이 없습니다. `replyToId` 링크만 따라 올라갑니다.
  - 중간 글을 '거두기'로 지우면 링크가 끊깁니다(가리키는 문서가 없음). 그때는
    **거기서 멈춰** 그 글이 새 뿌리가 됩니다 — 남은 대화가 통째로 사라지는
    것보다 낫습니다. 고리 방어(`seen`)도 함께 — 없으면 화면이 멈춥니다.
  - **모둠원 칩은 '새로 쓰기'**, 목록은 '이어 가기'입니다. 대화 안에서 쓴 글은
    답장할 글을 따로 고르지 않았으면 **그 대화의 마지막 글**에 답한 것으로
    둡니다 — 안 그러면 대화 안에서 쓴 글이 새 스레드로 갈라집니다.
- 읽는 것은 **이 반에서 내가 주고받은 메모 전부**입니다
  (`subscribeMyGroupMemos` — `fromUid==나` · `toUid==나` 등호 하나짜리 둘).
  친구를 고를 때마다 그 짝의 리스너를 새로 걸던 것을 접었습니다 — 목록도
  대화도 같은 자료를 나눠 씁니다. 더 줄이려면 `createdAt` 정렬 + limit이
  필요한데 그건 복합 색인이 생깁니다(모둠 친구 서넛과의 쪽지라 그만큼
  커질 자리가 아닙니다).
- **`users/{uid}/notifications`에 쓰지 않습니다.** 그 자리는 본인만 읽고
  쓰게 잠가 둔 곳이라(교사도 못 읽습니다) 학생끼리 서로의 알림함에 쓰게
  열면 그 잠금이 무너지고, 서버 함수로 우회하면 이 기능 하나 때문에
  Cloud Functions 배포가 걸립니다. 대신 **알림 벨이 두 곳을 함께 구독해
  한 목록으로** 보여 줍니다(`NotificationBell` — `_src`로 갈라 읽음 처리도
  저마다). 메모 알림을 누르면 `/study?memo=<보낸사람>`으로 갑니다.
- **읽음은 그 대화를 연 순간** 바뀝니다(모달이 처리). 알림의 '읽음'을 따로
  누르게 하면 읽고 답까지 했는데 배지가 남습니다.
- **질의는 등호뿐**이라 복합 색인이 없습니다. 다만 **질의가 `toUid`/`fromUid`를
  증명하지 못하면 거부됩니다**(실측) — list 규칙은 결과 문서가 아니라
  **질의**로 판정해서, `pairKey` 하나만 걸면 "Property toUid is undefined
  on object"로 떨어집니다. 그래서 보낸 것·받은 것 **둘로 나눠** 구독합니다
  (책방 동료 평가와 같은 이유·같은 해법).
  `tests/rules/groupMemos.test.mjs`에 시험으로 박아 두었으니 지우지 마세요.
- 규칙: 읽기는 **주고받은 두 사람만**(교사도 못 읽습니다 — 누가기록과 주인이
  반대입니다). 쓰기는 **같은 기본 모둠일 때만** — 반이 같은 것만으로 열면
  화면에 없는 길로 아무 급우에게나 보낼 수 있습니다. 규칙 언어에 반복이
  없어 모둠 여섯 개를 손으로 펼쳐 봅니다(`groupAssignments`가 6개로 이미
  못 박혀 있습니다). 문서가 없는 반에서 `get()`이 평가 오류로 끝나지 않게
  `exists()`를 앞에 둡니다. 받은 사람은 `read` 한 칸만, 지우는 것은 보낸
  사람만.
- `groupAssignments` **읽기도 그 반 학생에게 열었습니다**(`isClassMember`).
  여기 든 것은 이름·학번·이모지뿐이고 그건 이미 급우끼리 열려 있습니다.
  모둠을 짜는 것은 그대로 교사만.
- **CSS 접두사는 `gmemo-`**입니다. `.memo-list`·`.memo-item`은 이미
  **교사의 수업 메모**가 쓰고 있어(globals.css 1350줄 언저리) 같은 이름을
  아래에서 다시 쓰면 이쪽이 이겨 교사 화면이 함께 흔들립니다. 주인이 반대인
  두 '메모'라 이름도 갈라 둡니다.

### 책방 교사 화면은 넷 다 세 칸입니다

- 닿소리 채우기(`BookGroupBoard`)의 **왼쪽 목록 · 가운데 판 · 오른쪽 진행**을
  곁텍스트·RAFT·KWLS도 그대로 씁니다 — 뼈대도 CSS도 같은 것입니다
  (`.book-workspace` · `.book-group-rail` · `.book-workspace-center` ·
  `.dash-side`). 네 활동의 교사 화면이 서로 다른 모양이면 교사가 화면마다
  다시 익혀야 합니다.
- **예전에는 셋이 학생 카드를 가로 격자로 깔았습니다.** 카드를 누르면 화면이
  통째로 그 학생의 상세로 바뀌어서, (ㄱ) 옆 학생으로 옮기려면 '← 학생 목록'
  으로 나갔다 다시 들어가야 했고 (ㄴ) 상세를 보는 동안 반의 진행 상황이
  화면에서 사라졌습니다. 목록을 왼쪽에 세우면 둘 다 풀립니다.
- 공용 조각은 둘입니다 — `BookStudentRail`(왼쪽) · `EntryProgressPanel`
  (오른쪽). 활동이 넘기는 것은 **단계 정의(`rows`)와 칸 색(`cellState`)**
  뿐이고, 그 둘은 **전광판과 같은 것**을 씁니다(`lib/paratext.js`의
  `paratextRows`·`paratextCellState`, `lib/raft.js`, `lib/kwls.js`).
  한쪽만 고치면 같은 학생이 두 화면에서 다른 상태로 보입니다.
- **머리말 둘째 줄은 한 줄에 다 섭니다** — 주제어 배지 · 반 고르개 ·
  진행 요약(`시작 n명 · 완성 n명 / 전체 N명`) · (전광판) · (잠김 안내) ·
  동료 평가 여닫기. 배지 두어 개뿐이라 오른쪽이 늘 비어 있는데 진행 요약이
  아래에 제 줄을 차지하고 있었습니다(곁텍스트만 처음부터 이 줄이었고
  나머지 셋이 어긋나 있었습니다). 줄 하나가 줄면 그만큼 카드가 위로
  올라옵니다.
  - **전광판은 서는 자리가 둘입니다** — 모둠 칩이 있으면 그 줄 끝(모둠을
    다 지나온 자리), **칩이 없으면 이 머리말의 진행 요약 옆**입니다.
    KWLS는 모둠이 없고 곁텍스트·RAFT도 개인 활동이면 칩이 없는데, 그때
    단추 하나를 세우려고 줄 하나를 통째로 쓰고 있었습니다. 칩이 없으면
    `GroupFilterRow`를 아예 안 그립니다.
  - 이 줄에 설 때는 크기를 옆 배지에 맞춥니다(13px · padding 6/12) —
    제 줄에서 쓰던 12.5px · 6/14로 두면 2px 높아 줄이 어긋납니다.
- **제목은 늘 활동 이름입니다.** 예전에는 학생을 열면 제목이 학생 이름으로
  바뀌었는데, 그러면 지금 어느 활동인지 화면에 없습니다. 학생 이름은 가운데
  칸의 머리(`.entry-detail-head`)가 말합니다 — 왼쪽 목록의 켜진 카드만으로는
  약합니다.
- **구르는 것은 가운데 칸 하나**입니다(`.book-workspace-center`의
  `overflow-y`). 페이지가 통째로 구르면 왼쪽 목록과 오른쪽 패널이 함께 밀려
  올라가, 학생을 바꾸려면 도로 올라와야 합니다. 그래서 세 칸 안에서는 KWLS
  네 칸의 `min-height: 100vh`도 풉니다(안 풀면 늘 스크롤이 생깁니다).
- **가운데 칸 안의 배치는 `@container`로 접습니다** — 화면 폭 기준 미디어
  쿼리는 여기서 안 듣습니다. 창이 1900px이어도 양옆에 '오늘' 패널·학생
  목록·진행 패널·'멋진 순간'이 서면 가운데는 600px밖에 안 남는데, 미디어
  쿼리는 창만 보고 '넓다'고 판단합니다. 실제로 KWLS 네 칸이 130px씩으로
  짜부라져 '이미 알고 있는 것'이 한 글자씩 세로로 쪼개지고 가로 스크롤이
  생겼습니다. `.book-workspace-center`에 `container-type: inline-size`를 주고
  `@container (max-width: …)`로 접습니다(KWLS는 1000px에서 두 칸, 620px에서
  한 칸). 이 칸 안에 새로 여러 칸짜리 배치를 넣을 때는 **미디어 쿼리 말고
  컨테이너 질의**를 쓰세요.
- **RAFT의 다섯 칸은 두 칸 고정**입니다(`.raft-detail-grid`) — R·A 한 줄,
  F·T 한 줄, 글쓰기가 아래 한 줄(`.wide`). `.entry-detail-grid`의 `auto-fill`을
  그대로 두면 가운데 칸 폭에 따라 한 줄에 셋이 서다 둘이 서다 하고, 셋이 서는
  화면에서는 R·A·F 다음에 T 하나만 덩그러니 남아 네 요소가 한 묶음으로 안
  읽힙니다. RAFT는 개수가 넷으로 못 박힌 틀이라 폭에 맡길 것이 아닙니다.
  900px 이하에서는 한 줄에 하나(두 칸을 우겨 넣으면 글이 자꾸 접힙니다).
- 오른쪽 패널은 **모둠으로 좁혀 봐도 반 전체**를 셉니다(닿소리의 개별
  활동과 같습니다 — 한 모둠만 보면 견줄 대상이 없습니다). 왼쪽 목록만
  모둠으로 좁힙니다.
- 색은 **줄 번호로**(`ROW_COLORS` 10색 되풀이). 이 학생들은 모둠 안 자리가
  없어 모둠 색을 쓸 수 없고, 붙어 있는 줄끼리만 달라도 충분합니다.
- **읽는 문서가 늘지 않습니다** — 보드가 이미 구독해 둔 `entries`를 그대로
  나눠 씁니다(닿소리 패널은 판마다 낱말을 따로 구독해야 했지만, 이쪽은 학생
  한 명이 문서 한 장이라 셀 것이 이미 손에 있습니다).

### 수업 화면 창 — 지금 띄우는 것을 교사도 (`CastStageModal`)

- 영역의 **'수업 시작'을 누르면 큰 창이 함께 열립니다.** 지금 학생 화면에
  떠 있는 그 내용을 교사도 보고, **같은 영역을 학생별로** 넘기며 그 자리에서
  과일을 줍니다. 곁텍스트·RAFT·KWLS 셋이 **같은 창 하나**를 씁니다 — 방송
  배관(`castCard`·`castIndex`·`step`)이 셋 다 같은 모양이라서요.
- **왜**: 교사 화면에 있던 것은 머리말의 방송 막대 한 줄(`CastBar`)뿐이라,
  칠판에 무엇이 떠 있는지 볼 수 없었고 **다음 학생의 같은 항목으로 넘길 길이
  아예 없었습니다** — 학생을 바꾸려면 왼쪽 목록에서 고른 뒤 그 영역의
  '수업 시작'을 다시 눌러야 했습니다(실제 신고).
- **축이 둘이고 주인이 다릅니다.**
  - **학생 축**(주인공) — 같은 영역을 학생별로. 창 아래 큰 단추 둘 + 키보드
    ← →. 툴팁이 **갈 학생을 미리** 적습니다(KWLS 크게 보기의 화살표와 같은
    방식).
  - **영역 축**(딸림) — 같은 학생의 다른 영역. `CastBar`가 하던 그것인데,
    창이 그 막대를 덮으므로 여기에도 작게 둡니다(12.5px).
  - **키보드는 학생 축에만** 겁니다. 한 번 눌러 둘이 함께 움직이면 어느 쪽을
    넘긴 것인지 알 수 없습니다(수업 화면의 슬라이드 넘기기와 같은 까닭).
- **넘기면 학생 화면도 함께 바뀝니다.** 이 창이 보여 주는 것이 곧 '지금
  띄우는 것'이라, 창에서만 넘어가면 창이 거짓말을 합니다. '미리보기'로
  갈라 두는 길도 있었지만, 그러면 지금 뜬 것과 고른 것을 수업 중에 둘 다
  머리에 둬야 합니다.
- **닫기(×·Esc·배경)는 수업을 끝내지 않습니다** — 창만 걷고 방송은 그대로
  둡니다. 끝내는 길은 '수업 종료' 하나뿐. 닫아 둔 창은 방송 막대의
  **'수업 화면 보기'**로 다시 엽니다(`CastBar`의 `onOpenStage`) — 없으면
  다시 보려고 수업을 껐다 켜야 합니다.
- **영역만 옮길 때는 창 상태를 안 건드립니다**(`castSection(card, i, openStage)`
  의 셋째 인자 — `step()`이 지금 상태를 그대로 물려줍니다). 닫아 둔 창이
  '다음 영역 →'에 되살아나면 닫은 뜻이 없어집니다.
- **같은 자리를 다시 누르면 방송이 꺼집니다**(`useEntryCast`의 토글) — 그때는
  창도 함께 닫습니다. 안 그러면 '띄우는 중'이라 적힌 빈 창이 남습니다.
- 넘길 때 **뒤의 화면도 따라옵니다**(`setOpenUid`) — 창을 닫았을 때 그 학생이
  열려 있어야 방금 본 답을 이어서 읽습니다.
- **학생 축의 차례는 왼쪽 목록에 보이는 그대로**입니다(모둠으로 좁혀 놓았으면
  그 모둠 안에서). 지금 띄우는 학생이 그 목록에 없으면(다른 모둠을 띄운 채
  좁혔을 때) 반 전체로 되돌아갑니다 — 안 그러면 넘길 곳이 없습니다.
- **본문은 학생 화면과 같은 것**을 씁니다(`.entry-cast-body` ·
  `.paratext-read-field`). 교사가 보는 것이 곧 칠판에 뜬 것이어야 하므로 글자
  크기(19px)도 그쪽 것을 그대로 물려받습니다. 칠판용 `data-size` 클램프는
  **안 붙입니다** — 그것은 `.entry-cast[data-size]` 아래에만 걸려 있어
  창에서는 안 커집니다.
- 과일은 **그 학생 문서 한 건**만 구독합니다(`subscribeMyClassRewardCount`).
  반 전체 `rewards`를 받으면 학생을 넘길 때마다 스물몇 건이 따라옵니다.
  주는 길은 **델타**(`addStudentReward(+1)`) — 카드 격자·수업 노트와 같습니다.
- **`.modal`이 `max-width: 640px`으로 못 박아 두어** width만 키우면 안
  넓어집니다(실측 640 그대로였음). `max-width: 960px`을 함께 올리고,
  `.modal`의 `overflow-y`도 꺼서 **구르는 것은 본문 한 칸**뿐입니다 —
  머리줄과 학생 넘기는 줄은 늘 제자리에 있어야 손이 같은 곳을 짚습니다.
- 머리줄 오른쪽 끝의 과일·닫기는 **한 묶음**입니다(`.cast-stage-tools`).
  따로 두고 저마다 `margin-left: auto`를 주면 남는 폭을 둘이 나눠 가져
  그 사이가 벌어집니다.
- 실측(1280·820px): 창 960/772px · 오른쪽 묶음이 창 끝에서 18px · 가로 넘침 0.

### 책방 전광판 — 곁텍스트 · RAFT · KWLS (교사)

- 세 활동 화면의 **모둠 고르는 줄 끝**(마지막 모둠 뒤)에 `전광판`.
  KWLS는 모둠이 없는 활동이라 그 줄에 단추 하나만 섭니다.
  **여러 단계 × 반 전체**를 한 격자로 엽니다 — 한 줄이 단계 하나, 한 칸이
  학생 한 명(가로 배치). 공부방의 `StudyProgressBoard`를 옮긴 것이라
  **격자도 CSS도 그대로** 씁니다(`.progress-grass`·`.grass-*`·
  `.progress-tip`) — 두 방의 전광판이 다른 모양이면 교사가 같은 것을 두 번
  익혀야 합니다.
- **껍데기는 `BookProgressBoard` 하나**입니다. 무엇이 한 줄인가와 칸 색을
  어떻게 고르는가만 활동이 넘깁니다 — `ParatextProgressBoard`(여덟 단계,
  '읽는중 전광판') · `RaftProgressBoard`(R·A·F·T + 글쓰기 다섯 줄,
  '글쓰는중 전광판') · `KwlsProgressBoard`(K·W·L·S 네 줄, '성찰중 전광판').
  활동이 늘어도 껍데기는 안 고칩니다.
- **RAFT는 다섯 줄 — 글쓰기를 빼지 마세요.** 넷은 계획이고 글쓰기가 결과물이라,
  넷만 보여 주면 '다 정해 놓고 아직 안 쓴 학생'이 다 한 것처럼 보입니다
  (학생 카드의 네모 넷이 실제로 그렇습니다 — 거기서는 아래 '글 n자'가 그것을
  말해 주지만 격자에는 그럴 자리가 없습니다). 글쓰기 줄만 '쓰는 중'이 있고,
  기준은 공부방이 이미 쓰는 `DONE_MIN_CHARS`를 그대로 씁니다 — 이 앱에서
  '충분히 썼다'는 선은 한 곳에만 있어야 합니다.
- **이름은 '모아 보기'가 아닙니다.** `모아보기`는 이미 다른 뜻입니다 —
  '한 칸에 대한 반 전체의 답을 한 화면에'(`StudyActivityWall`, 공부방 활동
  줄·`TeacherKwlPanel`·발표 오버레이·책방 모아보기 화면들). 띄어쓰기만 다른
  이름을 붙이면 두 기능이 섞입니다. 공부방과 같은 낱말인 **전광판**으로 뒀고,
  창 제목은 `공부중 전광판`과 짝이 되게 `읽는중 전광판`입니다.
- **반 전체를 보여 줍니다**(`cards`, `shownCards` 아님). 모둠을 다 지나온
  자리에 단추를 두는 까닭이 그것입니다. 모둠이 없는 활동에서는 칩 줄 없이
  그 단추만 섭니다(`GroupFilterRow`의 `trailing`).
- **단계 이름표는 한 줄입니다**(이름 · 상태 · 진행률). 아래에 물음(`prompt`)을
  한 줄 더 깔았었는데, 여덟 줄이 모두 말줄임으로 잘려 '무엇을 묻는지'는 어차피
  안 보이면서 격자만 두 배로 길어졌습니다. 물음은 마우스를 올리면 뜹니다.
  세 칸은 **px로 못 박습니다**(`17px 1fr 58px 38px`) — 줄마다 따로 선 격자라
  `auto`로 두면 `0/29`와 `26/29`, `표지`와 `참고문헌`이 저마다 다른 자리에
  섭니다. 이름 칸 폭은 활동마다 달라 `--head-w`로 넘깁니다(곁텍스트 232px ·
  RAFT 176px — 가장 긴 이름에 맞춘 실측값이라 이름을 늘리면 다시 재세요).
- **`.bookboard--nolock`(RAFT의 배지 없는 세 칸)은 반드시 기본 규칙 뒤에.**
  특이도가 같아(0,2,0) 앞에 두면 기본의 네 칸이 이겨, 없는 배지 칸이 96px을
  차지한 채 이름 칸만 28px로 짜부라집니다(실제로 그랬습니다).
- **칸 색은 학생 카드의 네모(`.paratext-mark`)와 같은 값**입니다 —
  초록(다 씀)·주황(쓰는 중)·회색(시작 전)·빗금(잠김). 같은 화면에서 카드를
  보다가 격자를 여는데 색이 다르면 두 번 익혀야 합니다. 공부방에 없는
  '쓰는 중'이 여기 있는 까닭은 한 단계가 여러 칸인 것이 셋(제목 5칸 ·
  목차 3칸 · 머리말 2칸)이라 '반쯤 쓴' 상태가 실제로 자주 생겨서입니다.
  칸 상태를 고르는 차례는 공부방과 같습니다 — **쓴 것이 가장 세고**,
  그다음이 잠김.
- **학생 이름을 세로쓰기로 적습니다**(`.grass-name`). 공부방 전광판은 이름을
  아예 안 적고 툴팁에 맡기는데, 여기는 줄이 여덟뿐이라 이름 줄 하나를 얹을
  자리가 남고, 칠판에 띄워 함께 보는 자리라(마우스를 못 올립니다) 누구인지가
  화면에 적혀 있어야 합니다. 칸이 22px이라 가로로는 못 적습니다.
- **붙어 있는 칸(sticky)의 바탕은 그 모달의 바탕과 같아야 합니다.** 공부방의
  `.grass-act`는 크림색(`--bg`)인데, 이름 줄이 스물아홉 칸으로 늘어서니 그
  색이 칸마다 끊겨 머리줄이 아니라 **알약 스물아홉 개**로 보였습니다. 뿌리
  클래스 `.bookboard` 아래에서 흰색으로 덮습니다.
- **뿌리 클래스를 `.paratext-progress`로 짓지 마세요.** 그 이름은 이미 활동
  만들기 창들(`ParatextForm`·`RaftForm`·`KwlsForm`·`MindmapForm`)과 학생
  카드의 진행 배지가 쓰고 있어, 모달에 붙이면 창 전체가 `--primary-light`로
  물듭니다(실제로 겪었습니다 — 모둠 메모의 `.memo-list` 충돌과 같은 함정).
  `.book-progress`도 피했습니다 — `.book-progress-legend`·`-done`·`-sum`이
  이미 따로 있어, 뿌리로 쓰면 그것들이 이 모달의 자식처럼 읽힙니다.
- **출석은 안 봅니다.** 공부방 전광판의 '오늘 결석' 색을 여기 두려면 이
  화면이 출석 기록을 새로 구독해야 하고, 곁텍스트는 여러 차시에 걸쳐 채우는
  활동이라 '오늘 결석'이 빈칸의 이유가 되지도 않습니다.
- **읽는 문서가 늘지 않습니다** — 단계별 인원도 완성 인원도 두 보드가 이미
  구독해 둔 `entries`로 셉니다.

### 전광판 — 상단바에서 학생 칭찬하기 (`meta/marquee`)

- `components/AppMarquee.jsx`. 교사가 알리고 싶은 한 줄을 걸어 두면
  **상단바에 돌아가며** 섭니다('이달의 주니어 개발자 10101 홍길동').
  상단바는 다섯 화면에 모두 떠 있어 어디에 있든 눈에 들어옵니다.
- **배움나눔 전체에 하나입니다 — 반마다 따로가 아닙니다.**
  한때 `classes/{cId}.marquee`에 담아 반마다 달랐는데 두 가지가 어긋났습니다:
  (ㄱ) 상단바는 **반을 안 고르는 화면**(질문방·리포트)에도 떠 있는데 거기서는
  아무것도 안 걸렸고, (ㄴ) 교사가 반을 옮길 때마다 걸린 글이 갈려 '지금 무엇이
  걸려 있나'를 반마다 따로 기억해야 했습니다. 칭찬은 학교 전체에 알리는
  것이라 한 자리에 두고 모두가 같은 것을 봅니다(실제 신고).
- **`meta/marquee` 문서 하나의 배열 한 필드**입니다(`marquee: [{id,text,at}]`).
  이 컬렉션은 읽기가 이미 `signedIn()`, 쓰기가 `isTeacher()`라 전역으로
  옮기면서도 **규칙을 한 줄도 안 건드립니다** — 반 문서에 담았던 까닭과 같은
  이유입니다(원래 `meta/keywordsSeeded` 플래그가 쓰던 자리).
  - **`setDoc(merge)`로 씁니다** — `updateDoc`은 문서가 없으면 실패하는데,
    이 문서는 교사가 **처음 글을 걸 때 비로소 생깁니다**.
  - 시각은 `Date.now()`입니다 — `serverTimestamp()`는 **배열 안에 못 넣습니다**
    (Firestore가 배열 속 센티널 값을 거부합니다).
- 읽기는 `subscribeAppMarquee`(문서 하나)이고 **컴포넌트가 제 구독을 스스로
  겁니다** — 반이 없으니 위에서 내려 줄 것도 없습니다.
  - `TopNav`의 `subscribeClass`(반 문서)는 **그대로 남습니다** — 이제 그것은
    '오늘의 활동'(`classTaskOf`) 하나만을 위한 구독입니다. 전광판이 그 문서를
    같이 쓰던 시절의 '읽는 문서가 안 는다'는 설명은 더 이상 안 맞습니다:
    전광판이 **문서 하나를 따로** 구독합니다(반 수와 무관한 고정 1건).
  - 옛 반 문서의 `marquee` 필드는 **읽는 곳이 없어졌습니다.** 지우지 않고
    두었습니다 — 남아도 해가 없고, 지우려면 반마다 쓰기가 한 번씩 듭니다.
    다만 **옮겨 오지도 않습니다**: 전역 전광판은 빈 채로 시작하므로 걸어 둔
    글은 한 번 다시 적어야 합니다.
- **흐르는 글씨가 아닙니다.** 7초에 한 번 조용히 갈립니다 — 수업 중 화면
  위에서 끊임없이 움직이면 눈길을 계속 뺏습니다. 글이 하나뿐이면 아예 안
  바꾸고(바뀔 것이 없는데 타이머만 돕니다), 움직임을 줄여 달라고 한 기기
  에서는 전환도 없앱니다.
- **빈 전광판은 교사에게만 보입니다.** 학생 화면에는 아무것도 안 그립니다 —
  빈 알약이 상단바에 남아 있을 이유가 없습니다.
- **글자는 15px, 이모지는 안 답니다.** 상단바에서 눈에 들어와야 하는 한 줄이라
  본문(13px)보다 큽니다. 옆 문구(`.topbar-tagline`, 세리프 19px)보다는 작게
  둬 둘이 경쟁하지 않게 합니다. 처음에는 앞에 📣를 달았는데, 이 줄에서 읽을
  것은 학생 이름 하나뿐이라 그림이 앞에 붙으면 그만큼 글자가 밀리고 눈이 먼저
  그림을 짚었습니다. **여기가 전광판이라는 것은 알약 바탕이 이미 말합니다.**
- **연한 회색 바탕(`--neutral`)에 검은 글자(`--dark`)입니다.** 살구색 바탕에
  주황 글자였는데, 상단바에서 가장 먼저 읽혀야 하는 줄이 색으로 튀기만 하고
  정작 글자는 덜 또렷했습니다. 바탕을 죽이고 글자를 살립니다. 점과 연필도
  주황을 걷어 먹빛·`--text-sub`으로 맞췄습니다 — 알약 하나에 색이 둘 남으면
  바탕만 회색으로 바꾼 티가 납니다.
- 알약은 **글자만큼만** 차지합니다(`flex-basis: auto`). `flex: 1`로 남는 가로를
  다 받게 했더니 글이 짧을 때 900px짜리 빈 띠가 생겨 알약이 아니라 넓은 판으로
  보였습니다(실측). `max-width`는 560px — 1행이 통째로 비어 있어 이 값을 풀면
  긴 글이 상단바를 가로지르는 띠가 됩니다.
- **왼쪽 묶음(로고·문구)과 오른쪽 묶음(아이콘) 사이, 그 한가운데에 섭니다**
  (`.topbar-mq-slot`의 `justify-content: center`). 그 칸이 사이의 남는 가로를
  통째로 받고 알약은 그 안에서 가운데 서므로, **좌우 여백이 늘 같습니다** —
  실측 1920px에서 408·408 · 1366px에서 131·131 · 1024px에서 119·119.
  - 여기까지 오면서 두 번 어긋났습니다. **오른쪽 끝에 붙였을 때**는 그 앞이
    1,100px 비어 알약이 구석에 밀려난 것처럼 읽혔고, **아이콘을 2행으로
    내렸을 때**는 1행 오른쪽이 통째로 비어 '가운데'가 화면 중심보다 224px
    오른쪽이었습니다(둘 다 실제 신고). 아이콘이 1행 오른쪽 끝을 잡아 주어야
    이 칸의 가운데가 곧 눈에 보이는 가운데가 됩니다.
  - 한 줄이던 시절의 '남는 가로는 뒤에' 규칙은 여기서 안 통합니다 — 그때는
    알약이 제 글자 폭으로 직접 서서 남는 가로를 어느 쪽에 두느냐가 곧 글자
    자리였지만, 지금은 이 칸이 그것을 받고 알약은 제 글자만큼 자랍니다.
- **감싼 칸 `.topbar-mq-slot`을 없애지 마세요.** 남는 가로를 받는 것은 그 칸
  (`flex: 1 1 0`)이고 알약은 그 안에서 가운데에 서서 글자만큼만 자랍니다.
  알약이 직접 `flex-basis: auto`로 서면 **그 글자 폭이 그대로 줄바꿈을 부릅니다**
  — 769px에서 알약이 로고·문구 아래로 떨어져 상단바가 네 줄이 되었습니다(실측).
  줄을 나누는 셈은 **줄인 뒤가 아니라 기준 폭으로** 하기 때문입니다.
  768px 이하에서는 **알약이 아니라 이 칸을** 접어야 합니다 — 칸만 남으면 그것이
  첫 줄의 남는 가로를 통째로 받아 아이콘 줄이 로고 옆에 붙습니다(실측 400px에서
  사용자 영역 x=108 → 고친 뒤 280).
- **손바닥 자리를 비워 두는 것은 그대로입니다**(`.question-signal-hold`).
  아이콘 줄이 1행 오른쪽 끝을 잡고 있어, 수업 중에 손이 오르내릴 때마다
  그 줄이 28px씩 들썩이면 **전광판의 가운데도 14px씩 함께 움직입니다.**
  손바닥은 인원수를 숫자로 안 달아 늘 28px 고정 — 그 값을 고치면 빈 상자도
  함께 고치세요.
- **줄이 꽉 차면 이 칸이 먼저 줄어듭니다**(`flex-shrink: 100`) — 모자란 만큼을
  전광판이 거의 다 떠안아 글이 말줄임으로 접힙니다.
- `min-width`는 그 **바닥(140px)**입니다. `0`으로 두면 좁은 화면에서 28px짜리
  부스러기가 됩니다(실측). **`auto`로 되돌리지 마세요** — flex 항목이 내용보다
  좁아지기를 거부해 긴 글이 줄을 넘칩니다.

**상단바는 두 줄입니다 — 1행 로고·문구·전광판·아이콘 / 2행 이동 메뉴**

한 줄에 여섯을 세우던 것을 나눴습니다. 폭이 좁은 기기(크롬북)에서 뒤쪽이
잘렸고, 넓은 화면에서도 **전광판이 늘 모자랐습니다** — 안 줄어드는 문구
285px이 함께 서서 1600px에서도 알약이 바닥(140px)에 붙어 글이 접혔습니다.
이동 메뉴 505px을 2행으로 내리면 그만큼이 통째로 전광판 자리가 됩니다
(실제 신고에서 나온 요청).

- **내려간 것은 이동 메뉴 하나뿐입니다.** 손들기·확성기·알림·프로필은
  **1행 오른쪽 끝에 그대로** 둡니다 — 한때 메뉴와 함께 내렸다가 되돌렸습니다:
  1행 오른쪽이 통째로 비어 전광판의 '가운데'가 화면 중심보다 224px 오른쪽이
  되고, 아이콘도 메뉴 줄에 얹혀 눈에 덜 들어왔습니다(실제 신고).
  **아이콘이 오른쪽 끝을 잡아 주어야** 그 사이의 가운데가 눈에 보이는
  가운데가 됩니다.
- **모든 폭에서 두 줄입니다.** 폭에 따라 한 줄↔두 줄이 갈리면 '좁혔는데 문구가
  나타나는' 모양이 생기고 지킬 규칙도 그만큼 늡니다. 예전에는 1025px 위가
  한 줄 · 769~1024px이 두 줄 · 768px 이하가 또 다른 두 줄로 **셋**이었는데
  지금은 **하나**입니다 — 768px 이하도 줄 나누는 방식은 같고, 가운데의
  문구와 전광판만 빠집니다.
- 차례는 로고(1) · 문구(2) · 전광판(3) · 아이콘(4) · 줄바꿈(5) · 메뉴(6).
  `globals.css`의 order 값이 그 차례이고, **`TopNav.jsx`에 적힌 차례도
  같습니다** — CSS의 order는 자리만 바꾸고 탭 이동 차례는 안 바꿔서, 둘이
  어긋나면 눈으로 보는 차례와 키보드로 옮겨 다니는 차례가 갈립니다. 요소를
  더할 때 둘을 함께 맞추세요.
- **좁아지면 가운데부터 빠집니다.** 1행에 넷이 서므로 **문구가 먼저**
  (1180px), 그다음이 전광판(768px)입니다. 넷을 다 세우고 전광판 글자까지
  온전히 보이려면 1,150px 남짓이 필요한데, 그 아래에서 문구를 남기면 알약이
  바닥까지 눌려 이름이 두어 자만 보입니다. 문구를 접으면 그 285px이 통째로
  전광판 자리가 됩니다. **로고 옆 세로선(`.topbar-divider`)도 함께**
  접습니다 — 문구를 가르려고 있는 선이라 혼자 남으면 짧은 막대가 됩니다.
  (1180px은 한 줄이던 시절 전광판이 접히던 그 폭입니다 — 양보하는 쪽이
  전광판에서 문구로 바뀐 것뿐입니다.)
- 줄을 가르는 것은 **`.topbar-gap`**입니다 — 넓은 화면에서 남는 가로를 받던
  그 칸을 폭 100%·높이 0으로 바꿔 '여기서 줄을 나눠라'로 씁니다(요소를 새로
  만들지 않습니다). 눈에는 두 줄이지만 flex 줄로는 셋입니다(가운데가 높이 0).
- **이동 메뉴도 기준 폭이 0이어야 합니다**(`flex: 1 1 0`). 제 내용 폭(505px)을
  기준으로 두면 좁은 화면에서 줄이 하나 더 접힙니다(실측 — 상단바가 네 줄이
  됐습니다). 단추는 왼쪽부터 차므로 칸이 넓어져도 보이는 모습은 그대로이고,
  좁아지면 **단추를 줄이는 대신 줄이 옆으로 구릅니다**(`overflow-x` + 단추의
  `flex-shrink: 0`). 예전에는 줄이 꽉 차면 브라우저가 글자를 쪼개 '배움나눔'이
  '움/나'로, '질문방'이 '질/문/방'으로 세로로 섰습니다(실제 신고).
- **실측한 자연 폭**(교사 화면): 로고 129 · 문구 285 · 전광판 421(가장 긴
  '배움나눔의 오류를 찾아낸 주니어 개발자, 김주은!!') · 사용자 영역 215 ·
  이동 메뉴 505.
- **실측(400~1920px)**: 가로 넘침 **0**, 상단바 높이 **91px**(769px 위) ·
  103~119px(768px 이하, 예전과 같음). 전광판 좌우 여백은 1920px에서 408·408 ·
  1366px에서 131·131 · 1024px에서 119·119로 **정확히 같습니다**. 글자가
  말줄임으로 접히는 것은 **820px 아래**뿐입니다.
- 대가는 세로 **35px**(56 → 91px)입니다. 화면 높이를 쓰는 화면들
  (`.board-shell` 따위)은 모두 세로 flex라 남는 자리를 알아서 채웁니다 —
  56px을 못 박아 둔 곳은 없습니다(확인함).
- 이동 메뉴 단추의 좌우 여백이 기본 `.btn-ghost`(6/12)보다 좁은 것은 그대로
  둡니다(8px · 아이콘 간격 5px). 두 줄이 되며 급하지 않게 됐지만, 다섯 개가
  한 줄에 서는 자리라 여백 1px이 다섯 배로 돌아옵니다. **`.btn-ghost` 자체를
  고치지 마세요** — 앱 전체가 쓰는 클래스입니다.
- 저장은 **'저장'을 눌러야** 반영됩니다. 상단바에 바로 걸리는 글이라, 적는
  도중의 반쪽짜리 문장이 학생 화면에 뜨면 안 됩니다.
- **로그아웃은 프로필 메뉴 안입니다.** 상단바 오른쪽 끝에 아이콘+글자 단추로
  나와 있던 것을 옮겼습니다 — 전광판이 생기면서 그 폭(90px 남짓)이 아까웠고,
  로그아웃은 하루에 한 번 누를까 말까 한 것이라 늘 자리를 차지할 일이
  아닙니다. 메뉴의 **마지막에 선으로 갈라** 둡니다(`.profile-menu-item--out`)
  — 위 둘은 '여는 것'이고 이것은 '나가는 것'이라, 나란히 두면 잘못 눌러
  로그아웃됩니다. `TopNav`가 `onLogout`으로 넘겨 줍니다.
- **mock은 반 문서를 갈아 끼웁니다**(제자리에서 고치지 않고). Firestore는
  스냅샷마다 새 객체를 주는데 mock이 같은 객체를 고쳐 두면 **문서 하나를
  구독하는 쪽이 늘 같은 참조를 받아 React가 다시 그리지 않습니다** — 데모
  모드에서 교사 화면만 바뀌고 학생 화면은 그대로인 일이 실제로 났습니다.
  `subscribeClass`도 얕은 사본을 넘겨 둡니다(`setClassPyTarget` 등 제자리에서
  고치는 다른 쓰기 함수까지 함께 막으려고).

### 기록 관리 — 창 안에서 반 바꾸기 (교사)

- 머리말의 **반 고르개**(`.notes-mgr-class`)로 창을 닫지 않고 옆 반의 기록을
  봅니다. 예전에는 반 이름 배지뿐이라 창을 닫고 화면에서 반을 바꾼 뒤 다시
  열어야 했는데, 교사가 실제로 하는 일은 '오늘 세 반 중 누가 노트를 썼나'를
  훑는 것입니다. 판정도 클래스도 **책방 머리말의 반 고르개와 같습니다** —
  고를 반이 하나뿐이면 지금까지처럼 이름 배지입니다.
- **창을 연 그 반은 페이지가 만들어 둔 명단을 그대로 씁니다**(`isHome`).
  흔한 경우라 여기서 읽는 문서가 하나도 안 늡니다. 옆 반을 고른 동안만
  `subscribeClassMembers` + `fetchClassRosterProfiles`로 그 반 명단을
  만듭니다 — `subscribeUserDirectory`(users 통째로)를 안 쓰는 까닭은 필요한
  것이 그 반 학생 스물몇 명뿐이라 uid를 아는 사람만 한 건씩 읽는 편이
  좁아서입니다.
- **`otherRoster`는 `null`이면 '아직 안 왔다', `[]`면 '없다'입니다.**
  갈라 두지 않으면 반을 바꿀 때마다 '아직 이 반에 입장한 학생이 없어요'가
  한 번 스칩니다(위 '첫 화면' 절의 그 함정).
- 반을 바꾸면 **열어 둔 학생 창을 닫습니다**(`selected`를 비움) — 앞 반
  학생이 그대로 남으면 새 반 맥락에서 남의 반 기록을 보게 됩니다.
- 화면에서 반을 바꾸면 창도 따라갑니다(`classId` prop → `pickedId`).
- 두 탭이 함께 따라갑니다 — 누가기록은 `subscribeClassNoteCounts(pickedId)`,
  수업 노트는 `CornellNotesPanel`에 `pickedId`·고른 명단을 내려 줍니다.

**수업 노트 탭의 카드는 '색은 과일 · 글자는 피드백'입니다.** 한 카드가 두
가지 사실을 말하는데, **채널 하나가 사실 하나씩** 맡습니다.

| | 판정 | 화면 |
|---|---|---|
| 바탕색 | `rewardedAt` 도장이 있나 | 흰색(그날 안 씀) · 살구빛(`.has`) · **초록**(`.awarded`) |
| 배지 글자 | `feedback`에 글자가 있나 | `노트 있음` ↔ `피드백 남김` (안 쓴 학생은 **배지를 안 그림**) |

교사가 노트를 하나씩 열어 읽으며 과일을 주는 자리인데, 스물몇 장을 오가다
보면 누구까지 줬는지 잊습니다. 뱃지를 하나 더 다는 것보다 바탕색이 눈에 먼저
들어옵니다.

- **색에 피드백을 섞지 마세요.** 색이 '둘 중 몇 가지를 했나'(0·1·2)를 말하게
  하는 길도 따져 봤는데, 그러면 가운데 색 하나가 두 가지 뜻이 되어 **과일을
  알려면 색에서 글자를 빼는 셈**이 한 번 필요합니다. 이 바탕색을 넣은 까닭이
  '내가 과일을 줬나'를 색만 훑어 읽는 것이라 축을 갈라 둡니다(진하기로 개수를
  나타내지 않기로 한 잔디·'모둠원별 진행' 14칸과 같은 결).
  - 그 셈으로 갈 때는 **'옅은 살구'를 새로 만들 수도 없었습니다.** 지금 살구
    (`--primary-light` #f8ece5)가 흰 바탕과 대비 **1.16:1**이라 그보다 옅은
    색은 흰 카드(안 쓴 학생)와 안 갈립니다 — 같은 값이 `.cornell-zone--cue`와
    `.attend-seat--absent` 주석에 이미 '거의 안 보인다'로 적혀 있습니다.
  - **초록은 '진하기 사다리'에 안 올라갑니다** — 살구(#f8ece5)와 초록
    (#e8f7ed)의 밝기 차이가 1.05:1로 사실상 없고, 둘을 가르는 것은 색상과
    테두리입니다. '점점 진해진다'가 아니라 **'살구는 아직 · 초록은 됐다'**로
    적어 두세요.
- 초록은 출석부의 '출석' 자리(`.attend-seat--present`)와 **같은 값**
  (#e8f7ed / #a6d8b6)입니다 — 이 앱에서 초록은 늘 '됐다'는 뜻입니다.
- CSS는 `.has` **뒤에** 와야 이깁니다(특이도가 같습니다).
- **안 쓴 학생의 '없음' 배지는 없앴습니다** — 흰 카드가 이미 말하는 것을 한 번
  더 적는 말이었습니다. 흰 카드와 살구 카드의 밝기 차이가 옅은데(위 1.16),
  그 단서를 이제 **알약이 있고 없고**가 받칩니다. 그러니 자리를 맞추려고
  **빈 알약을 남기지 마세요** — 글자 없는 회색 알약은 단서 노릇을 못 하면서
  자리만 먹습니다. (실측: 배지를 걷어도 격자가 높이를 맞춰 카드 78px 그대로)
- **판정은 `isCornellRewarded` 한 곳** — 노트 문서에 **`rewardedAt` 도장이
  있나**, 그것뿐입니다.
  - **지급 이력으로 세지 마세요.** `rewardEvents`에는 `uid · delta · count ·
    byUid · at`뿐이고 **어느 화면에서 줬는지가 안 남습니다** — 자리표에서
    준 것, 발표 모드에서 준 것, 손들기의 '🍎 확인'이 전부 같은 모양입니다.
    한때 '그 날짜의 지급 이력'으로 셌는데, 그러면 수업 노트와 무관하게 준
    과일까지 초록이 되고(실측으로 확인) 9월 14일 노트를 15일에 읽고 준
    경우에는 **14일 화면이 끝내 안 초록**이었습니다.
  - 그래서 답을 **노트에 적어 둡니다**(`markCornellNoteRewarded` — 수업
    노트 화면의 과일 단추만 찍습니다). 그 화면이 이미 구독해 둔 문서라
    **읽기가 1건도 안 늡니다.** 찍는 곳이 그 단추 하나뿐이라, **도장이
    있다는 것 자체가 이미 '이 노트를 읽고 줬다'는 뜻**입니다.
  - **한때 '피드백을 쓴 날과 같은 날인가'까지 봤습니다** — 도장이 없던
    시절 지급 이력으로 세던 흔적인데, 도장이 생긴 뒤로는 그 조건이 덧붙은
    채로 두 가지를 망가뜨렸습니다.
    - 피드백 없이 격려만 준 노트가 **안 초록**.
    - `saveCornellFeedback`이 저장할 때마다 `feedbackAt`을 새로 찍으므로,
      **다음 날 피드백을 고치면 초록이던 카드가 살구빛으로 돌아갑니다.**
    - 실측(지금) — 오늘 피드백+오늘 과일 초록 · **과일만 줌도 초록** ·
      **어제 과일 + 오늘 피드백 고침도 초록** · 도장이 아직 null(서버
      대기)이면 아님.
  - **창의 과일 단추도 이 함수 그대로입니다** — 카드 색과 단추가 **같은
    판정 하나**를 봅니다. 그래서 화면을 설명하는 문장이 하나입니다:
    '이 노트에 과일을 줬으면 카드가 초록이고, 창에는 단추 대신 🍊 줬어요가
    선다.' (한때 단추만 '오늘 줬나'로 갈라 뒀습니다 — 아래 절 참고)
  - 노트가 없으면 찍을 자리도 없습니다(규칙이 노트 생성을 본인에게만 열어
    두어 교사가 대신 못 만듭니다). 읽을 노트가 없으니 뜻하는 일도 안 일어납니다.
  - **지난 기록은 초록이 안 됩니다** — 이 도장이 생기기 전에 준 과일에는
    표시가 없습니다. 앞으로 주는 것부터 붙습니다.
  - 실측(네 조합) — 과일X·피드백X `살구 + 노트 있음` · 과일X·피드백O
    `살구 + 피드백 남김` · 과일O·피드백X `초록 + 노트 있음` ·
    **닷새 전에 준 과일O·피드백O** `초록 + 피드백 남김` · 노트 없음
    `흰색 + 배지 없음`. 빈 알약 0개 · 카드 높이 다섯 장 모두 78px ·
    가로 넘침 0.
- **규칙을 열었습니다**(`rewardedAt`·`rewardedBy`) — 교사의 `changedOnly`에
  넣고, 학생 update에는 피드백·하이라이트와 같이 '그대로일 것'을 요구합니다
  (학생이 지우면 이미 준 학생에게 한 번 더 주게 됩니다).
  - 교사 갈래의 `feedback` 검사를 **`get`으로 바꿨습니다** — 피드백보다
    과일을 먼저 주면 그 칸이 아직 없어서, 그대로 읽으면 규칙이 평가 오류로
    끝나 **선생님조차 거부**됩니다.
  - **코드보다 규칙을 먼저 배포하세요.** 시험은
    `tests/rules/cornellNotes.test.mjs`(여섯 개 — 도장만 따로 찍기 · 피드백
    없는 노트에 찍기 · 본문 수정 끼워 넣기 거부 · 남의 반 교사 거부 ·
    학생이 지우기 거부 · 학생이 스스로 찍기 거부).

### 수업 중 활동 내보내기 (`classes/{cId}.task`)

교사가 수업 모드에서 **이미 만들어 둔 것 하나를 학생 화면으로 보내면**,
학생의 수업 노트 서랍에 '오늘의 활동' 탭이 서고 거기서 답을 씁니다. 보낼 수
있는 것은 둘이고, **저장되는 자리가 서로 다릅니다.**

| 보내는 것 | 저장되는 곳 | 학생이 이어 쓰는 곳 |
|---|---|---|
| 공부방 프로젝트의 **활동** | 그 프로젝트의 내 카드 | 공부방 |
| 곁텍스트 읽기의 **단계**(여덟) | 그 활동의 `entries/{uid}` | 책방 |
| RAFT의 **네 요소와 글쓰기**(다섯) | 그 활동의 `entries/{uid}` | 책방 |

두 방의 말이 짝을 이룹니다 — 공부방 **프로젝트**:**활동** = 책방 **독서
활동**:**단계**. 바깥 껍데기의 잠금은 '이건 끝냈다'라 보낼 수 없고, 안쪽
칸의 잠금은 '아직 진도가 거기까지 안 나갔다'라 보내면서 열립니다.

- 반 문서의 값은 `{ kind, …, at }`입니다 — `study`면 `boardId`·`actIndex`,
  `book`이면 `activityId`·`sectionKey`. **`kind`가 없는 옛 값은 공부방으로
  읽습니다**(이미 나가 있는 것이 그대로 굴러가야 합니다).
- **한 번에 하나뿐입니다.** 새로 보내면 앞의 것이 내려갑니다 — 필드가 하나라
  그렇게만 되고, 수업 중 학생에게 시키는 일도 한 번에 하나입니다.
- 칸은 방마다 따로입니다 — `LessonTaskPanel`(공부방) ·
  `LessonBookTaskPanel`(책방). 저장되는 자리가 아예 달라(카드 한 장 ↔ 그
  활동의 내 기록) 한 껍데기에 담으면 어느 쪽 규칙을 따르는지 흐려집니다.
  책방 쪽은 곁텍스트와 RAFT를 **한 칸이 함께** 맡습니다 — 둘은 저장 함수까지
  같은 것을 써서, 갈리는 것이 '무엇을 그리나'뿐입니다.
- **서랍에는 학생이 쓸 수 있는 것이 다 있고, 보내기는 그중 하나로 눈을
  맞춥니다.** 한때 보낸 칸 하나만 그렸는데, 그러면 앞서 쓴 것을 수업 중에
  고칠 수 없고(책방으로 건너가야 합니다) RAFT는 문장이 채워지는 과정도
  안 보였습니다.
  - **RAFT는 네 칸이 늘 다 섭니다.** 원래 단계 잠금이 없는 활동이라 학생은
    언제나 넷 다 쓸 수 있고, 보낸 칸만 도드라지며 커서가 갑니다. 위에는
    `raftSentence`가 책방 화면과 **같은 자리·같은 모습**으로 서서, 칸을
    채울 때마다 밑줄이 하나씩 메워집니다. **글쓰기는 따로 보냅니다** —
    칸이 커서 네 칸과 함께 두면 서랍이 너무 길어집니다(그때는 문장 + 글칸).
    - **형식 칩은 형식 칸 바로 아래, 어느 칸이 나가 있든 늘 섭니다**(책방
      화면과 같은 자리). 네 칸을 다 지나 맨 밑에 두었을 때는 무엇을 고르는
      칩인지 알 수 없었고, 형식을 보냈을 때만 그렸을 때는 형식을 정한 뒤
      선생님이 주제를 보내면 칩이 사라져 다른 형식으로 바꿀 길이 없었습니다.
      고른 뒤에도 그대로 두어 눌러서 바꿉니다.
  - **곁텍스트는 연 단계만 보입니다.** 안 연 단계는 학생이 쓰면 안 되는
    자리라 서랍에 깔 수 없습니다. 보낸 단계가 펼쳐지고, 앞서 열어 둔 단계는
    한 줄로 접혀(`.ltask-fold`) 위에 서다가 누르면 그 자리에서 펴집니다.
  - **칸마다 이름 줄이 섭니다.** 펼친 칸에 이름이 없으면 바로 위 접힌 줄의
    것처럼 보입니다(실제로 그랬습니다). 그 줄이 곧 **펴고 접는 자리**입니다 —
    편 것을 다시 못 접으면 하나씩 펴 볼수록 서랍이 길어지기만 합니다. 여닫는
    자리가 늘 같은 곳이라야 손이 기억합니다(수업 노트 손잡이와 같은 생각).
    보낸 칸의 줄만 누를 수 없고(늘 펼쳐집니다) 방송 점이 붙습니다(`.now`).
    그래서 **머리말에는 칸 이름을 안 적습니다** — 그 줄이 안 서는 때(잠김·모둠)
    에만 적습니다. 둘 다 적으면 같은 말이 두 줄입니다.
  - **공부방도 열린 활동이 다 섭니다 — 곁텍스트와 같은 모양입니다.** 보낸
    활동이 펼쳐지고 앞서 열어 둔 활동은 접힌 한 줄(번호 · 이름 · 한 줄
    미리보기)로 위에 섭니다. 잠긴 활동은 접어서도 두지 않습니다.
    - 그래서 **저장할 곳을 자리 하나가 아니라 맵으로** 붙듭니다
      (`pendingRef`의 `drafts`). 칸이 여럿 펼쳐질 수 있어 한 자리만 들고
      있으면, 접힌 것을 펴서 고친 글이 다음 저장에 덮입니다. 카드는 한
      문서라 어차피 통째로 다시 짜 쓰므로 한 번에 모아 씁니다.
- **곁텍스트는 한 단계를 통째로 보냅니다.** 제목 단계는 다섯 칸, 목차는 셋,
  머리말은 둘인데 그 칸들이 한꺼번에 섭니다 — 칸 하나씩 보내면 선생님이 같은
  단계를 다섯 번 보내야 합니다.
- **보낸 칸이 바뀌면 껍데기 문서를 다시 읽습니다**(1건) — 책방은 활동,
  공부방은 프로젝트. 교사는 보내면서 그 칸을 여는데, 같은 활동·같은
  프로젝트의 다음 칸을 보내면 문서 id가 그대로라 처음 읽기 효과가 다시 돌지
  않습니다. 그러면 학생 화면이 방금 연 칸을 아직 잠긴 것으로 알고 '선생님이
  이 활동을 열어 주면 쓸 수 있어요'에서 멈춥니다(**두 방에서 다 났던
  일입니다** — 한쪽만 고치지 마세요). **답·카드는 다시 안 읽습니다** — 지금
  화면에 든 것이 더 새것이고, 덮어쓰면 방금 친 글자가 날아갑니다.
- **자리를 맞추는 고민이 책방에는 없습니다.** 답이 칸마다 제 이름표를 달고
  저장되고 단계·요소가 `lib/paratext.js`·`lib/raft.js`에 못 박혀 있어,
  공부방 카드처럼 '몇 번째 자리인가'로 짚지 않습니다.
- **닿소리 채우기는 안 보냅니다.** 판이 열네 칸 격자라 서랍 폭에 안 들어가고,
  빠른 입력 칸만 떼어 오면 모둠 판이 채워지는 모습을 학생이 못 봅니다 —
  그게 그 활동의 절반입니다. KWLS는 아직인데, 붙인다면 곁텍스트와 같은
  모양이라 `LessonBookTaskPanel`에 갈래 하나를 더하면 됩니다.
- **'무엇을 보낼 수 있나'는 `lib/bookPush.js` 한 곳**입니다
  (`isPushableBookActivity` · `bookPushSteps`). 교사 고르개와 학생 서랍이
  같은 목록을 봐야 합니다 — 교사가 보낼 수 있는데 서랍이 못 그리는 칸이
  하나라도 있으면 학생 화면이 빈 채로 뜨고 까닭도 안 적힙니다.
- 교사 화면의 독서 활동 목록은 **한 번만 읽습니다**(`fetchBookActivities`).
  구독으로 두면 수업 모드가 떠 있는 내내 연결이 살아 있는데, 고르개를 한 번
  펴 보는 일에 그만한 값이 없습니다. **잠긴 활동과 지운 활동은 목록에서
  뺍니다** — 보내 봐야 규칙이 학생 저장을 거부합니다.
- 독서 활동도 **칩**입니다 — 활동 칩 한 줄, 고르면 그 아래로 단계 칩 한 줄.
  한 묶음에 곁텍스트와 RAFT가 섞여 서므로 활동 이름 앞에 종류를 적습니다
  (`bookKindLabel`). 아직 아무 활동도 안 골랐으면 **지금 내보내는 중인 활동**의
  단계를 폅니다 — 수업 모드를 다시 열어도 이어서 다음 단계를 누를 수 있게.
- **단계 잠금을 여는 것은 곁텍스트에만.** RAFT는 네 칸 어느 것도 잠기지 않아
  열 것이 없고, 잠금 맵을 써 넣으면 없던 개념이 그 문서에 생깁니다.

**학생 화면의 말은 '오늘의 활동 1 · 2'입니다.** 한때 '선생님이 내보낸 활동'
이었는데, '내보냈다'는 교사 쪽에서 하는 일을 가리키는 말이라 받는 학생에게는
무슨 일이 일어난 것인지 흐렸습니다. 탭 이름 · 머리말 · 못 찾았을 때의 안내가
모두 이 말입니다(교사 화면은 그대로 '내보내기' — 그쪽에서는 그것이 하는 일
그대로입니다). 머리말의 차례(`오늘의 활동 2`)가 이미 몇 번째인지 말하므로,
활동 이름이 기본값('활동 2')뿐이면 이름 줄을 따로 안 그립니다.

- **반 문서의 `task` 한 필드**입니다(`{ boardId, actIndex, at }`).
  `pyTarget`·`marquee`와 같은 길이라 **규칙을 한 줄도 안 건드립니다**.
  - **방송 문서에 두지 마세요** — `LessonMode`가 일시정지에도 그 문서를
    지웁니다. 잠깐 멈출 때마다 학생 화면의 활동이 사라집니다.
  - `at`(내보낸 시각)이 있어야 합니다. 학생이 서랍을 닫아 둔 뒤에도 **새로
    내보낸 것**은 다시 열려야 하는데, 같은 것을 두 번 내보내면 가리키는 곳이
    그대로라 시각 없이는 알아채지 못합니다.
  - **오늘 보낸 것만 삽니다**(`classTaskOf`). 내보낸 것은 교사가 '그만
    보내기'를 눌러야 내려가는데, 수업을 마치고 발표만 끄면 반 문서에 그대로
    남습니다. 그러면 다음 날 학생이 접속할 때 어제 것이 다시 서고 서랍이
    저절로 열립니다 — 화면에 '오늘의 활동'이라 적힌 채로요. 이어서 할
    활동이면 교사가 그날 다시 보내면 되고, 학생이 어제 쓴 글은 공부방·책방에
    그대로 있습니다.
    - 판정은 **`classTaskOf` 한 곳**입니다. 학생 화면에서만 걸러 내면, 다음
      날 교사 줄에는 '내보내는 중'이라 적혀 있는데 학생 화면에는 아무것도
      없습니다. 교사(`LessonMode`)와 학생(`TopNav`)이 둘 다 이 함수를 거칩니다.
    - `at`이 없는 옛 값도 여기서 함께 떨어집니다(오늘일 리가 없으므로).
- **읽는 문서가 늘지 않습니다** — 상단바가 전광판 때문에 이미 반 문서를
  구독하고 있어(`subscribeClass`) 그 값을 서랍에 내려 줍니다. 서랍은 활동이
  바뀔 때만 프로젝트 1건과 내 카드 1건을 읽습니다.
- **모달로 덮지 않습니다.** 수업 중에는 뒤에 선생님 화면이 있어서, 덮으면
  학생이 쓰는 동안 물음이 안 보이고 슬라이드가 넘어가도 모릅니다. 서랍은
  발표 화면을 그만큼 좁힐 뿐이라(`.broadcast-overlay--noted`) 둘이 나란히
  섭니다. **평소에는 탭 줄이 아예 없습니다** — 내보낸 활동이 있을 때만.
- **쓰는 칸이 곧 그 탭입니다 — 큰 창을 열지 않습니다.** 처음에는 '작성하기'로
  큰 창을 열게 했는데(공부방 카드의 활동 칸과 같은 방식), 한 번에 활동 하나만
  내려오므로 서랍 한 칸이 답 하나에 넉넉하고, 그 창이 발표 화면을 덮어 쓰는
  동안 선생님 화면이 안 보였습니다. 지금은 탭을 열면 바로 쓰는 칸이라 누르는
  단계가 없고 발표 화면이 계속 보입니다. 서식은 수업 노트 필기 칸과 **같은
  다섯**입니다(굵게·밑줄·글머리 기호·번호 목록·**코드 블록**) — 한 서랍
  안에서 탭만 바꿔 오가는 자리라 툴바가 다르면 다른 도구로 보입니다.
  - 코드 블록이 여기 있는 까닭: 이 칸이 저장되는 곳은 결국 공부방의 내
    카드이고, 그 카드를 크게 쓰는 창은 처음부터 툴바 전체를 써 코드 블록이
    있었습니다. **서랍에만 빠져 있어**, 수업 중에 서랍으로 답한 코드는 밋밋한
    글이 되고 나중에 공부방에서 열어 고쳐야 검은 블록이 되었습니다. 정화기가
    `PRE`를 허용하고(`lib/html.js`) `parseActivitySections`도 그대로
    통과시키므로 **저장 규칙도 자료 모양도 안 건드립니다**. 서랍이 좁아 코드가
    가로로 넘치는 것은 공용 코드 블록 규칙의 `overflow-x: auto`가 맡습니다.
  - **책방 쪽(`LessonBookTaskPanel`)은 그냥 `<textarea>`라 서식이 없습니다.**
    거기에 코드 블록을 넣는 일은 단추 하나를 더하는 것이 아니라 곁텍스트·
    RAFT의 저장 형식을 평문에서 HTML로 바꾸는 일입니다(학생 화면·교사 화면·
    전광판의 글자 수 셈까지 함께). 손대려면 따로 잡으세요.

**코드 블록 안에서는 들여쓰기가 문법입니다**(`RichTextEditor`). 이 칸은
CodeMirror가 아니라 그냥 글자 칸(contentEditable)이라, 손대지 않으면 **Tab이
칸 밖으로 포커스를 옮기고 Enter가 앞 줄 들여쓰기를 안 물려받습니다** — 파이썬을
쓰려면 줄마다 스페이스를 손으로 넣어야 했습니다.

- **코드 블록 안에서만** 바꿉니다 — 바깥에서 Tab을 가로채면 키보드만 쓰는
  사람의 이동 경로가 막힙니다. Tab = 공백 4칸, Shift+Tab = 그 줄 앞 공백을
  최대 네 칸, Enter = 앞 줄 들여쓰기 물려받기(`:`로 끝나면 한 단 더).
- **`execCommand('insertText')`로 줄바꿈을 넣지 마세요.** 크롬은 `<pre>` 안에서
  `\n`을 만나면 글자를 넣는 대신 **블록을 둘로 쪼갭니다** — Enter를 칠 때마다
  코드 블록이 하나씩 늘어납니다(실측). 줄바꿈만 Range로 직접 넣고, Tab의 공백
  넣기는 쪼개지 않으므로 execCommand 그대로입니다.
- **꼬리 줄바꿈 하나.** `<pre>` 맨 끝에 `\n` 하나만 넣으면 그 줄이 안 그려집니다
  (HTML이 마지막 줄바꿈을 흘려 버립니다) — 높이도 그대로고 커서도 안 보여
  Enter가 안 먹은 것처럼 보입니다(실측 39→39px). 끝에서 칠 때만 `\n`을 하나 더
  붙이고 커서를 그 앞에 둡니다(39→54px). 꼬리는 한 번만 생기고 쌓이지
  않습니다 — 그 뒤로는 커서 뒤에 글자가 있어 '맨 끝'이 아니기 때문입니다.
- **빈 코드 블록의 공백 한 칸 — 커서는 그 앞에.** 빈 `<code>`에는 커서를 둘 수
  없어 공백 하나로 시작하는데, `insertHTML`은 커서를 넣은 것 **뒤**에 둡니다.
  그래서 학생이 치는 첫 글자가 그 공백 뒤에 붙어 **코드 첫 줄이 한 칸
  들여쓰기된 채** 시작했습니다 — 파이썬에서는 그것만으로 IndentationError입니다
  (서랍에서 코드를 돌릴 수 있게 되면서 드러났습니다. 그 전에는 아무도 안 돌려
  티가 안 났습니다). 커서를 공백 앞에 두면 그 칸은 첫 줄 **끝**에 남고, 줄 끝
  공백은 파이썬이 흘려보냅니다.

### 파이썬 연계 프로젝트의 활동 칸은 셀입니다 (코랩처럼)

- **`board.pyLinked`인 프로젝트**(모둠 아님)에서는 서랍 활동 칸이 서식 에디터
  한 칸이 아니라 **셀 편집기**(`components/PyCellEditor.jsx`)입니다.
  '프로젝트 활동'도 수업 중 '오늘의 활동'도 같습니다 — 판정이 보내는 길이 아니라
  **프로젝트**(`LessonTaskPanel`의 `cellMode`)라서, 같은 카드의 같은 칸이 두
  모양이 되지 않습니다.
- **파이썬을 돌리는 곳은 이 셀뿐입니다.** 연계하지 않은 프로젝트의 서랍 칸은
  서식 에디터 그대로라 코드 블록은 적을 수 있어도 **▶ 실행 줄이 없습니다.**
  예전에는 그 칸에도 실행 줄이 있어, 돌릴 블록을 커서로 추측하고(커서가 든
  블록 · 마지막 블록 · 결과 블록 건너뛰기 `isResultPre`) '결과 붙이기'가 결과를
  덧붙이기만 해 같은 결과가 두 번 붙곤 했습니다. 셀이 그 일을 구조로 맡으면서
  걷었습니다(`pickCodeEl` · `pyResultHtml` · 칸마다 입력값 · RichTextEditor의
  `codeAtEnd`도 함께). 코드를 돌리게 하려면 프로젝트 편집에서 '파이썬 실행기'와
  연계하면 됩니다.
- **카드는 보기 · 첨부만**입니다(`StudyMyActivityCard`의 `drawerOnly` —
  서랍 단추 `onOpenPython`이 오는 카드 = 연계 · 내 카드 · 모둠 아님 · 학생).
  - 칸의 '✎ 크게 쓰기'가 **'⤢ 크게 보기'**가 되어 `ActivityViewModal`(읽기
    전용 · 머리의 '파이썬 실행기')을 엽니다. 활동 이름도 입력칸이 아니라
    글자입니다 — 이름은 글과 한 덩이(`buildActivityHtml`)로 저장돼, 여기서
    고치려면 글까지 함께 써야 합니다.
  - 자동 저장은 **`{ imageUrl, attachments }`만** 보냅니다(카드가 아직
    없을 때만 글까지 넣어 만듭니다 — `addStudyCard`와 규칙이 content를
    요구하고, 그때는 덮을 글도 없습니다). 카드가 들고 있던 글(구독으로 늦게
    들어왔을 수 있는)을 함께 쓰면 방금 서랍에서 쓴 것을 되돌리기 때문입니다.
    카드가 있으면 첨부를 다 지운 것도 저장합니다(`valid`).
  - 크게 보기 창이 떠 있어도 **밖의 글을 들입니다** — 보기만 하는 창이라
    서랍에서 쓴 것을 따라가야 합니다(편집 창은 그대로 비켜 줍니다).
  - 교사 안내 카드·남의 카드·연계하지 않은 프로젝트는 지금까지 그대로입니다.
  - **읽는 자리는 셀 모양으로 맞춰 그립니다**(`lib/pyCells.js`의
    `tidyCellsHtml` = 풀었다 다시 적기, `StudyMyActivityCard`의 `shownHtml` —
    칸 미리보기 · 크게 보기 · 교사가 보는 읽기 전용 칸). 옛 '결과 붙이기'가
    결과를 두 번 붙인 칸이나 끝에 남은 빈 코드 블록이 서랍과 같은 모양(코드
    하나에 결과 하나)으로 보입니다. **저장된 글은 안 건드립니다** — 학생이
    서랍에서 고칠 때 그 모양으로 저장됩니다. 연계 프로젝트의 카드에만
    씁니다(`board.pyLinked`). **모아보기 · 활동보기 · 발표 모드도 같은 셈**을
    거칩니다 — 모아보기는 `StudyProjectView`의 `wallRows`에서(그 값이 학급
    화면 띄우기에도 그대로 실립니다), 활동보기는 `LessonAnswerPanel`의
    `pyLinked` prop으로, 발표 모드는 활동 칸마다(화면)와 방송 꾸러미
    (`castContent` — **칸을 먼저 나눈 뒤** 셀로 풀고 다시 묶습니다. 카드
    전체를 통째로 풀면 활동 구역 표식이 사라집니다). 새 읽는 자리를 만들면
    여기에도 함께 거세요 — 안 그러면 그 화면에서만 결과가 두 번 섭니다.
- 읽는 자리의 모양(`globals.css`의 `pre.py-result`): 코드는 검은 블록, 결과는
  **옅은 바탕 + 초록 왼쪽 선**, '실행 결과' 이름은 **코드 셀 바로 뒤일 때만**
  CSS가 그립니다(옛 '결과 붙이기'는 `<p>실행 결과</p>` 글줄을 넣어 두어,
  거기까지 달면 이름이 두 번 섭니다). 대상은 `.study-card-content` ·
  `.study-card-body` · `.rte-area`.
  - **글 셀은 `div.py-text` 블록입니다** — 흰 바탕 + 옅은 테두리 + **살구빛
    왼쪽 선**(결과의 초록 선과 색으로 갈림), 모서리·여백은 코드·결과와 같은
    값. 코드와 결과 사이에서 글만 맨바닥에 흘러 어디까지가 한 셀인지 안
    보였습니다. 대상은 `.study-card-content` · `.study-card-body`.
    - **감싸는 것은 읽는 자리뿐입니다** — `tidyCellsHtml`이
      `serializeCells(cells, { wrapText: true })`로 적습니다. **저장 모양은
      그대로**(감싸지 않음)입니다: 감싼 모양이 저장되면 셀로 되읽을 때 그 겹을
      벗겨야 하는데, 글 셀 안에도 `<div>`가 들 수 있어 짝을 가리기 어렵습니다.
    - 정화기(`lib/html.js`)가 `DIV`의 `py-text`를 남깁니다 — 모아보기·발표
      방송처럼 한 번 더 정화되는 자리에서도 블록이 남아야 합니다.
  - **카드 미리보기의 본문은 칸 안에서 구릅니다**(`.study-mycard-preview-body`의
    `overflow-y: auto` — 칸은 세로 flex, 높이 260px 그대로). 예전에는 넘치는
    글을 아래쪽 흐림으로 잘라 두고 '크게 보기'를 눌러야 읽었는데, 서랍을 연
    채로 옆에서 쓴 것을 확인하는 자리라 창을 열고 닫는 일이 잦았습니다.
    - 구르는 것은 본문뿐이라 위의 '파이썬 실행기' 단추와 아래 '크게 보기'는
      제자리입니다. 끝에 닿아도 페이지가 따라 구르지 않습니다
      (`overscroll-behavior: contain`). 흐림(`::after`)은 걷었습니다 — 끝까지
      내려도 마지막 줄을 덮었습니다.
    - 단추는 흘리지(float) 않고 **제 줄의 오른쪽 끝**에 섭니다(flex 안에서는
      float가 안 듣습니다). 그래서 예전의 `pre { clear: both }`(코드 블록이
      흘린 단추 옆에 끼어 잘리던 것을 막던 줄)도 걷었습니다.
    - 실측(데모, 1920·1160px): 칸 260px · 본문 191px에 글 605~700px →
      휠로 끝까지 내려가고 페이지 scrollY 0 그대로 · 휠만으로 창이 안 열림 ·
      가로 넘침 0.
- **`.py-code`라는 CSS 클래스를 다른 데 쓰지 마세요.** 카드에 저장되는 코드 셀
  (`pre.py-code`)의 이름입니다. 실행기의 입력값 칸이 같은 이름(min-height
  200px · 밝은 바탕)을 쓰고 있어 카드의 코드 셀이 전부 커다란 흰 상자가 될
  뻔했습니다 — 그쪽을 `.py-mono`로 바꿨습니다.
- 코드 셀의 들여쓰기는 **네 칸**(`indentUnit`, `@codemirror/language`를 직접
  의존성으로 올렸습니다). CodeMirror 기본은 두 칸이고, 이 앱의 코드 블록은
  Tab = 공백 4칸입니다. (실행기는 아직 기본값 그대로입니다.)
- 학생이 셀 종류를 **직접 고릅니다** — 아래 `＋ 글`(서식: 굵게·밑줄·글머리·번호,
  **코드 블록 없음**) · `＋ 코드`(CodeMirror). 넣는 자리는 **고른 셀 바로 아래**
  (왼쪽 띠가 고른 셀), 없으면 맨 끝. 셀 머리의 ↑ ↓ ✕로 옮기고 지웁니다 —
  글이 든 셀을 지우면 6초 동안 '되돌리기'가 섭니다(되묻는 창 대신).
- **▶는 그 셀만** 새 이름 공간에서 돌립니다(앞 셀의 변수는 없습니다 —
  선생님이 고른 규칙). 끝나면 출력이 **그 셀의 결과**가 되어 곧바로 저장되고
  (`onChange(html, { flush: true })`), 코드를 한 글자라도 고치면 결과를
  걷습니다. 돌리는 사이에 고쳤으면 결과를 안 붙입니다. '결과 붙이기' 단추는
  없습니다 — 두 번 붙는 일이 구조적으로 없습니다.
- 입력값 칸은 셀마다(`input(`이 보일 때만), **저장하지 않습니다**. input()이
  찍은 값이 결과에 남아 무엇을 넣었는지는 보입니다.
- 오류는 **Pyodide 자신의 줄을 걷고** 넘깁니다(`lib/pyRun.js`의
  `tidyTraceback` — 실행기도 같음). 결과가 카드에 저장되는 글이라
  `/lib/python312.zip/_pyodide/…` 두 벌이 늘 붙어 있으면 안 됩니다.
- **적은 모양이 달라질 때만 올립니다**(`lastHtmlRef`). 빈 셀을 더하거나(카드의
  '파이썬 실행기' 단추) 옛 카드를 열기만 해서는 쓰기가 안 나갑니다 — 옛 카드는
  고칠 때 비로소 새 모양(`pre.py-code` + `pre.py-result`)이 됩니다. 실측:
  옛 방식으로 쓴 카드(글 + 코드 블록)를 서랍으로 열면 글 셀 · 코드 셀 · (단추가
  넣은) 빈 코드 셀로 풀리고 저장 상태·카드 모두 그대로.
- **`aliveRef`는 효과 안에서 켭니다.** 개발 모드(StrictMode)는 마운트 직후
  정리를 한 번 돌려, 끄기만 두면 처음부터 아무것도 안 올립니다(실제로 그랬습니다).
- 카드의 '파이썬 실행기' 단추(`codeAtEnd`)는 맨 끝이 빈 코드 셀이면 그 셀에,
  아니면 새 코드 셀을 붙여 커서를 둡니다. 빈 글 셀 하나뿐이면 그것을 코드 셀로
  바꿉니다(빈 두 칸이 나란히 서지 않게). 마운트 때 받은 번호는 건너뜁니다.
- 실측(실제 Pyodide): 셀1 `x = 41; print(x+1)` → 42 · 셀3 `print(x)` →
  NameError(독립 실행) · input 셀 `이름: 홍길동 / 홍길동님` · 코드를 고치면 결과
  걷힘 · 지우기→되돌리기 · 옮기기 · 서랍을 닫았다 열면 셀·결과가 그대로.

### 파이썬 실행 엔진 · 출력 (실행기 · 코드 셀 공통)

- 학생이 서랍에서 파이썬을 돌리는 곳은 **연계 프로젝트의 코드 셀**
  (`PyCellEditor`, 위 절)이고, 실행기와 **엔진 한 벌**을 나눠 씁니다. 이 절은
  둘이 함께 따르는 약속입니다.
- **엔진은 `lib/pyRun.js` 한 곳**입니다. 워커를 만드는 코드가 실행기
  (`PythonRunner`) 안에 있던 것을 옮겼습니다 — 그대로 복사해 서랍에도 두면
  **Pyodide가 두 벌 뜹니다**(하나가 100MB 남짓). 두 화면은 나란히 열릴 수
  있으므로 한 벌을 나눠 쓰고, 동시에 두 곳에서 돌리지는 않습니다(`"busy"`).
  - **화면을 여는 것만으로 워커를 만들지 마세요.** Pyodide는 CDN에서 10MB
    남짓을 받습니다(처음 한 번, 그 뒤로는 브라우저 캐시). 서랍을 열자마자
    받게 하면 **코딩이 아닌 수업에서도 반 전체가** 그만큼 받습니다. 워커는
    `runPython`을 처음 부를 때 생기고, 그때 `"fresh"`를 돌려주어 부르는 쪽이
    '불러오는 중' 안내를 답니다.
  - 실행기를 닫아도 **워커를 끄지 않습니다** — 서랍과 한 벌을 나눠 쓰므로,
    닫았다고 남의 실행까지 끊으면 안 됩니다. 꺼지는 때는 중단·시간 초과뿐.
  - **실행은 늘 새 이름 공간에서**입니다(`runPythonAsync(code, { globals: 새 dict })`).
    예전에는 Pyodide의 공용 전역에서 돌려 **앞 실행의 변수가 다음 실행에
    남았습니다**(실측: `x = 41`을 돌린 뒤 `print(x + 1)`만 돌려도 42). 같은
    코드가 두 번째에만 돌고, 붙인 결과가 그 코드만으로 나온 것인지 알 수
    없었습니다. 실행기·서랍·파이썬 연계 셀이 모두 이 규칙입니다.
    - `__name__`을 `"__main__"`으로 넣어 둡니다 — 빈 dict면
      `if __name__ == "__main__":`이 NameError로 멈춥니다.
    - 불러온 모듈은 파이썬이 따로 기억해(`sys.modules`) 다시 받지 않습니다.
      새로 만드는 것은 변수가 사는 공간뿐입니다.
- **돌릴 때는 공통 들여쓰기를 걷습니다**(`lib/pyCells.js`의 `dedent` — 파이썬
  textwrap.dedent와 같은 셈). 옛 코드 블록의 '공백 한 칸'(아래 '빈 코드 블록의
  공백 한 칸')이 **이미 저장된 카드에는 그대로** 남아 있어, 안 걷으면
  IndentationError입니다. 옛 카드를 셀로 풀 때도 같은 셈을 씁니다.
- **실행 단축키는 Ctrl(⌘)+Enter** — 실행기와 코드 셀이 같습니다(둘 다
  CodeMirror의 keymap).
- **오류 글은 Pyodide 자신의 줄을 걷습니다**(`tidyTraceback`). 결과가 카드에
  저장되는 글이라 `/lib/python312.zip/_pyodide/…` 두 벌이 늘 붙어 있으면 안
  됩니다.
- **입력값 칸은 코드에 `input(`이 보이는 즉시 뜹니다**(`codeUsesInput` — 코드
  셀마다). 늘 세워 두지 않는 까닭은 서랍이 380px이라 그만큼 쓰는 자리가
  줄어들기 때문입니다.
  - 한때는 **▶ 실행을 한 번 눌러야** 떴습니다. 그 첫 누름은 아무것도 돌리지
    않고 '아래 칸에 적고 다시 누르세요'만 말하는 헛걸음이었고, 파이썬이
    **값을 미리 적어 두는 방식**이라는 것을 누르기 전에는 알 길이 없었습니다
    (브라우저 안에서 한 번에 도는 것이라 실행 도중에 키보드를 기다릴 수
    없습니다). 지금은 **누르면 늘 돌아갑니다** — 칸을 비워 둔 채 돌리면
    파이썬이 EOFError로 알려 주고, 그때 그 칸은 이미 화면에 있습니다.
- **`input()`이 받은 값은 출력에 한 번 찍히고, 옅게 그립니다**(`.py-echo` —
  옅은 색 + 점선 밑줄). `print(input())`을 돌리면 같은 값이 두 줄 서는데,
  하나는 입력이고 하나는 출력입니다(터미널에서 친 글자가 화면에 남는 것과
  같습니다). 두 번 찍힌다는 신고가 있었습니다.
  - **찍기를 빼지 마세요.** `input("이름: ")`의 안내 뒤에 줄바꿈이 없어 다음
    출력과 한 줄로 뭉개지고(`이름: 홍길동님 안녕하세요`), 붙여 둔 결과에서
    '무엇을 넣어 나온 것인지'가 사라집니다.
  - 워커의 `input()`이 값 앞뒤에 표시 글자(`chr(1)`·`chr(2)`)를 둘러 찍고,
    `lib/pyRun.js`의 `splitEcho`가 그 줄을 `{ text, parts }`로 나눕니다 —
    `text`는 표시를 걷은 그 줄 그대로, `parts`는 표시가 있을 때만. 그리는 것은
    `components/PyLineText.jsx` **한 곳**이고 실행기와 서랍이 함께 씁니다.
    이벤트를 따로 보내지 않고 한 줄 안에 표시하는 까닭: 안내 문구가 있으면
    **같은 줄**에 입력값이 붙어야 하는데(`이름: ` + `홍길동`), 따로 보내면
    차례와 줄이 갈립니다.
  - 셀의 결과에는 **입력한 값도 그대로 들어갑니다**(카드에는 색을 담을
    수 없으므로 터미널 모양 그대로).
- **실행 결과의 줄은 줄바꿈으로 잇습니다**(`outputTextOf`의 `join("\n")`).
  Pyodide는 출력을 한 줄씩, 끝의 줄바꿈을 **뗀 채** 넘깁니다(0.26.4 실측).
  화면은 줄마다 `display: block`이라 멀쩡해 보여도, 빈 글자로 이었을 때는
  저장한 결과가 `skan테스트테스트` 한 줄로 뭉개졌습니다.
- **Pyodide를 이 컨테이너에서 돌려 보려면** CDN이 막혀 있으니 `npm i
  pyodide@0.26.4`를 스크래치 폴더에 받고, Playwright에서
  `context.route("https://cdn.jsdelivr.net/pyodide/**")`로 그 파일을 내어
  줍니다(워커가 부르는 것도 잡힙니다). Node에서는 `loadPyodide()`로 곧바로.
- 출력 칸(`.ltask-out`)은 **`max-height`로 제 안에서 구릅니다** — 안 그러면
  for문 한 번에 서랍이 한없이 길어져 쓰던 칸이 화면 밖으로 밀립니다.
- **수업 노트 필기 칸에는 ▶가 없습니다.** 노트는 들은 것을 받아 적는
  자리고, 짜서 돌려 보는 일은 연계 프로젝트의 코드 셀에서 합니다(코드 블록
  자체는 있습니다 — 노트에 적어 둔 코드를 돌리려면 실행기로 가면 됩니다).
- **실행기를 통째로 서랍에 넣지 마세요.** 줄 번호·문법 강조·자동 완성·크기
  조절까지 든 CodeMirror 패널은 380px에 안 들어갑니다. 서랍에서 하는 일은
  '짜고 돌려 보고 남기는 것'이고, 길게 짜 보는 일은 실행기(1단 연습장)에서
  하고 **복사해 서랍의 코드 셀에 붙입니다**. 실행기에서 카드로 곧바로 '보내던'
  길은 없앴습니다 — 보내기에서 탈이 줄줄이 났고, 쓰는 자리가 서랍 하나면 보낼
  일이 없습니다.
- **두 칸을 함께 그려 두고 감춥니다**(`hidden` — KWLS 노트 탭과 같은 방식).
  탭을 오갈 때마다 지웠다 만들면 그때마다 프로젝트와 카드를 다시 읽고, 쓰던
  칸이 새로 마운트되어 커서가 튑니다. `.cornell-pane`은 `display: flex`라
  `[hidden]`만으로는 안 숨으므로 CSS에 한 줄이 함께 있어야 합니다.

**수업 밖에서 — 프로젝트 활동**(`lib/projectTask.js` · `CornellNoteDrawer`의
`projTask`). 파이썬 실행기와 연계된 프로젝트에서 학생이 활동 칸의 '파이썬
실행기' 단추를 누르면, **실행기가 아니라 이 서랍**이 열리고 그 활동 칸이
섭니다. 수업 중 '오늘의 활동'과 **같은 칸**(`LessonTaskPanel` — 연계 프로젝트라
셀 편집기)이고, 쓴 것은 그 프로젝트의 내 카드에 곧바로 저장됩니다. 학생이 익힐 것이 '코드를 쓰는 곳은 오른쪽 서랍' 하나가 됩니다.

- **서랍이 저절로 열리는 것은 단추를 눌렀을 때뿐입니다.** 연계된 프로젝트에
  들어가기만 해도 서랍이 열리면 코드를 안 쓰는 날에도 매번 닫아야 합니다
  (선생님이 고름).
- **다만 탭은 그 프로젝트에 머무는 동안 늘 섭니다** — 손잡이로 서랍을 직접
  열어도 `수업 노트 · 프로젝트 활동` 탭 줄이 있습니다. 단추를 누른 순간만
  알려 주던 때는 손잡이로 열면 탭 자체가 없었습니다(실제 신고).
  - 공부방 페이지가 **머무는 프로젝트**를 `setProjectContext`로 적어 두고,
    서랍이 `subscribeProjectContext`로 받습니다(`lib/projectTask.js`). 이벤트가
    아니라 **값을 들고 있는 작은 저장소**입니다 — 페이지와 상단바가 같은
    렌더에서 마운트되어, 알리는 순간 서랍이 아직 귀를 안 열었을 수 있습니다.
  - 적는 조건: 학생 · `pyLinked` · 모둠 아님. 페이지가 넘기는 활동은 **처음으로
    열린 활동**이고(잠긴 활동을 짚으면 칸이 '선생님이 열어 주면…'에서 멈춥니다),
    실제로 펼칠 칸은 아래 '마지막으로 손댄 활동'이 정합니다.
    프로젝트를 나가면(목록 · 다른 화면) 비웁니다 — 실측으로 탭 줄이 사라지고,
    다시 들어가면 되살아납니다.
  - 손잡이로 열면 **노트 칸만** 켜진 채이고 탭을 눌러 폅니다. **코드 블록도
    안 넣습니다**(`task.focus` — 단추로 연 것에만). 탭을 편 뜻이 코드를 짜겠다는
    것인지, 앞서 쓴 글을 보러 온 것인지 모르기 때문입니다.
  - 차례는 **단추로 연 것 → 선생님이 보낸 오늘의 활동 → 머무는 프로젝트**
    (`projTask ?? classTask ?? ctxTask`). 오늘 선생님이 보낸 활동이 있으면 그
    탭이 '오늘의 활동'으로 섭니다.
  - 닫힌 손잡이의 툴팁 '오늘의 활동이 있어요'는 선생님이 보낸 것에만 답니다
    (`!task.local`) — 머무는 프로젝트만으로 그 말을 띄우면 틀립니다.
- 탭·머리말의 이름은 **'프로젝트 활동'**입니다(`task.local`). '오늘의 활동'은
  선생님이 보냈다는 말이라 스스로 연 칸에 붙이면 틀립니다. 방송 점도 안 답니다.
- 열 때는 **활동 칸만** 켜고(오늘의 활동이 올 때와 같은 규칙) **누른 활동의
  맨 끝 빈 코드 셀에 커서를** 둡니다 — 누른 뜻이 '여기에 코드를 짜겠다'라서요
  (`PyCellEditor`의 `codeAtEnd`). 맨 끝이 이미 빈 코드 셀이면 그 셀에, 빈 글 셀
  하나뿐이면 그것을 코드 셀로 바꿔 씁니다. 빈 셀은 저장되지 않으므로 단추만
  눌러 보고 닫아도 카드에 아무것도 안 남습니다. 열린 활동이 다 서고 누른 것만
  펼쳐집니다(수업 중과 같은 모습). 수업 중에는 안 합니다 — 선생님이 보낼 때마다
  스물몇 명의 손이 끊기고, 보낸 활동이 코드 활동이 아닐 수도 있습니다.
  - 한때 이 칸이 서식 에디터라 빈 코드 블록을 손으로 끼워 넣었는데(글 줄 끝에
    넣으면 크롬이 `<span>`으로 바꿔 넣음 · 감싼 `<div><pre>`를 못 알아봐 누를
    때마다 블록이 늚 · 조용히 넣기), 셀이 되면서 그 우회가 모두 없어졌습니다.
  - **마운트 때 받은 번호는 건너뜁니다.** 밖에서 그 칸이 고쳐지면 편집기가
    새로 마운트되는데(`ver`), 그때 같은 번호로 다시 돌면 빈 셀이 또 생깁니다.
- **프로젝트 활동은 모든 칸을 펴고 접습니다.** 수업 중에는 선생님이 보낸 칸
  하나가 늘 펼쳐져 있고 못 접지만(`on` — 그 순간 모두가 보는 칸이라서),
  학생이 스스로 연 칸에 그럴 이유가 없습니다. 그래서 `on`은 `!local`일 때만
  서고, 프로젝트 활동의 칸 머리는 전부 접는 줄(단추)입니다. 늘 첫 활동을
  고정해 펼쳐 두던 때는 3번을 쓰다 서랍을 다시 열면 1번이 크게 서고 3번은
  접혀 있었습니다(선생님 지적).
- **처음 펼칠 칸은 마지막으로 손댄 활동입니다**(`LessonTaskPanel`의
  `remember`). 손댐은 셋 — 그 칸에 글을 씀 · 카드의 단추로 그 활동을 엶 ·
  접힌 줄을 눌러 폄. 단추로 연 것은 기억보다 **누른 활동**이 먼저입니다.
  - 기억은 **이 기기에만**(localStorage `ptask_last:<uid>:<boardId>`) 둡니다 —
    화면을 여는 편의라 서버에 적을 일이 아닙니다. 없거나 못 읽거나 그 활동이
    잠겼으면 처음 열린 활동을 펼칩니다.
  - `localStorage.getItem`이 `null`을 주면 **`Number(null)`이 0**이라 '활동 1을
    기억했다'로 읽힙니다 — 날 값이 `null`인지 먼저 봅니다.
  - 단추로 연 것을 적을 때 **`remember`를 먼저 부르고** 표시(`rememberedRef`)는
    그 안에서 세웁니다. 표시를 먼저 세우면 '이미 적었다'로 보고 건너뛰어,
    단추로 연 활동이 기억에 안 남았습니다(실측).
  - 실측: 처음엔 1번 · 3번을 펴 쓰고 1번을 접음 → 다시 열면 3번 · 카드에서
    2번 단추 → 2번(코드 블록) · 손잡이로 다시 → 2번.
- **선생님이 새로 보내면 그것이 이깁니다** — 새 `task.at`이 오면 `projTask`를
  비웁니다('새 활동이 오면 그 활동으로'와 같은 규칙).
- **서랍을 닫으면 단추로 연 것은 비웁니다**(다 미끄러져 나간 뒤에). 손잡이로
  다시 열면 노트 칸으로 돌아오고, 탭은 머무는 프로젝트로 남습니다. 선생님이
  보낸 활동은 닫아도 남습니다.
- 페이지와 서랍은 부모·자식이 아니라(서랍은 상단바가 그립니다) **창 이벤트
  하나**로 잇습니다(`openProjectTask` → `onOpenProjectTask`). 상단바는 다섯 화면이
  함께 쓰는 것이라 이 한 가지를 위해 모든 페이지에 prop 통로를 뚫지 않습니다.
- **같은 카드가 서랍 뒤에 열려 있습니다** — 그래서 활동 칸이 카드를 **구독**합니다
  (`subscribeStudyCard`, 문서 1건). 예전에는 한 번 읽고 카드 전체를 다시 짜
  썼는데(수업 중에는 뒤가 방송 화면이라 괜찮았습니다), 여기서는 카드 화면에서
  고친 다른 칸을 **옛 값으로 되돌렸습니다**(실측). 지금은 연계 프로젝트의 카드가
  글을 안 쓰지만(보기·첨부만 — 위 셀 절), 수업 중 '오늘의 활동'은 연계하지 않은
  프로젝트에서도 오고 그쪽 카드는 여전히 글을 쓰므로 이 구독이 필요합니다.
  - 쓰는 중인 칸(아직 저장 안 한 글이 있는 칸)은 서버 값이 와도 안 들입니다.
    나머지 칸은 에디터가 담은 글(`shownRef`)과 다를 때만 `ver`를 올려 다시
    그립니다.
  - 저장할 때 다른 칸은 **지금 구독으로 든 카드**에서 옮겨 담습니다(글자를
    칠 때 붙든 카드가 아니라).
  - 저장한 값은 **쓰기 전에** `shownRef`에 되읽은 모양으로 적어 둡니다 — 쓰는
    순간 구독이 곧바로 돌아오므로(지연 보정) 그 뒤에 적으면 제 글을 '밖에서 온
    값'으로 보고 커서가 튑니다.
  - 실측(연계하지 않은 프로젝트): 서랍에서 활동 1 저장 → 카드 화면이 들여옴 ·
    카드의 '크게 쓰기'로 활동 2 저장 → 서랍 칸 2가 곧바로 따라옴 · 이어 서랍에서
    활동 1 저장 → **활동 2가 그대로 남음**. 연계 프로젝트는 카드에서 파일을
    첨부해도 서랍에서 쓴 코드·결과가 그대로입니다(실측).
- **'크게 쓰기' · '크게 보기' 창은 서랍 위로 뜹니다**(`.study-act-backdrop`, z-index 3010).
  서랍(3001)이 창 오른쪽을 덮어 × 단추를 못 눌렀습니다(실측). 서랍의 Esc는
  페이지에 `.modal-backdrop`이 떠 있으면 비켜 줍니다 — 그 리스너는 캡처
  단계라 멈추면 창이 Esc를 못 받습니다(실측: Esc 한 번에 창만, 두 번에 서랍).
- **데모 모드에서는 이 서랍이 아예 안 뜹니다** — 상단바가 데모에서는 학생
  소속을 안 읽어(`!isFirebaseConfigured`) `activeClassId`가 비기 때문입니다
  (원래부터 그렇습니다). 시험할 때는 그 조건을 잠시 풀고 확인했습니다.
  데모 학생의 카드는 교사식 자동 ID로 생겨, mock의 `subscribeStudyCard`만
  작성자로도 찾습니다(실서비스는 규칙이 학생 카드 ID를 uid로 못 박습니다).
- **규칙을 안 건드립니다.** 학생이 자기 카드를 읽고 쓰는 길 그대로입니다.

**서랍은 날개를 폅니다 — 수업 노트와 오늘의 활동을 나란히**
(`.cornell-panes.wide`). 필기하면서 답하려는 자리라 둘을 함께 볼 수 있어야
하는데, 지금까지는 탭이 '하나 고르기'여서 한 칸을 보면 다른 칸이 사라졌습니다.

- **탭 줄만 켜기/끄기입니다**(`.cornell-tabrow` · `.cornell-tab`, `aria-pressed`).
  같은 모양을 쓰는 다른 두 곳(닿소리 '전체 보기' · 노트 크게 보기)은 '하나
  고르기'라 `.dash-view-tabs`를 **일부러 안 씁니다** — 같은 생김새가 화면마다
  다르게 움직이면 눌러 봐야 알게 됩니다. 켜짐은 체크 표시로 말하고, 체크 자리는
  꺼져 있을 때도 비워 둡니다(안 그러면 켜고 끌 때마다 글자가 7px씩 움직입니다).
- **마지막 한 칸은 못 끕니다** — 둘 다 꺼지면 빈 서랍이 남습니다.
- **활동이 내려온 순간에는 활동 칸만 켭니다.** 폭이 380px 그대로라 수업 중에
  스물몇 대의 화면이 동시에 벌어지지 않습니다. 필기가 필요한 학생이 노트 탭을
  더 눌러 폅니다(둘 다 켜 두었어도 **새 활동이 오면 활동만**으로 돌아옵니다).
- **폭은 `--cornell-w2` — 칸 하나가 380px 그대로 둘**입니다. 노트 칸은 그 폭에
  맞춰 만든 것이라(코넬 세 칸을 세로로 쌓는 까닭이 그것입니다) 날개를 펴도
  지금까지 쓰던 자리가 그대로입니다. 좁은 창에서는 `min()`으로 손잡이 자리
  (28px + 여백 = 96px)를 남깁니다 — 안 그러면 날개를 편 순간 닫을 길이 화면
  밖으로 나갑니다(실측 820px 창에서 서랍 724px · 손잡이 x=68).
- **노트가 왼쪽, 활동이 오른쪽입니다.** 서랍의 오른쪽 끝은 화면 가장자리에
  붙박이라 날개를 펴면 새로 생기는 자리는 **왼쪽**입니다. 활동이 오른쪽
  380px을 차지한 채 노트를 더 펴면 쓰던 활동 칸이 제자리에 그대로 남습니다.
  탭 줄의 차례도 이 왼→오와 같습니다.
- **760px 아래에서는 날개를 못 폅니다**(서랍이 아래에서 올라오는 시트라
  설 자리가 없습니다). 남기는 쪽은 **활동**입니다 — 수업 중에 지금 내려온
  것이 더 급하고, 노트는 탭을 눌러 언제든 돌아갑니다(글은 자동 저장이라 어느
  쪽을 남겨도 안 날아갑니다). 고른 것(`panes`)은 그대로 두고 **보여 줄 칸만**
  줄이므로, 다시 넓히면 펴 두었던 그대로 돌아옵니다(실측 확인).
  - JS(`narrow`, matchMedia)와 CSS의 `@media (max-width: 760px)`가 **같은
    값이어야** 합니다. CSS 쪽도 `.cornell-drawer.wide { width: auto }`로 함께
    막아 둡니다 — 그 선택자는 특이도가 한 칸 높아 그냥 두면 여기서 이깁니다.
- 발표 화면은 그만큼 **더** 좁아집니다(`.broadcast-overlay--noted2`).
  가려지는 것은 감수하는 손실입니다 — 그 순간 학생이 보는 주인공은 슬라이드가
  아니라 제 칸입니다.
- 손잡이도 함께 옮겨 붙습니다(`.cornell-handle.open.wide`). 760px 아래에서는
  `.cornell-handle.open`과 **같은 줄에 적어** 특이도로 지지 않게 합니다.
- **저장은 자동입니다 — '제출'이 아닙니다.** 이 앱에는 제출 상태가 없고,
  교사는 전광판으로 누가 썼는지 봅니다. **'제출' 단추를 만들지 마세요** —
  누른 학생은 '이제 못 고친다, 선생님께 전해졌다'로 믿는데 둘 다 사실이
  아닙니다. 진짜 제출을 만들려면 카드 update 규칙의 `changedOnly([...])`에
  필드를 더하고 배포해야 합니다.
- **저장할 곳은 글자를 칠 때 붙듭니다**(`pendingRef`에 쓸 곳을 함께 담습니다).
  학생이 쓰는 사이에 선생님이 다음 활동을 내보낼 수 있어('다들 1번 다 썼지?
  이제 2번'), 저장하는 순간의 `actIndex`로 쓰면 1번에 쓰던 글이 2번 칸에
  들어갑니다. 활동이 갈리면 정리(cleanup)가 **바뀌기 전에** 돌아 남은 글을
  원래 칸으로 씁니다. 화면을 벗어날 때도 한 번 더 씁니다.
- 짚는 것은 **자리(index)**입니다 — 파이썬 실행기의 '보내기'와 같은 이유로
  `matchActivitySections`를 쓰지 않습니다(제목이 안 맞는 순간 앞 활동 자리로
  뒤 활동의 글이 끌려옵니다).
- **Esc는 서랍을 닫습니다.** 활동 탭에 뜨는 창이 없어져 겹치는 것은 노트
  크게 보기 하나뿐입니다(`if (!open || viewerOpen)`).
- **여닫을 때 미끄러집니다**(`--cornell-slide` = 0.25s). 길이는 파이썬 실행기
  서랍(`.py-panel`)과 **같은 값**입니다 — 이 앱에서 옆에서 나오는 서랍은 늘
  같은 속도로 움직입니다. 함께 움직이는 것이 셋이라 **한 변수를 나눠 씁니다**:
  서랍(transform) · 손잡이(`right`) · 발표 화면(`right`). 하나만 고치면
  손잡이가 서랍에서 떨어져 나오거나 발표 화면 가장자리가 뒤늦게 따라옵니다.
  - **닫은 뒤에도 그만큼 더 그려 둡니다**(`rendered`). 곧바로 걷어 내면
    미끄러질 것이 없어져 그냥 사라집니다. `SLIDE_MS`(250)가 CSS의 그 값과
    **같아야** 합니다 — 짧으면 다 미끄러지기 전에 사라지고, 길면 다 닫힌
    자리에 빈 서랍이 남습니다.
  - 열 때는 `slidIn`을 **다음 프레임에**(rAF 두 번) 켭니다. 같은 프레임에
    붙이면 브라우저가 '화면 밖'을 한 번도 안 그리고 끝 모습으로 건너뜁니다.
  - **손잡이의 모습은 `open`이 아니라 `slidIn`을 봅니다.** `open`은 렌더에서
    바뀌는데 서랍의 `slidIn`은 그 뒤 effect에서 바뀌어, `open`으로 걸면
    손잡이가 한 프레임 앞서 출발합니다(실측: 여는 동안 서랍 모서리보다 최대
    100px 앞섬 → 고친 뒤 **3.4px**). 읽어 주는 기기에 전하는 값
    (`aria-expanded`·이름·툴팁)은 그대로 `open`입니다.
  - 닫으면 **초점을 손잡이로 되돌립니다**. 미끄러지는 동안 서랍은 아직
    DOM에 있는데 `aria-hidden`이라, 초점이 그 안(머리말 ×)에 남으면 '없는
    것'에 초점이 놓입니다.
  - **날개를 펴고 접을 때 칸이 짜부라지지 않습니다.** 칸을 `flex: 1`로 두면
    폭이 벌어지는 내내 두 칸이 남는 폭을 나눠 가져, 학생이 **지금 글을 쓰고
    있는 칸**이 380 → 190 → 380으로 출렁입니다. 칸 폭을 `--cornell-w`에 못
    박고 `.cornell-panes`에서 넘치는 만큼을 가립니다(`overflow: hidden` +
    `justify-content: flex-end` — 새 자리가 생기는 쪽이 왼쪽이므로 넘치는
    쪽도 왼쪽이어야 합니다). 실측: 펴는 내내 활동 칸 x=1020 · w=380 그대로.
  - 접히는 칸도 **250ms 더 그려 둡니다**(`linger`). 곧바로 감추면 글이 먼저
    사라지고 빈 자리만 뒤늦게 줄어 한 동작이 둘로 보입니다. 좁은 화면은
    뺍니다 — 거기서는 칸이 폭을 나눠 가져 두 칸이 잠깐 반씩 줄어듭니다.
  - 두 칸을 가르는 선도 `.wide`가 아니라 **보이는 칸 둘**로 판정합니다
    (`.cornell-pane:not([hidden]) + .cornell-pane:not([hidden])`) — `.wide`는
    접기 시작할 때 이미 빠져, 그것으로 보면 선만 먼저 사라집니다.
  - 760px 아래에서는 오른쪽이 아니라 **아래에서 올라옵니다**
    (`translateY(100%)`) — 미끄러지는 방향은 서랍이 붙어 있는 가장자리를
    따라갑니다. 거기서는 칸 폭 못 박기도 풉니다(시트가 화면 폭을 다 씁니다).
  - 움직임을 줄여 달라고 한 기기에서는 `--cornell-slide`만 0.01s로 줄입니다
    — 전환을 통째로 끄지 않아 `SLIDE_MS` 뒤에 걷어 내는 흐름이 그대로입니다.
  - **손잡이의 `transform`은 전환 목록에 넣지 마세요.** 그 단추는
    `translateY(-50%)`로 세로 가운데를 잡고 있어, 넣으면 날개를 펼 때마다
    그 값까지 다시 계산되어 위아래로 흔들립니다.
- **저장 단추는 노트 칸 안, 이름은 '노트 저장'입니다**(`.cornell-foot`이
  `.cornell-pane` 안에). 서랍 바닥에 두면 날개를 폈을 때 옆에 활동 칸이 함께
  서는데, 그 단추가 어느 칸을 저장하는지 흐려집니다(활동은 제 칸이 알아서
  자동 저장합니다). 그래서 칸마다 제 스크롤을 갖고, 저장 줄은 노트 칸에만
  붙습니다 — 노트를 내려도 활동 칸이 따라 밀리지 않습니다.
- 쓸 수 없는 자리는 미리 막고 까닭을 적습니다 — **모둠 프로젝트**(규칙이
  학생의 카드 생성을 막습니다)와 **잠긴 활동**. 교사의 '내보내기'는 잠긴
  활동을 **함께 엽니다** — 그 단추를 누르는 순간의 뜻이 '지금 이걸 쓰세요'라
  칸만 뜨고 못 쓰면 안 됩니다(칩 안의 자물쇠가 그대로 남아 따로 잠급니다).

**교사 쪽은 누르는 자리가 곧 내보내기입니다.** 예전에는 '활동 열기' 칩 줄
아래에 고르개 둘 + 단추 하나짜리 내보내기 줄이 따로 있었는데, 활동을 여는
일이 곧 내보내는 일이 되면서 두 줄이 같은 것을 두 번 묻게 되었습니다.

**공부방 활동은 세로 목록**(`.lesson-act-list` · `.lesson-act-item`),
**책방 활동·단계는 칩 줄 그대로**(`.lesson-book-chip` · `.lesson-act-chip`)입니다.

- 세로로 쌓는 까닭: 가로 칩이었을 때는 칩이 `활동 {i+1}`만 찍어 **활동 이름이
  아예 안 떴습니다**(이름을 넣으면 다섯 개가 한 줄에 안 섰습니다). 그래서
  선생님이 무엇을 내보내는지 번호로만 짚어야 했습니다. 한 줄이 통째로 한
  활동이면 이름을 넉넉히 적을 수 있습니다.
- **이름이 기본값 `활동 N`이면 이름 줄을 안 그립니다** — 그대로 그리면
  `활동 1 · 활동 1`로 같은 말이 두 번입니다(학생 서랍 머리말이 쓰는 방식과
  같습니다).
- 번호 칸은 **폭을 못 박습니다**(`min-width: 62px`) — 줄마다 이름이 다른
  자리에서 시작하면 목록이 들쭉날쭉해 보입니다. 방송 점이 그 칸 **안**에
  들어 있어 점이 붙어도 번호가 안 밀립니다. 긴 이름만 말줄임으로 접습니다.
- **책방까지 세로로 바꾸지 마세요** — 곁텍스트 단계는 이름이 짧고(표지·목차·
  머리말…) 여덟이라, 세로로 세우면 이 카드만 유독 길어집니다.
- 색·테두리·방송 점은 칩과 **같은 값**이라 책방 칩과 나란히 두어도 '같은 것을
  누르는 자리'로 읽힙니다.
- **자물쇠를 달지 마세요.** 이 줄에서 알아야 할 것은 '어디까지
  내보냈나'뿐이고 그건 **줄 색이 이미** 말합니다 — 살구빛이 내보낸 것, 회색이
  아직입니다. 자물쇠를 더하면 줄마다 그림이 하나씩 붙어 정작 지금 나가 있는
  줄의 점이 묻힙니다. 수업의 흐름도 '처음엔 잠김 → 내보내면 열림' 하나뿐이라
  여기서 잠글 일이 없습니다(다시 잠그는 것은 공부방 '활동 설정'의 활동별
  잠금 토글). 한때 칩 안에 자물쇠 단추를 뒀다가 그림만 남겼다가, 결국 둘 다
  걷었습니다.
  - **공부방 프로젝트에는 통째로 잠그는 길이 화면에 없습니다.** `editMode`
    (`'open'`/`'locked'`)는 자료와 규칙에 그대로 있지만 켜는 스위치를 없앴고,
    남아 있던 잠금은 담당 교사가 그 프로젝트를 열 때 `StudyProjectView`가
    `'open'`으로 풀어 줍니다 — 규칙이 이 값을 직접 보므로, 그냥 두면 활동을
    열어 줘도 학생 저장이 조용히 실패합니다. 잠그는 단위는 **활동 하나**뿐.
    (책방 독서 활동은 다릅니다 — 거기는 활동 전체 잠금 `locked`가 '활동
    잠그기' 단추로 살아 있고, 잠긴 활동은 내보내기 목록에서 아예 빠집니다.)
- **모드 고르개(열기/내보내기 전환)도 만들지 마세요.** 지금 어느 쪽인지
  화면 어딘가에 적혀 있어도 수업 중에는 눈이 그 줄에만 갑니다 — 열려고
  눌렀는데 학생 화면 스물몇 대가 바뀝니다.
- 지금 나가 있는 것만 방송 점 + 테두리 한 겹(`.live`). 그것을 다시 누르면
  **한 번 더 나갑니다**(거두는 것이 아니라) — `at`이 바뀌어 서랍을 닫아 둔
  학생 화면이 다시 열립니다. 거두는 길은 아래 상태 줄의 '그만 보내기' 하나뿐.
- 아래 **상태 줄**(`.lesson-push-row`)은 '지금 무엇이 나가 있나'만 적습니다.
  나가 있는 것이 이 화면에 안 보이는 칩일 수 있어(다른 프로젝트의 활동, 안
  고른 독서 활동) 이름이 필요합니다. 나간 것이 없으면 줄 자체가 없습니다.
- 이 칸은 **연결된 프로젝트가 없어도** 섭니다 — 독서 활동만 있으면 그것만.
- **손잡이에 활동 배지를 달지 마세요.** 한때 '활동'이라 적힌 배지를 뒀는데,
  손잡이가 세로쓰기라 글자가 한 자만 보여 무슨 말인지 알 수 없었고, 활동이
  오면 서랍이 저절로 열리므로 그 배지가 보일 새도 없습니다. 닫아 둔 학생에게
  알리는 것은 손잡이 툴팁('오늘의 활동이 있어요')입니다.
- 활동 탭에서 타이핑하면 전광판의 ✍️ 신호가 함께 올라갑니다(`onType`) —
  내보내 놓고 누가 손을 움직이는지 보는 자리라 여기서 값이 큽니다.

### 활동보기 — 수업 중 자리표의 첫 탭 (교사)

- `components/LessonAnswerPanel.jsx`. **지금 내보낸 활동에 학생들이 쓴 답을
  이전/다음으로 한 명씩** 읽습니다. 활동을 내보내 놓고 '누가 뭐라고 썼나'를
  보려면 공부방으로 건너가 모아보기를 열어야 했는데, 그러면 슬라이드도
  자리표도 화면에서 사라졌습니다.
- **탭 줄은 한 줄에 넷입니다** — `활동보기 · 개별 보기 · 모둠 보기 · 궁금한
  순간`(`LessonSeatPanel`의 `TABS`). 바깥 탭을 한 겹 더 두면 탭 줄이 위아래로
  둘이 되는데, 어차피 한 자리에서 갈아 끼우는 넷이라 나란히 두는 편이 줄
  하나를 아끼고 판이 그만큼 위로 올라옵니다. **활동보기가 맨 앞**입니다 —
  활동을 내보낸 뒤 가장 자주 여는 자리라서요.
  - 넷이 되면서 손든 인원 뱃지가 둘째 줄로 내려가는 폭이 **패널 300px →
    351px**로 올라갔습니다(실측). 탭 글자를 늘리면 다시 재세요.
- **패널 이름은 `우리는 공부중`입니다**(`LessonSeatPanel`의 `PANEL_NAME`).
  탭이 넷이 되면서 '자리표'는 이 패널의 한 보기일 뿐이라, 그 이름을 머리말에
  두면 활동 답을 읽는 동안 화면이 거짓말을 합니다. 보고 있는 탭을 따라 제목을
  바꿔 봤더니 이번에는 접힌 단추(`자리표`)와 이름이 어긋났습니다 — 넷을 함께
  덮는 이름 하나면 둘 다 풀립니다. 지금 무엇을 보는지는 바로 아래 딸림글이
  말합니다. **접힌 단추 · 펼친 머리말 · 읽어 주는 이름표가 그 상수 한 곳을
  봅니다** — 세 군데에 따로 적으면 한쪽만 고쳤을 때 두 이름이 생깁니다.
  (수업 화면 머리말의 손들기 툴팁도 이 이름으로 적습니다.)
- **머리말의 손들기를 누르면 개별 보기 탭으로 돌아옵니다.** 보고 있는 탭도
  펼침(`open`)과 함께 `LessonMode`가 쥡니다 — 안 그러면 손들기를 눌렀는데
  학생 답이 떠 있습니다.
- **읽는 문서가 하나도 안 늡니다** — 수업 화면이 이미 구독해 둔 학생 카드
  (`subscribeStudyCards`)를 그대로 받습니다. 무거운 셈(`matchActivitySections`
  × 명단)은 memo 안에 있고, 그 칸은 **활동보기 탭일 때만 마운트**되므로 다른
  탭을 보는 동안은 아예 안 돕니다.
- **차례는 학번순, 반 전체입니다.** 안 쓴 학생을 빼면 '누가 아직 안 썼나'가
  이 화면에서 사라집니다(자리표·출석부와 같은 기준). 머리에 '쓴 사람 n / N'.
- **모아보기(`StudyActivityWall`)를 그대로 안 씁니다.** 그쪽은 정렬·띄우기·
  실명 펼치기·과일까지 담은 큰 모달인데, 여기는 자리표 옆 반 칸짜리 자리라
  하는 일을 하나로 줄였습니다. 크게 견주어 볼 일이면 지금처럼 모아보기를
  엽니다.
- **책방 활동은 여기 안 옵니다.** 곁텍스트·RAFT의 답은
  `bookActivities/{id}/entries/{uid}`에 있어 이 화면이 구독하지 않는
  자리입니다. 그때는 어디서 보는지만 한 줄 적습니다 — 빈 화면을 띄우면
  '고장'으로 보입니다. 다른 프로젝트의 활동을 내보낸 중(`taskActIndex`가
  null)일 때도 같습니다.
- 짚는 것은 **제목까지 함께 보는 `matchActivitySections`**입니다 — **읽기만
  하는 화면**이라 교사가 활동 이름을 바꿔도 학생이 쓴 답을 그대로 잡아야
  합니다(자리를 정해 **쓰는** 곳에서는 그 함수를 쓰지 않습니다).
- 답 본문은 **`.study-card-content`**로 그립니다 — 코드 블록·글머리 기호
  서식이 걸린 선택자입니다. 새 클래스를 지으면 학생이 서랍에서 쓴 코드 블록이
  여기서만 밋밋한 글로 보입니다.
- **방향키를 걸지 않습니다** — 수업 화면의 ← →가 이미 슬라이드를 넘깁니다.
  한 번 눌러 슬라이드와 학생이 함께 넘어가면 어느 쪽을 넘긴 것인지 알 수
  없습니다.
- 키는 자리표가 정합니다(`.lesson-seat-body`의 `bodyH`) — 답이 길면 그 안에서
  구릅니다. 그래서 `.lesson-ans`에 `height: 100%`를 덧대지 마세요(자리표를
  아직 안 그려 `bodyH`가 0인 순간 그 칸이 0으로 접힙니다).

**모둠 보기는 개별 보기의 자리표 그대로이고, 줄의 첫 칸이 모둠 이름입니다.**
한때 모둠마다 색 띠를 두른 상자(`.lesson-seat-group`)를 세우고 그 안에 학생
칸을 흐름 배치로 흘려 놓았는데(`width: 52px`), 같은 패널에서 탭만 바꿨을
뿐인데 칸이 작아지고 한 줄에 드는 수도 달라져 두 화면이 남남처럼 보였습니다.

- 지금은 자리표와 **같은 `.attend-seatmap-grid` 하나**(6칸)에 학생 칸을 그대로
  깔고, **모둠 이름이 그 줄의 첫 칸**이 됩니다
  (`.lesson-seat-groupgrid > .lesson-seat-group-name`의 `grid-column-start: 1`).
  이름 칸이 늘 1열에서 시작하므로 앞 모둠이 줄을 덜 채웠어도 격자가 다음
  줄로 건너뛰고, 그 뒤로 모둠원이 차례로 섭니다 — **자리표 한 판 안에서 줄이
  곧 모둠**입니다.
  실측(패널 320·430·560px): 자리 칸이 48.3·66.7·88.3px 정사각형으로 두
  보기에서 **같은 값**, 이름 칸도 같은 정사각형. 모둠이 4·5·7·0·4·2명일 때
  줄은 `이름+4` · `이름+5`(꽉 참) · `이름+5`에 이어 `2` · `이름+안내` ·
  `이름+4` · `이름+2` — 한 줄에 두 모둠이 섞이는 일이 없습니다.
- **이름 칸을 `grid-row: span N`으로 늘이지 마세요.** 모둠원을 늘 2~6열에만
  세우면 이어지는 줄이 더 깔끔하지만, N을 내려면 '한 줄에 몇 명인가'를 JS가
  알아야 합니다 — 그 값은 6이 아니라 **768px 아래에서 4**로 바뀝니다
  (`@media` 안의 `.attend-seatmap-grid`). 한쪽만 고치면 좁은 화면에서 빈 칸이
  줄줄이 생기므로, 열 수를 모르고도 맞는 지금 방식을 씁니다.
- **학생 칸에 width나 flex를 덧대지 마세요.** 그 한 줄이 곧 두 보기가 다시
  갈리는 자리입니다. 규칙은 전부 `.lesson-seat-groupgrid` 아래로 한정해,
  모둠 보기에만 있어야 할 것이 개별 보기로 새지 않게 합니다.
- 이름 칸은 **흰 바탕에 모둠 색 테두리 2px + 모둠 색 글자**입니다. 출석 색
  (초록·주황·회색)을 쓰는 학생 칸 사이에서 '사람이 아닌 칸'으로 곧바로
  갈립니다. 테두리가 2px이라 안쪽이 1px 좁아지는 만큼 padding에서 돌려줍니다
  (`.attend-seat--me`와 같은 셈).
- **글자는 학생 이름과 같은 12.5px**입니다 — 한 판에 나란히 선 칸이라 크기가
  다르면 이름 칸만 따로 놉니다. 모둠 이름은 교사가 지어 길 수 있고('3모둠'이
  37px인데 가장 좁은 패널의 칸 안쪽이 40px뿐입니다 — 실측), 넘치면
  `overflow-wrap: anywhere`로 줄을 바꿔 담습니다. 실측으로 '책읽는 사자 모둠'도
  세 줄로 접혀 칸 안에 들어가고 칸은 정사각형 그대로입니다.
- `aspect-ratio: 1 / 1`이 **아무도 없는 모둠의 줄**을 지킵니다 — 학생 칸이 선
  줄에서는 줄 높이가 이미 칸 높이라 그대로 맞지만, 그 줄에는 정사각형이
  하나도 없어 빼면 납작해집니다. 안내('배정된 학생이 없어요')는 이름 칸
  오른쪽 남은 자리를 다 씁니다(`grid-column: 2 / -1`) — 칸 하나만 한
  폭(48px)에 넣으면 글자가 서너 자씩 접힙니다.
- **자리 이동(드래그)은 그대로 개별 보기에만** 있습니다. 격자가 같아졌다고
  여기에 붙이지 마세요 — 모둠 보기에는 '자리'가 없어 어디로 옮기는 것인지가
  성립하지 않고, 끌어 옮기기가 쓰는 자리 번호(index)도 여기엔 없습니다.
- **선생님 보기(180도 뒤집기)도 안 겁니다** — 여기 줄 차례는 교실의 앞뒤가
  아니라 모둠 순서라, 돌리면 모둠이 거꾸로 서기만 합니다.

### 수업 메모 (교사) — 지난 메모는 옆 패널로

- `components/LessonMemoModal.jsx`. **쓰는 칸이 먼저**입니다 — 수업 중에 여는
  화면이라 쓰기까지 한 번에 닿아야 하고, 목록을 아래에 펼치면 그만큼 쓰는
  칸이 화면 위로 밀립니다.
- 쓰는 칸 아래에 **반 버튼 줄**(`.memo-class-row`) — 내가 맡은 반이 알약으로
  서고, 누르면 (ㄱ) 그 반의 지난 메모가 **오른쪽 패널**로 미끄러져 나오고
  (ㄴ) **앞으로 적는 메모가 그 반에 저장됩니다**(`writeClassId`).
  예전에는 '지난 메모' 드롭다운 하나였고 그것은 늘 **지금 이 반**의
  것이었습니다. 옆 반 메모를 보려면 캘린더를 켜고 날짜를 짚어 들어가는
  길뿐이었는데, 대개 찾는 것은 '그 반에 뭘 적어 뒀더라'이지 '9월 3일에 뭘
  적었더라'가 아닙니다.
- **여기서 고른 반이 곧 저장될 반입니다.** 한때는 이 줄이 '보기'만 했고
  저장은 늘 화면이 열려 있던 반(페이지의 `classId`)으로 갔습니다 — 옆 반
  메모를 펴 놓고 적으면 그 글이 엉뚱한 반에 들어갔고, 화면에는 옆 반 이름만
  보였습니다. 지금은 고른 반 하나가 '보는 반'이자 '쓰는 반'이고, **창 제목의
  반 이름이 곧 저장될 곳**입니다.
  - 같은 반을 다시 눌러 패널을 닫아도 **쓰는 반은 그대로**입니다. 닫는
    동작이 '어디에 쓸지'까지 흔들면 안 됩니다.
  - 보관된 반을 고르면 저장을 잠그고 까닭을 밝힙니다 — 규칙
    (`ownsClassEditable`)이 어차피 막으므로, 눌러 보고서야 실패를 알게 하지
    않으려고요.
  - 페이지에서 반을 바꾸면 쓰는 반도 그 반으로 되돌아갑니다.
- **한 줄에 셋씩, 격자입니다**(`repeat(3, 1fr)` — flex-wrap 아님). 이름
  길이대로 흘려 두었더니 한 줄에 넷이 서다 셋이 서다 해서, 같은 반이 창을
  열 때마다 다른 자리에 있었습니다. 칸이 `1fr`이라 마지막 줄에 하나만 남아도
  줄을 통째로 늘여 잡지 않고(활동 열기 칩의 `flex: 1 1 0`에서 겪은 그 모양),
  긴 이름은 말줄임으로 접습니다. 768px 이하에서는 둘씩 — 셋으로 나누면 칸이
  100px 안팎이라 '인공지능 기초 A'가 접힙니다(실측).
- **진도 칸 — 주제(`topic`) · 시작 페이지(`pageFrom`) · 마지막 페이지
  (`pageTo`)**. 본문에 글자로 섞지 않고 **제 칸으로** 둡니다. `date`가 제
  칸이라 달력이 되는 것과 같은 이유입니다 — 글자 속에 있으면 앱이 어디부터가
  주제인지 몰라 표로 뽑거나 골라 볼 수 없습니다.
  - **페이지도 같은 이유로 두 칸입니다.** 한 칸에 '112~119'로 적으면 그 안이
    다시 글자 속이라, 시작만 적힌 것인지 끝만 적힌 것인지 앱이 모릅니다.
    **한쪽만 적어도 되고 둘 다 비어도 됩니다** — '112쪽부터'만 아는 날도,
    '119쪽까지'만 정해 둔 날도 있습니다.
  - **`pages`는 거울입니다**(수업 노트의 blocks ↔ cue/notes와 같은 방식).
    `progressFields`가 `112~119`·`112`·`~119` 꼴로 함께 적어 둡니다 — 옛
    메모를 읽는 자리가 아직 있고 나중에 표로 뽑을 때도 한 줄이 필요해서요.
    **진짜 값은 `pageFrom`·`pageTo`입니다.**
  - 읽는 쪽은 `lessonMemoPages` **한 곳**을 거칩니다. 새 두 칸이 있으면 그것,
    없으면 옛 `pages` 한 줄을 물결·붙임표로 갈라 읽습니다(가르는 글자가
    없으면 통째로 '시작'). **자료를 미리 옮기지 않습니다** — 그 메모를 고쳐
    저장할 때 두 칸으로 옮겨 갑니다.
  - 화면에 그릴 때 마지막만 있으면 `~p.119`입니다 — 어느 쪽을 적은 것인지
    남깁니다(`p.119`로 적으면 시작으로 읽힙니다).
  - 쓰는 칸은 **한 묶음 안에 칸 둘**입니다(`페이지 [112] ~ [119]`). 칸마다
    '시작 페이지'·'마지막 페이지' 이름표를 세우면 좁은 패널에서 이름표가
    글자 칸보다 넓어져, 가운데 물결이 그 구실을 하고 이름은
    placeholder·title·aria-label로 남깁니다. 두 칸은 **같은 폭**입니다 —
    한쪽이 넓으면 그쪽이 주된 값처럼 보입니다.
  - 세 칸을 그리는 곳은 `ProgressFields` **한 조각**입니다(쓰는 칸 ·
    지난 메모 고치기 · 달력 고치기). 옆 패널에서는 좁아서 위아래로
    풉니다(`.memo-side-panel .memo-prog-row`).
  - **'반 고르기' 줄의 '진도' 토글로 켤 때만** 두 칸이 나옵니다. 수업
    메모는 진도만 적는 자리가 아니라('오늘 3모둠 분위기가…'), 늘 띄우면
    진도가 아닌 메모를 적을 때 쓸모없는 빈 칸 둘을 보게 됩니다. 켠 상태는
    localStorage(`memo_progress_on`)에 기억합니다 — 차시마다 적는 선생님이
    열 때마다 다시 누르면 그 자체가 일입니다.
    - **자리는 쓰는 칸 바로 아래 줄의 왼쪽 끝**입니다(`.memo-foot`).
      그 단추가 늘리는 것이 쓰는 칸이라 그 칸 옆에 있어야 합니다 — 한때
      '캘린더 보기' 옆(반 고르는 줄)에 뒀는데, 나란히 선 그 단추가 옆
      패널을 여는 것이라 이것도 무언가를 여는 단추로 읽혔습니다. 그보다
      먼저는 날짜 줄 오른쪽 끝에 작은 알약으로 혼자 있었습니다.
    - 크기는 `.memo-cal-btn` 규칙을 그대로 씁니다 — 같은 줄의 '저장'과
      높이가 맞습니다(테두리 1px만큼 세로 여백을 덜어 둔 값입니다).
    - **그 줄의 오른쪽 끝은 '저장'이고, 평소에 다른 글자는 없습니다.**
      한때 'Ctrl+Enter로도 저장돼요'가 늘 떠 있었는데, 한 번 읽으면 그만인
      말이 창을 열 때마다 자리를 차지했습니다(단축키는 그대로 됩니다).
      그 가운데 자리는 이제 **저장이 막혔을 때 그 까닭만** 씁니다
      (`blockReason` 한 곳에서 정합니다 — 보관된 반 · 글자 수 초과 ·
      주제만 채우고 본문이 빈 경우). 늘 있는 글이 아니라야 떴을 때
      눈에 띕니다.
  - **지난 메모 패널의 '진도 보기'**(`.memo-prog-filter`, 머리줄 × 왼쪽)로
    **주제·페이지가 있는 메모만** 봅니다. 한 반의 목록에는 그날그날의 메모와
    진도가 섞여 있어 학기 흐름을 훑으려면 눈으로 걸러 내야 했습니다.
    - **읽는 문서가 늘지 않습니다** — 이미 받아 둔 목록에서 골라 그립니다.
    - 판정은 `lessonMemoProgress` **한 곳**을 그대로 씁니다. 목록에 진도
      줄을 그리는 기준과 거르는 기준이 다르면, 걸러 낸 목록에 줄 없는
      메모가 섞입니다.
    - 머리의 건수도 **보이는 만큼**을 셉니다(전체를 그대로 두면 목록은 둘인데
      '12건'이라 적혀 고장으로 보입니다). 빈 상태도 갈라 적습니다 —
      '진도를 적어 둔 메모가 없어요'.
    - **반을 바꿔도 켜진 채로 둡니다.** 이 단추를 누르는 까닭이 '반마다
      진도가 어디까지 나갔나'를 훑는 것이라, 반을 옮길 때마다 꺼지면 매번
      다시 눌러야 합니다(고치던 것·지우려던 것만 놓습니다).
  - **고치는 폼에서는 늘 보여 줍니다**(지난 메모 패널 · 달력 패널 둘 다).
    이미 있는 값을 고치러 온 자리라 접어 두면 열어 봐야 압니다.
  - **본문은 여전히 있어야 합니다.** 규칙이 `text.size() > 0`을 요구해
    주제·페이지만 채우고 저장할 수 없습니다. 화면이 미리 막고 까닭을
    적습니다('수업 내용도 한 줄 적어야 저장돼요') — 눌러 보고서야 알게 하지
    않으려고요.
  - **`updateLessonMemo`는 `extra`를 안 주면 진도 칸을 손대지 않습니다.**
    체크 줄을 켜고 끄는 경로가 본문만 넘기는데, 거기서 주제·페이지가 지워지면
    안 됩니다. 빈 문자열을 담아 넘겼을 때만 지웁니다(칸을 비운 것).
    시험으로 확인했습니다.
  - 읽는 자리의 한 줄은 `lessonMemoProgress` **한 곳**에서 짓습니다
    (`조건문 · p.112~119`). 페이지에 이미 `p`나 `쪽`이 있으면 `p.`를 안
    붙입니다 — `p.p.120`이 되면 안 됩니다. 셋 다 비면 `null`이라 **진도 칸이
    없는 예전 메모는 아무것도 안 그립니다**(자료를 옮기는 일이 없습니다).
  - **규칙을 안 건드립니다.** `lessonMemos`의 create/update가
    classId·authorId·text·createdAt만 검사하고 필드 목록을 못 박아 두지
    않아, 칸을 늘려도 그대로 통과합니다(확인함).
  - 아직 **표로 내보내기는 없습니다.** 칸이 생겼으니 `downloadWorkbook`으로
    날짜·주제·페이지·내용 네 열을 뽑는 일은 그때 붙이면 됩니다.
- **체크 줄**(`checkList`) — 수업 메모 툴바의 다섯째. 할 일을 적어 두고
  나중에 지우는 자리라 글머리 기호만으로는 모자랐습니다.
  - **표시는 목록(`<ul>`)이 아니라 줄(`<li class="check">`)에 붙습니다.**
    한 목록 안에서 글머리 기호 줄과 체크 줄을 **섞어 쓸 수 있어야** 하기
    때문입니다. 처음에는 `<ul class="checklist">`였는데 그러면 한 목록이
    통째로 한 가지 모양이 됩니다. 옛 모양도 계속 읽습니다 —
    `CHECK_ITEM_SELECTOR`(`li.check, ul.checklist > li`)가 둘 다 잡고,
    그 목록을 손대면 줄마다로 풀어 둡니다(`spreadLegacyList`).
  - 켠 줄은 `class="check done"`입니다. **`<input type="checkbox">`를 쓰지
    않습니다** — 그러려면 정화기(`sanitizeHtml`)에 태그 하나와 속성 둘을
    새로 열어야 하는데, 남의 글이 그대로 뿌려지는 자리가 여럿이라
    (질문·답변·카드·메모) 여는 문은 적을수록 좋습니다. class 두 값을 더하는
    쪽이 훨씬 좁고, 네모는 CSS가 그립니다. 한 줄 미리보기(`stripHtml`)에
    글자가 남는 것도 이쪽뿐입니다.
  - **켜고 끄는 것은 읽는 자리에서** 합니다(지난 메모 패널·달력의 펼친 메모).
    켜려고 '수정'으로 들어갔다 나오게 하면 체크 줄을 둔 뜻이 없습니다.
    누르면 그 메모 문서의 HTML을 고쳐 바로 씁니다(`toggleChecklistItem`).
  - **네모는 글머리 기호가 서던 자리(글자 왼쪽)에** 그립니다 — 그래야 한
    목록 안에서 '• 줄'과 '☐ 줄'의 글자가 같은 세로선에 섭니다. 누르는 자리
    판정도 거기 맞춰 `hitCheckBox` **한 곳**에 있습니다(줄의 왼쪽 끝보다
    왼쪽). 글자를 누르면 안 켜집니다 — 읽다가 무심코 눌러 할 일이 지워지면
    안 됩니다.
  - 목록 자체는 브라우저(`execCommand`)가 만들므로 Enter로 줄이 이어지고,
    새 `<li>`는 앞 줄의 표시를 물려받습니다(체크 줄 뒤에서는 체크 줄).
- **버튼에 건수를 적지 마세요.** 반마다 메모를 미리 읽어야 하는데(반이
  서넛이면 그만큼 리스너) 이 화면은 쓰러 여는 곳입니다. 건수는 패널을
  열었을 때 그 머리말에만(`.memo-panel-count`).
- **한도가 부르는 자리마다 다릅니다** — `subscribeLessonMemos`의 셋째 인자.
  - 한 반을 펼쳐 보는 곳은 `LESSON_MEMO_HISTORY`(**200**). 50이던 것을
    올렸습니다: 수업 진도를 차시마다 적으면(주 2회 × 16주 = 32건) 거기에
    그날그날의 메모가 섞여 학기 중반에 넘고, 초반 진도가 목록에서
    사라졌습니다(달력으로는 찾히지만 날짜를 짚어 들어가야 합니다).
  - 달력이 **다른 반까지** 훑는 곳만 `LESSON_MEMO_CALENDAR`(**50**).
    달력은 켜는 순간 맡은 반 수만큼 리스너가 걸려, 200을 그대로 쓰면 한 번에
    1,200건입니다. 거기서 답하는 물음은 '그날 수업이 있었나'라 최근 것만으로
    충분합니다. **이 자리를 기본값으로 되돌리지 마세요.**
  - 정렬 질의는 그대로 `createdAt` 순입니다(`date`로 orderBy하면 그 필드가
    없는 예전 메모가 통째로 빠집니다). 200건이라 화면 정렬로 충분합니다.
- **옆 자리는 하나뿐**입니다 — 지난 메모 패널과 캘린더 패널이 같은 자리에
  서므로 껍데기(`.memo-side-panel`)를 함께 쓰고 **한 번에 하나만** 엽니다.
  달력만의 규칙은 `.memo-cal-panel`에 남아 있습니다.
- **차례는 이름순**입니다(`localeCompare("ko", { numeric: true })`).
  한때 만든 차례였는데, 같은 이름으로 시작하는 반들이 만든 날에 따라
  흩어졌습니다 — '인공지능 기초 A · J · B'처럼요. 교사는 이 줄을 이름으로
  훑으므로 A·B·J로 나란히 서야 눈이 한 번에 짚습니다. `numeric`을 켜 두어
  '1반 · 2반 · 10반'도 제대로 섭니다(글자순이면 10반이 2반 앞).
  `subscribeClasses`가 매겨 주는 차례(`sortByClassOrder`)를 쓰지 않는 까닭은,
  그것이 '반 관리하기에서 끌어 정한 값'이 있는 반을 앞세우고 없는 반을 뒤로
  미뤄, 한 번도 안 끈 반이 이름과 상관없이 맨 끝에 서기 때문입니다.
- **지금 이 반을 맨 앞으로 올리지 마세요.** 올리면 반을 옮길 때마다 줄이
  통째로 다시 짜여 어제 누르던 반이 매번 다른 자리에 있습니다(책방 활동
  목록을 만든 차례로 되돌린 것과 같은 이유). 지금 이 반은 자리가 아니라
  `.mine`(테두리)이 말합니다. 펼쳐 둔 반은 `.on`(채움) — 뜻이 다릅니다:
  `.mine`은 '어느 반에 쓰는 중인가'(= `writeClassId`), `.on`은 '지금 어느
  반을 펼쳐 뒀는가'. 대개 같은 반이지만, 패널을 닫으면 `.on`만 꺼집니다.
- 패널의 메모는 **`null`이면 '아직 안 왔다'**, 빈 배열이면 '없다'입니다.
  갈라 두지 않으면 반을 바꿀 때마다 '아직 적어 둔 메모가 없어요'가 한 번
  스칩니다(첫 화면 절의 그 함정과 같습니다).
- 지금 이 반의 메모는 **이미 걸어 둔 구독을 그대로** 씁니다 — 같은 컬렉션에
  리스너를 둘 걸 이유가 없습니다. 다른 반은 그 버튼을 눌렀을 때만.
- 보관된 반도 버튼에 섭니다(`.archived`, 옅게) — 지난 메모는 읽히고,
  고치기·지우기만 가립니다(규칙 `ownsClassEditable`이 어차피 막습니다).

### 답변 고치기·지우기 — 쓴 사람 본인 (`QuestionModal`)

- 말풍선 아래 한 줄(`.chat-msg-foot`)의 오른쪽 끝에 **수정 · 삭제**
  (`.chat-own-tools`). 왼쪽은 지금까지대로 반응 알약입니다 — 줄을 따로 두면
  말풍선 사이가 그만큼 벌어져 대화가 성기어집니다(실측 한 줄 25px).
- **규칙은 한 줄도 안 건드렸습니다.** `answers`의 update가 이미
  `resource.data.authorId == uid() && changedOnly(['content','imageUrl','images'])`
  이고 delete도 `authorId == uid() || isTeacher()`였습니다 — **없던 것은
  화면과 `lib/store.js`의 함수뿐**이었습니다(지난 노트 수정에서 겪은 그
  모양과 같습니다). 그래서 **규칙 배포가 필요 없습니다.**
  그 문이 얼마나 열려 있는지는 `tests/rules/answerEdit.test.mjs`(열두 개)에
  박아 두었습니다 — 작성자·시각·반응·`understood`는 고치기를 핑계로도 못
  바꾼다는 것까지 함께.
- **고치는 것은 본문 글뿐입니다.** 첨부 이미지는 그대로 두고 편집 칸 아래에
  '첨부한 이미지 n장은 그대로 남아요'로 알립니다 — 규칙은 첨부도 열어
  두지만, 거기까지 손대게 하면 '고치기'가 답변을 새로 쓰는 일만큼 커집니다.
- **고치는 자리는 말풍선 그 자리**입니다(`.chat-msg--editing`). 창을 새로
  띄우면 앞뒤로 오간 말이 가려져 무엇에 답한 글인지 보면서 고칠 수 없습니다.
  그동안만 가로를 다 씁니다 — 말풍선은 88% 폭이라 거기에 에디터를 욱여넣으면
  툴바가 넘칩니다(실측 410px 칸에서 에디터 382px · 툴바 넘침 0).
- **글을 통째로 비우면 저장이 막힙니다**(첨부가 있으면 빈 글도 허용).
  빈 말풍선이 남는 것보다 '지우려면 삭제를 누르세요'가 맞습니다.
- **'이해됐어요'로 짚힌 답변은 못 지웁니다**(단추가 꺼지고 툴팁이 까닭을
  적습니다). 지우면 질문 문서의 `understoodAnswerId`가 **없는 답변을 가리킨
  채 '해결됨'으로** 남는데, 그 값을 치우는 것은 질문자·교사만 할 수 있어
  작성자 혼자서는 뒷정리가 안 됩니다. 표시를 먼저 해제하면 지울 수 있습니다.
- **`answerCount`를 화면에서 내리지 마세요** — `onAnswerDeleted` Cloud
  Function이 서버에서 내립니다(올리는 쪽도 같습니다). 둘 다 하면 두 번 깎입니다.
- 첨부 파일은 `deleteAttachedFiles`로 함께 지웁니다(질문 삭제와 같은 길) —
  안 지우면 Storage에 주인 없는 이미지가 남습니다.
- **교사에게는 화면을 안 달았습니다.** 규칙은 교사의 수정·삭제를 허용하고
  시험도 그렇게 박아 두었지만, 남의 답변을 지우는 일은 따로 정할 문제라
  단추를 만들지 않았습니다. 붙인다면 `canEdit`·`canDelete`에 `admin`을
  더하는 것으로 끝납니다.
- mock은 **답변 문서를 갈아 끼웁니다**(제자리에서 고치지 않고) — 같은 객체를
  고쳐 두면 그것을 들고 있는 쪽이 늘 같은 참조를 받아 React가 다시 그리지
  않습니다(반 문서·활동 문서에서 겪은 그 함정과 같습니다).

### 서식 에디터 — 전송 버튼은 박스 아래 제 줄

- `variant="chat"`은 **두 덩이**를 내놓습니다 — 박스(`.rte`)와 그 아래
  전송 줄(`.rte-sendrow`, 오른쪽 끝). 감싸는 칸을 새로 두지 않고
  **형제로** 내보내므로, 쓰는 자리가 **세로 flex**라야 줄이 제대로 섭니다
  (지금 쓰는 둘 — `.chat-compose` · `.gmemo-thread-pane` — 다 그렇습니다).
  바깥에 칸을 하나 더 두면 `.rte`를 겨냥해 둔 규칙들이 한 겹 어긋납니다.
- **왜 옮겼나**: 예전에는 채팅 툴바 오른쪽 끝에 `margin-left: auto`로
  붙어 있었는데, 그 줄에 도구가 아홉(첨부·그리기 · B·I·U · 글머리·번호·
  체크 · 코드)이라 좁은 칸에서 줄이 넘쳐 **전송 버튼이 세로로 잘렸습니다**
  (`.rte`가 `overflow: hidden`이라 그대로 깎입니다 — 실제 신고).
  실측: 박스가 **378px 아래로 내려가면** 잘리기 시작해 366px에서 24/34px,
  346px에서 4/34px, 316px에서 아예 안 보였습니다. 질문 모달의 오른쪽 칸이
  꼭 그 언저리입니다.
- 줄을 따로 두니 **폭에 안 걸리고**(실측 316~386px에서 94px 온전히 보임)
  글자를 달 자리가 생겨 `✈ 보내기`가 됐습니다 — 그림 하나로 짐작하던
  자리에 할 일이 적힙니다. 대가는 세로 **약 42px**(줄 34 + 여백 8).
- **툴바는 넘치면 옆으로 구릅니다**(`overflow-x: auto` + 단추의
  `flex-shrink: 0`, 스크롤바는 감춤). 상단바 이동 메뉴와 같은 방식입니다 —
  아이콘을 쭈그려 알아보기 어렵게 만드는 것보다 낫습니다. 전송이 빠져
  실제로 구르는 것은 박스가 340px 아래일 때뿐입니다(실측).
- **`.gmemo-thread-pane`에서는 위 여백을 풉니다** — 그 칸은 제 `gap`(10px)이
  이미 줄 사이를 벌려 둬, 그냥 두면 이 줄만 18px로 벌어집니다.
- `toolbarTop`(모둠 메모)은 **툴바만** 위로 갑니다 — 전송 줄은 어느 쪽이든
  맨 아래입니다.
- 모바일(768px 이하)에서는 단추만 조금 줄입니다(34 → 32px). 그 자리의
  `.rte-send { width: 30px }`는 **걷어 냈습니다** — 글자가 든 알약이라
  폭을 못 박으면 '보내기'가 잘립니다.

### 서식 에디터 — '- '로 글머리 기호

- 줄 맨 앞에 붙임표 하나를 적고 **띄어쓰기**를 누르면 그 줄이 글머리 기호가
  됩니다(`RichTextEditor`의 `dashToBullet`). 마크다운을 쓰는 손이 그대로
  통해, 여러 줄을 잇달아 적을 때 툴바를 짚으러 가느라 흐름이 안 끊깁니다.
  `RichTextEditor`를 쓰는 자리 전부에 걸립니다(수업 메모·수업 노트·동료
  평가·질문·공지…).
- **툴바에 글머리 기호가 있는 에디터에서만** 합니다(`canBullet`) — `tools`로
  좁혀 둔 자리에서 저절로 목록이 생기면 되돌릴 단추가 없습니다.
- 안 걸리는 자리 셋: 이미 목록 안(`isInList`) · 코드 블록 안(거기 '-'는 진짜
  붙임표) · 줄 가운데(`a - b`). 판정은 **커서 앞 같은 줄의 글자가 정확히
  '-' 하나**일 때뿐입니다.
- **줄은 블록에서도, `<br>`에서도 새로 시작합니다.** `Range.toString()`은
  `<br>`을 없는 셈 쳐서 앞줄까지 이어 붙이므로(`<div>가나<br>-|</div>` →
  `가나-`), `linePrefix`가 조각을 떠서 직접 훑고 `<br>`을 만나면 지금까지
  모은 것을 버립니다. 그래서 Shift+Enter로 접은 줄에서도 앞줄을 안 건드리고
  그 줄만 목록이 됩니다(실측 `가나<br><ul><li>다</li></ul>`).
- 붙임표는 `execCommand('delete')`로 지웁니다 — 커서 앞 한 글자라 범위를 손으로
  잡을 것 없이 정확하고, **Ctrl+Z 두 번이면 '- '로 돌아옵니다**(한 번은 목록,
  한 번은 붙임표). 진짜로 붙임표를 적고 싶을 때 빠져나갈 길입니다.
- `'* '`는 안 걸립니다 — 붙임표 하나로 충분하고, 문 하나에 판정도 하나입니다.

### 서식 에디터 — '```'로 코드 블록

- 줄 맨 앞에 **백틱 셋**을 적으면 그 줄이 코드 블록이 됩니다
  (`RichTextEditor`의 `ticksToCode`). `- `로 글머리 기호를 만드는 것과 **같은
  결·같은 판정**입니다 — 툴바의 `</>`를 짚으러 가지 않아도 마크다운을 쓰는
  손이 그대로 통합니다.
- **셋째 백틱을 칠 때** 바뀝니다(넷째가 아니라). 앞의 둘은 이미 화면에 있어
  '한 번 더 치면 되겠다'가 보이고, 마크다운 편집기들도 그렇습니다.
- **툴바에 코드 블록이 있는 에디터에서만**(`canCode`) — `tools`로 좁혀 둔
  자리에서 저절로 블록이 생기면 되돌릴 단추가 없습니다(`canBullet`과 같은 까닭).
- 안 걸리는 자리 둘: **코드 블록 안**(거기 백틱은 진짜 백틱입니다 — 가로채면
  블록 안에 블록이 생기고 파이썬 문자열에 백틱을 적을 길도 막힙니다) ·
  **줄 가운데**(`값은 a```). 판정은 **커서 앞 같은 줄의 글자가 정확히 '``'
  둘**일 때뿐이고, `linePrefix`를 그대로 나눠 씁니다.
- 넣는 것은 **`insertCodeBlock` 그 함수**입니다 — 툴바 단추와 한 곳을 쓰므로
  빈 블록의 공백 한 칸과 그 **앞**에 서는 커서까지 그대로 따라옵니다(둘이
  갈리면 단축키로 만든 블록에서만 첫 줄이 한 칸 들여쓰기됩니다).
- 앞의 백틱 둘은 `execCommand('delete')`로 지웁니다 — **Ctrl+Z 두 번이면
  '``'로 돌아옵니다**(실측: 한 번은 블록, 한 번은 지운 백틱). 진짜로 백틱을
  적고 싶을 때 빠져나갈 길입니다.
- 실측으로 확인한 자리: 줄 맨 앞 · Enter로 넘긴 둘째 줄 · Shift+Enter로 접은
  줄(`<br>` 뒤) · 글머리 기호 줄 맨 앞 — 넷 다 걸리고, 블록 안 · 줄 가운데 ·
  `` `x ``는 그대로 글자로 남습니다.

### 코드 블록은 한 번에 사라집니다

- 안을 다 지워도 **빈 검은 블록이 한 줄 남아** backspace를 한 번 더 눌러야
  했습니다. 마지막 글자를 지우는 그 backspace가 블록까지 함께 걷어 가야,
  지우는 사람 눈에 '다 지웠다'와 '사라졌다'가 한 동작이 됩니다
  (`preWillEmpty` → `dropPre`, 자리에는 빈 줄 하나를 둡니다).
- **판정은 글자 수가 아니라 `trim()` 뒤의 길이로.** 코드 블록은 만들 때
  빈칸 하나로 시작해서(`escapeHtml(...) || " "`), 글자 수로 세면 보이는 것을
  다 지운 뒤에도 공백 하나가 남아 **고치려던 그 증상이 그대로 납니다**
  (실측 — `" ab"`에서 두 번 지우면 `" "`가 남음). 커서만 있을 때는
  `text.trim().length <= 1`, 골라 놓고 지울 때는 고른 것이 블록의 전부인가.
- 블록 위아래의 글은 건드리지 않습니다 — 블록만 빈 줄로 바뀝니다.
- **되돌릴 때는 Ctrl+Z가 아니라 '```'입니다.** `dropPre`는 `replaceWith`로
  DOM을 직접 고치는데 그건 브라우저의 되돌리기 목록에 안 올라가, 블록이
  사라진 뒤 Ctrl+Z를 눌러도 안 돌아옵니다(실측). 고치려고 지우다 블록까지
  걷힌 학생은 그 자리에서 백틱 셋을 다시 치면 됩니다 — 위 단축키를 둔
  까닭의 절반이 이것입니다. **되돌리기로 살리려면** `dropPre`를 execCommand
  경로로 다시 짜야 하는데, 그러면 '빈 블록이 한 줄 남는' 원래 증상이 도로
  살아날 위험이 있어 지금은 안 건드립니다.

### 손들기 — 태그와 메모를 함께

- 손바닥 하나로는 '질문이 있다'까지만 전해집니다. 학생이 손바닥을 누르면
  **그 옆에 작은 창**(`.question-signal-ask`)이 열려 태그 하나와 짧은 메모를
  함께 보냅니다. 교사는 다가가기 전에 무엇인지 압니다.
- 태그는 셋으로 못 박습니다(`lib/questionTags.js`) — 선생님, 질문이요! ·
  이런 기능 어때요? · 으악! 오류 발견!. 고르는 데 시간이 들면 손드는 일
  자체가 부담이 되어서요. **저장에는 key만** 들어가므로 나중에 말을 다듬어도
  지난 기록이 따라옵니다.
- **셋은 같은 크기**입니다(한 줄 3칸 격자 `1fr`). 창을 400px로 잡아야 한 칸이
  121px이 되어 아홉 글자가 한 줄에 섭니다(340px이면 101px이라 잘립니다 —
  실측). 학생 창의 알약에는 **이모지를 안 답니다**(그만큼 글자가 잘립니다).
  교사 목록은 알약이 하나뿐이라 자리가 남아 이모지를 답니다.
  - `.question-signal-ask`의 폭은 `.question-signal-dropdown`(330px)과
    특이도가 같고 뒤에 오므로, **이름을 겹쳐 써서**(`.question-signal-dropdown.question-signal-ask`)
    이깁니다 — 순서에 기대지 않게.
- 단추 둘(**내용 없이 손들기 · 내용과 함께 손들기**)은 **한 줄에 반씩**입니다
  (`.modal-actions`와 같은 `grid-template-columns: 1fr 1fr` — `flex: 1 1 0`으로
  두면 테두리가 있는 `.btn-ghost`가 2px 넓어집니다). 둘 다 손을 드는 단추이고,
  적은 내용이 있는 쪽만 채운 색입니다. 창이 400px이라 한 칸이 184px이고 긴
  쪽도 한 줄에 섭니다(실측) — 말을 늘리면 다시 재 보세요.
- 이미 손을 들었으면 그 값이 채워진 채 열려 **고쳐 보내거나 내릴** 수
  있습니다(예전에는 누르는 즉시 취소였습니다).
- 교사 목록은 이름 줄 아래에 **태그 → 메모** 차례로 답니다. 🍎 확인 · 닫기는
  지금까지대로 이름 줄에 있습니다.
- **규칙에 길이를 못 박았습니다**(`questionSignalSaid` — tag 20자 · note
  1000자). 메모는 교사 화면에 그대로 뿌려지는 글이라서요. 둘 다 없어도 손은
  올라갑니다(그냥 부르는 손). 시험은 `tests/rules/questionSignals.test.mjs`.

### 자리표 '선생님 보기'
- **다섯 화면이 한 값을 함께 씁니다**(`lib/seatView.js`의 `useSeatView`,
  단추는 `components/SeatViewToggle.jsx`): '멋진 순간' 패널 · 수업 중 자리표
  (`LessonSeatPanel`) · 자리 배정 창(`SeatGroupSetupModal`) · 기록 관리의
  누가기록 탭(`ClassNotesManagerModal`) · **기록 관리의 수업 노트 탭**
  (`CornellNotesPanel` — 날짜 칸 오른쪽). 화면마다 따로 기억하면 한 곳에서 뒤집어 놓고
  옮겼을 때 방향이 달라, 같은 반의 자리표가 두 얼굴이 됩니다. 같은 탭 안의
  다른 패널도 곧바로 따라오도록 창 이벤트로 알립니다 — `storage` 이벤트는
  **다른 탭에만** 오기 때문입니다.
- 기본은 학생 보기(학생이 앉아 칠판을 보는 방향), 누르면 교탁에서 본 방향.
- **자리표는 그림을 돌리고(`.seat-flipped` / `.attend-seatmap-grid--flipped`),
  기록 관리의 카드 격자는 배열을 뒤집습니다.** 자리표는 빈 칸이 섞인 격자라
  배열을 뒤집으면 빈 칸이 엉뚱한 곳으로 가고 끌어 옮기기의 자리 번호가
  어긋나지만, 기록 관리 격자는 안쪽이 구르는 칸이라 그림을 돌리면 **스크롤이
  거꾸로** 됩니다(내려야 할 것을 올리게 됩니다).
  - **줄 차례만 뒤집고, 줄 안의 왼쪽→오른쪽은 그대로 둡니다**
    (`lib/seatView.js`의 `flipRoster` — 두 탭이 같은 함수를 씁니다).
    맞춰야 할 상대가 자리표이기 때문입니다: 교사는 자리표를 보며 이 격자에서
    같은 학생을 짚으므로 두 화면의 줄이 같은 차례로 서야 합니다.
  - **'배열을 통째로 뒤집기'도 '진짜 180도 회전'도 아닙니다.** 둘 다 줄 안의
    좌우가 뒤집혀 자리표와 **반대로** 섭니다 — 자리표에서 왼쪽에 선 학생이
    여기서는 오른쪽 끝에 있습니다(실제 신고, 두 번). 자리표가 그렇게 보이는
    까닭은 선생님이 **교탁에서 봤을 때 학번이 왼쪽→오른쪽으로 올라가도록**
    자리를 배정해 두었기 때문이고, 학번순인 이 격자도 같은 방향이어야 합니다.
  - 마지막 줄이 덜 차 있으면 **그 줄 뒤를 빈 칸으로 채웁니다** — 안 채우면
    그 인원이 다음 줄로 흘러 들어가 줄 묶음이 통째로 밀립니다.
    실측(22명·6칸): 선생님 보기의 둘째~넷째 줄이 자리표의 그 줄들과 이름
    하나까지 같습니다.
  - 빈 칸은 `null`로 와서 `.notes-mgr-gap`(`visibility: hidden`)이 됩니다.
    **테두리도 배경도 두지 마세요** — '아직 안 쓴 학생'의 흰 카드와 헷갈립니다.
  - 칸 수를 알아야 해서 `useGridCols(6, 3)`이 **CSS의 `@media`와 같은 값**을
    듭니다(`.notes-mgr-grid`가 768px 아래에서 3칸). 한쪽만 고치면 **좁은
    화면에서만** 줄이 어긋나 찾기 어렵습니다.
- **배열을 뒤집지 않고 그림을 180도 돌립니다**
  (`.attend-seatmap-grid--flipped { transform: rotate(180deg) }` + 칸마다 도로
  180도). 자리표는 빈 칸이 섞인 격자라 배열을 뒤집으면 빈 칸이 엉뚱한 곳으로
  가고, 끌어 옮기기가 쓰는 자리 번호(index)도 어긋납니다.
- **뒤집힌 자리표에서 칸에 transform을 새로 주면 '도로 180도'가 지워집니다.**
  같은 속성이라 더해지지 않고 덮어씁니다 — 손댄 칸만 거꾸로 뒤집히고,
  transform에 transition이 걸려 있어 도는 것처럼 보입니다(hover에서 실제로
  났던 일). 칸에 transform을 줄 때는 `.attend-seatmap-grid--flipped > …`로
  `rotate(180deg)`를 **함께** 적으세요. 돌아간 좌표계라 화면에서 '위로 1px'은
  `translateY(1px)`입니다.
- 패널과 '확대' 창이 **같은 방향**을 씁니다 — 한 자리표가 두 얼굴이면 안 됩니다.
  **단추도 두 곳 다 있습니다.** 확대 창은 방향만 따라가고 돌리는 단추가
  없어서, 교실에 띄워 놓고 보다가 방향을 바로잡으려면 창을 닫고 패널
  머리줄까지 갔다가 다시 열어야 했습니다 — 크게 띄운 그 순간이 방향을
  바꾸고 싶은 때입니다. 두 단추가 `teacherView` **하나**를 함께 쥐므로
  어느 쪽에서 돌려도 나머지가 같이 돌아갑니다.
  - 자리는 **손바닥 뱃지 바로 왼쪽**입니다(`.reward-zoom-lead`가 남는 폭을
    받아 오른쪽으로 붙임). 패널 머리줄(`.reward-seat-head-btns`)의 차례와
    같게 — 왼쪽 끝에 두면 같은 단추를 화면마다 다른 데서 찾게 됩니다.
  - 그 칸에 **`aria-hidden`을 다시 붙이지 마세요.** 뱃지를 밀어내기만 하던
    빈 칸이라 붙어 있던 것인데, 누를 수 있는 단추가 들어온 뒤로는 읽어 주는
    기기에서 그 단추가 통째로 사라집니다.
  - **손든 학생 자리 확인**(`QuestionSeatModal`)도 같은 값을 씁니다. 한때
    `flipped`를 아예 안 넘겨 늘 학생 보기였는데, 패널에서 선생님 보기로
    돌려 두고 그 창을 열면 같은 교실이 반대로 보였습니다 — **자리로 사람을
    찾으려고 여는 창이라** 방향이 어긋나면 그 창의 쓸모 자체가 없어집니다
    (손바닥 표시는 자리 칸 안에 있어 늘 그 학생과 함께 움직입니다 —
    어긋나던 것은 판을 어느 쪽에서 본 그림인가뿐입니다).

**'칠판' 표시는 교사 자리표에서 걷어 냈습니다.** 교실 앞쪽이 어디인지 알려
주던 띠인데, 같은 머리줄의 '학생 보기 / 선생님 보기' 단추가 이미 방향을 글자로
말합니다 — 한 줄에 같은 뜻이 둘이면 자리만 먹고 판이 그만큼 내려앉습니다.
방향을 바꿀 수 있게 된 뒤로는 띠를 위아래로 옮기는 일(학생 보기는 판 위,
선생님 보기는 판 아래)까지 따라붙어, 지킬 것이 하나 더 느는 값이기도 했습니다.

- **학생의 '자리 배치' 창(`MySeatModal`)에는 그대로 있습니다.** 거기엔 보기
  단추가 없어(학생은 늘 앉은 자리에서 봅니다) 칠판 표시가 방향을 말하는
  유일한 것입니다. `.attend-seatmap-board`를 쓰는 곳은 이제 거기 하나뿐입니다.
- 칠판이 서던 머리줄의 그 자리는 **빈 칸(`.attend-seatmap-gap`)이 지킵니다**
  — `flex: 1`로 남는 폭을 받아 손바닥 뱃지를 오른쪽 끝으로 밀어내던 칸이라,
  그냥 빼면 뱃지가 줄 가운데로 들어옵니다.
- 그러고 나면 **수업 중 자리표에서는 그 머리줄이 통째로 빕니다**(손바닥 뱃지도
  그 패널에서는 감춰 둡니다 — 제 머리줄이 이미 셉니다). 높이 0짜리 빈 줄의
  아래 여백만 남으므로 `.lesson-seat-panel`에서 줄째로 접습니다. **그 패널의
  자리표 머리줄에 설 것을 새로 넘긴다면 그 규칙을 함께 지우세요** — 안 그러면
  조용히 안 보입니다.
- 단추를 끼우는 자리가 둘입니다 — `headLead`(줄 맨 앞) ·
  **`headTrail`(손바닥 뱃지 바로 왼쪽)**. 보기 방향 단추는 늘 뒤쪽입니다.
  `headLead`는 '멋진 순간' 패널과 확대 창이 이미 쓰고 있습니다.

- 고른 방향은 localStorage(`reward_seat_view`)에 기억합니다. 한 선생님은 대개
  늘 같은 쪽에서 봅니다.
- 자리 배정 창(`.seat-slot`)은 **정사각형이 아니라 3:2**입니다. 정사각형이면
  한 줄 6칸 × 5줄이 세로로 넘쳐 창에 스크롤 막대가 생겼습니다(실측 944px →
  710px). 칸에 드는 것은 자리 번호·학번·이름 세 줄뿐이라 3:2면 넉넉합니다.
- **켜짐 색을 두지 않습니다.** 글자가 '선생님 보기'/'학생 보기'로 바뀌어 지금
  어느 쪽인지 그대로 말합니다. 색까지 얹으면 옆의 손바닥 뱃지처럼 '켜져 있으니
  봐 달라'는 신호로 읽혀 눈길을 뺏습니다.
- 그 왼쪽에 **'출석 21/25'**(`.reward-seat-att`) — **출석을 끝낸 뒤에만**
  띄웁니다(`presentUids`가 있고 `attendanceOpen`이 아닐 때). 받는 중에 띄우면
  아직 오는 중인 학생이 결석 수처럼 읽힙니다. 세는 대상은 **지금 명단에 있는
  학생**뿐입니다 — 반에서 빠진 학생의 옛 기록이 섞이면 분자가 분모를 넘습니다.
  단추가 아니라 알림이라 배경 없이 글자만 둡니다.

### 모둠 현황 — 명단에 없는 사람 지우기
- 모둠에 저장된 `members`는 **배정하던 때의 스냅샷**이라, 반에서 빠지거나
  지운 계정(샘플 계정 등)이 그대로 남습니다.
- 예전에는 `moveToGroup`이 맨 앞에서 `roster.find(...)`가 없으면 그냥
  돌아가, **그 칩을 눌러도 아무 일이 없었습니다** — 화면에서 지울 길이 아예
  없었습니다(실제로 '학생강현수'가 그랬습니다). 지금은 **빼는 것**
  (`targetIndex == null`)은 명단에 없어도 되고, **넣는 것**만 막습니다
  (이름·학번을 명단에서 가져와야 하므로).
- 그런 칩은 `.reward-chip.stale`(점선·옅은 글자)로 표시합니다. 명단을 아직
  받는 중일 수 있어 `roster.length > 0`일 때만 봅니다.
- **자동으로 걷어 내지 않습니다** — 명단을 받는 중(roster가 빈 순간)에
  저장되면 모둠이 통째로 비워집니다. 지우는 것은 선생님이 눌러서.
  ('반 관리하기 → 자리 배정하기' 모달은 열 때 명단으로 걸러 두어, 거기서
  저장해도 정리됩니다 — 다만 그 화면에서는 보이지 않아 '지워야 할 게 있다'는
  것을 알 수 없습니다.)

### 프로젝트 제목 고치기 — 카드의 '편집'

- 대시보드 프로젝트 카드 아래 줄(`.study-project-actions`)의 **편집**이
  `StudyProjectEditModal`을 엽니다 — **제목 · 활동 안내 · 연계**(키워드 ·
  파이썬 실행기)를 고칩니다.
- **연계는 원본에도 함께 적습니다**(`syncTemplateLinks`) — 활동 목록과 같은
  까닭으로, 다음에 불러오는 반부터 고친 대로 옵니다. 이미 나간 다른 반의
  복사본은 그대로입니다. 제목·안내는 반마다 달리 쓸 수 있어 원본에 안
  적습니다. 연계를 정하는 자리가 만들기 창뿐이라 만들 때 빠뜨리면 켤 길이
  없던 것을 풀었습니다. 키워드 목록은 페이지의 `keywordNames`를
  `StudyProjectDashboard` → 편집 창으로 내려 줍니다(읽기 0).
- **왜**: 제목을 고치는 길이 프로젝트 상세에서 **제목을 더블 클릭**하는 것
  뿐이었는데(`StudyProjectView`의 `startEditTitle`, 지금도 그대로 됩니다)
  화면 어디에도 그 사실이 없어 '제목은 못 고친다'로 보였습니다(실제 신고).
  활동 안내도 설정(⚙) 패널을 펴야 나와, 목록에서 고치려면 두 단계를 지나야
  했습니다.
- **카드 안에 둘 수 없습니다** — 카드 전체가 하나의 '열기' 버튼이라
  (`.study-project-open`) 그 안에 단추를 넣으면 버튼 안의 버튼입니다. 책방
  활동 카드와 **같은 모양**으로 여는 단추 **아래 제 줄**에 답니다
  (`.book-activity-actions`와 같은 값: 선으로 가르고 오른쪽 끝, 12px).
  - 그래서 `.study-project-card`를 **세로(`column`)**로 바꿨습니다. 여는
    단추가 `flex: 1`이라 그 줄이 없는 학생 카드는 지금까지와 같습니다.
- **활동 목록은 여기서 안 고칩니다.** 활동을 바꾸는 일은 '학생이 이미 쓴
  내용이 있는가'를 따져야 하고(`LessonMode`의 `saveBoardActs`) 잠금 배열
  (`activityLocks`)과 자리를 맞춰야 합니다 — 그 자리는 상세의 활동 패널과
  수업 모드에 이미 있습니다. 창에 어디서 고치는지 한 줄 적어 둡니다.
  유형(개별/모둠)·공개 범위도 마찬가지로 안 둡니다.
- 제목 길이는 상세의 인라인 수정과 **같은 40자**입니다 — 두 자리가 다르면
  한쪽에서 쓴 제목이 다른 쪽에서 잘립니다. 빈 제목은 저장을 막습니다(카드에
  이름이 없어지면 목록에서 무엇인지 알 수 없습니다).
- **규칙을 안 건드립니다** — `studyBoards`의 update가 `ownsClassEditable`만
  보고 필드 목록을 못 박아 두지 않습니다(확인함).
- **삭제도 이 창에서 합니다**(`.study-edit-danger` — 선 아래 왼쪽, 저장·취소와
  갈라 둠). 곧바로 지우지 않고 **휴지통으로** 보냅니다 — 상세 화면의
  '프로젝트 삭제'와 같은 `deleteStudyBoard`입니다. 되묻는 창이 휴지통 자리를
  밝히고, 원본에서 시작한 프로젝트면 '원본은 그대로 남는다'를 덧붙입니다.
  지운 뒤 휴지통을 펴 두었다면 곧바로 다시 읽어 보여 줍니다.

### 활동 열기 칩 (`.study-act-gate`)
- 칩은 **언제나 한 줄**입니다(접히면 활동 수·화면 폭에 따라 줄 수가 오락가락해
  아래 카드가 밀립니다). 넘칠 때만 옆으로 구릅니다.
- 다만 **남는 폭을 나눠 가지지는 않습니다**(`flex: 0 1 auto`). 예전에는
  `flex: 1 1 0`이라 활동이 하나뿐인 프로젝트에서 칩 하나가 줄을 통째로 늘여
  잡아, 알약이 아니라 넓은 띠로 보였습니다. 이름만큼만 차지하고, 줄이 모자랄
  때 비로소 서로 줄어들며 말줄임으로 접습니다.
- 이름이 아주 긴 활동 하나가 줄을 독차지하지 않게 `max-width: 240px`,
  다 줄어들어도 글자가 사라지지 않게 `min-width: 62px`(그 아래로는 스크롤).
- **'순서 바꾸기 · 모두 열기(1번만 남기기)'는 마지막 칩 바로 옆**입니다
  (`.study-act-gate-row` — 칩 줄과 단추 묶음을 한 flex 줄에). 한때 라벨 줄
  ('활동 열기 n/N')의 오른쪽 끝에 섰는데, 칩에서 멀리 떨어져 무엇에 하는
  일인지 이어 읽기 어려웠습니다. 단추 묶음은 `flex-shrink: 0`이라 칩이
  모자라면 **칩이 먼저 줄어들고**(말줄임), 그래도 넘치면 칩 줄만 옆으로
  구릅니다 — 단추는 늘 줄 끝에 붙어 보입니다.

### 출석 찍힌 시각 — 밀리초까지

- 화면 셋이 `09:03:07.048`까지 적습니다 — 출석 목록 표(날짜까지 함께
  `2026. 09. 22. 09:03:07.048`) · 자리표 칸(시각만 — 날짜는 머리줄이 이미
  말합니다) · 학생 본인의 출석 기록 목록.
- **저장은 안 건드렸습니다.** `markStudyAttendance`가 쓰는
  `serverTimestamp()`는 처음부터 `{seconds, nanoseconds}` 두 칸이라
  **1/1000초는 이미 들어 있었습니다.** 잘리던 곳은 둘입니다 —
  (ㄱ) `toDate()`가 주는 JS `Date`가 밀리초까지만 담아 **나노초는 거기서
  버려지고**(실측: `Timestamp(1758500096, 789123456)` → `.789`, 뒤 여섯
  자리 소실), (ㄴ) 화면이 시·분만 찍고 있었습니다. 그래서 규칙도 자료도
  그대로이고 **읽어서 그리는 자리만** 고쳤습니다.
- **셈은 `lib/dates.js`의 `formatClockMs`·`formatStampMs` 두 곳**입니다
  (store.js가 같은 이름으로 다시 내보냅니다).
  세 화면이 저마다 만들면 한 곳만 고쳤을 때 같은 기록이 화면마다 다른
  자릿수로 보입니다.
- **`Intl`의 `fractionalSecondDigits`를 쓰지 마세요.** ES2021 옵션이라
  모르는 기기에서는 **조용히 무시되어** 그 화면에서만 밀리초가 사라집니다.
  `String(d.getMilliseconds()).padStart(3, "0")`으로 손수 붙이면 어디서나
  같은 글자가 나옵니다.
- **값이 없으면 빈 문자열입니다** — `toDate(null)`은 '지금'을 돌려주는데,
  방금 찍은 기록은 서버가 답할 때까지 이 칸이 null이라(`serverTimestamp()`
  의 그 함정) 그대로 두면 **지어낸 밀리초**가 화면에 찍힙니다. 부르는 쪽은
  빈 값을 받아 `-`를 그리거나 아예 줄을 안 그립니다.
- 글꼴은 `--font-mono` + `font-variant-numeric: tabular-nums`입니다 —
  자리마다 숫자 폭이 다르면 표에서 밀리초 열이 들쭉날쭉해 세로로 훑을 때
  자릿수가 어긋나 보입니다. `white-space: nowrap`으로 한 줄에 붙듭니다.
- **`.study-attendance-table-wrap`에 `overflow-x: auto`가 필요합니다** —
  칸이 길어지며 좁은 화면에서 표가 그 테두리 밖으로 삐져나갔습니다(실측
  400px에서 48px). 지금은 그 안에서 옆으로 구릅니다(상단바 이동 메뉴와 같은
  방식 — 글자를 줄이는 대신 구르게).
- 실측: 자리표 칸 96~108px에 시각 83px이라 400·600·780·1280px 어디서도
  한 줄에 들어가고, 문서 가로 넘침은 0입니다.
- **엑셀 출석부에는 시각이 없습니다** — `lib/exportAttendance.js`는 명단 ×
  날짜 격자에 `○`만 찍습니다. 시각을 넣으려면 그 한눈에 보는 격자를 다시
  짜야 해서(칸마다 열한 글자) 일부러 두었습니다.

### 출석부 내려받기 (교사)
- '출석 관리' 모달 머리줄의 **⬇ 출석부 내려받기** — 명단 × 수업한 날짜의
  표 한 장을 엑셀로(`lib/exportAttendance.js`). 학기말에 학교 시스템으로
  옮겨 적거나 보관하려면 화면이 아니라 파일이 필요합니다.
- 누르면 **범위 고르기 창**이 먼저 뜹니다(전체 기간 / 하루 / 기간).
  예전에는 곧바로 학기 전체가 받아졌는데, 실제로는 '오늘 하루'나 '이번 달'로
  좁혀 뽑는 일이 더 잦아 받아 놓고 엑셀에서 열을 지워야 했습니다.
  - 날짜는 **자유 입력이 아니라 수업한 날 중에서** 고릅니다 — 달력에서 아무
    날이나 집으면 수업이 없던 날을 고르고도 '왜 비었지' 하게 됩니다.
  - 기간은 시작·끝이 뒤집혀 있어도 받아 줍니다(누르는 차례까지 강요하면
    고르다 막힙니다). 거르기는 `filterRecordsByRange` — 양 끝을 포함하고,
    ISO 날짜 문자열은 사전순이 곧 시간순이라 그대로 견줍니다.
  - 갈래를 바꿔도 창 높이가 안 흔들립니다(`.attend-export-when`의 min-height)
    — 고르는 도중에 단추가 움직이면 누르려던 곳이 어긋납니다.
  - 파일 이름에 실제로 담긴 범위를 적습니다(하루면 그 날짜, 여러 날이면
    `처음~끝`). 여러 개 받아 두었을 때 열지 않고 고르려면 필요합니다.
  - 출석률은 **고른 범위 안에서** 다시 셉니다 — '이 기간에 며칠 왔나'가
    그 표의 뜻이라서요.
- 열은 **기록이 있는 날짜만** 세웁니다. 달력의 모든 날을 세우면 수업이 없던
  날의 빈 칸이 결석과 구분되지 않습니다.
- 표 마지막 줄의 '출석 인원'은 **지금 명단에 있는 학생만** 셉니다 — 기록에는
  반에서 빠진 학생도 남아 있어, 그대로 세면 위 ○ 개수와 아래 합계가
  어긋납니다.
- 만드는 법은 활동 자료 내보내기와 같은 `downloadWorkbook`. 열이 날짜 수에
  따라 달라져 열 이름을 인자로 받습니다 — 열이 고정인
  `downloadStudyWorkbook`이 이것을 감싸 씁니다.
- 같은 줄에 서는 것들(단추 둘·상태 알약·내려받기·'출석 n / N')은 **높이를
  하나로**(`height: 30px`) 맞춥니다. 세로 padding으로 저마다 정하면 글자
  크기가 다른 만큼(13 · 12.5 · 12px) 줄이 들쭉날쭉해집니다.

### 엑셀 내보내기는 진짜 `.xlsx`입니다
- `lib/exportStudy.js`의 `downloadWorkbook`이 **OOXML 몇 장을 zip으로 묶어**
  만듭니다(`buildXlsxParts` + `lib/zip.js`). 라이브러리(SheetJS·JSZip)는
  여전히 안 씁니다.
- 예전에는 SpreadsheetML 2003(XML 한 장)을 `.xls` 이름으로 내려 주었는데,
  엑셀이 열 때마다 **'파일 형식 및 확장명이 일치하지 않습니다'** 경고를
  띄웠습니다 — 속은 XML인데 이름은 옛 이진 형식이라서입니다. 이름만
  `.xml`로 바꾸면 경고는 사라져도 더블클릭으로 안 열립니다.
- `lib/zip.js`는 **압축하지 않습니다**(store, method 0). deflate를 구현하거나
  비동기 `CompressionStream`을 쓸 만큼 파일이 크지 않습니다(표 한 장 = 수십 KB).
- 셀은 전부 `t="inlineStr"` — `sharedStrings.xml`을 두면 조금 작아지지만
  부품이 하나 늘고 어긋날 자리도 하나 늡니다.
- 열 이름은 `colName`이 만듭니다(0→A … 26→AA). **출석부는 날짜만큼 열이
  늘어 26칸을 쉽게 넘습니다** — Z 다음을 잘못 세면 엑셀이 파일을 통째로
  거부합니다.
- `xmlEscape`는 XML 1.0이 금지하는 제어문자도 걷어 냅니다. 학생 글에 섞여
  들어오면 '읽을 수 없는 파일'이 됩니다.

### 잠김 / 열림은 늘 자물쇠 한 쌍으로

- 그림은 `components/StatusIcons.jsx`의 **`IconLock`(닫힌 고리) ·
  `IconUnlock`(열린 고리 + 반짝임)** 둘뿐이고, 상태로 고르는 자리는
  **`IconLockState({ locked })`** 한 곳을 거칩니다. 두 화면이 서로 다른
  그림을 쓰는 일을 막으려고요.
- 이모지(🔒/🔓)는 **쓰지 않습니다.** 기기마다 굵기·색이 달라 교실 화면에
  띄우면 두 그림이 잘 갈리지 않았고, 고리가 열린 쪽이 오히려 작아 보였습니다.
- **색만으로 상태를 가르지 마세요.** 열린 칩(연한 살구)과 잠긴 칩(연한 회색)은
  둘 다 옅어 빔프로젝터에서 차이가 좁아집니다. 자물쇠를 함께 답니다.
- **누르면 상태가 바뀌는 것에는 그림, 할 일을 적은 단추에는 글자만.**
  칩·토글처럼 그 자리가 곧 상태인 것(수업 모드의 활동 열기, 공부방·곁텍스트
  칩)은 그림이 지금 상태를 말하고 툴팁이 할 일을 말합니다. 반대로 글자가
  이미 할 일인 단추(책방 활동의 `활동 시작하기`/`활동 잠그기`)에는
  **자물쇠를 달지 않습니다** — 상태 배지의 자물쇠와 방향이 어긋나, 잠긴 활동
  하나에 열린 자물쇠와 닫힌 자물쇠가 같이 뜹니다. 상태는 배지가, 할 일은 단추가.
- 그 단추의 말은 **`활동 시작하기` / `활동 잠그기`** 입니다. `잠금 해제`·
  `잠그기`만 적었을 때는 바로 옆 `편집`(이름·주제어 고치기) 단추와 섞여
  무엇을 잠그는 것인지 흐렸습니다. 여는 쪽이 '잠금 해제'가 아닌 까닭:
  선생님이 그 단추를 누르는 순간은 자물쇠를 푸는 때가 아니라 **이 활동을
  시키는 때**이고, 잠갔다 다시 여는 것도 그 반에게는 새로 시작하는 일입니다.
- **상태 배지는 한쪽 상태에만 달지 마세요.** 잠겼을 때만 배지를 두면 배지
  없는 카드가 '열려 있다'가 아니라 '아직 안 봤다'로도 읽힙니다. 책방 활동
  카드(`.book-activity-state`)는 늘 달고, 말과 색은 진행 대시보드의 같은
  알약(`.progress-act-state`)과 맞춥니다 — **잠김 / 열림**.
  ('편집'이었는데, 옆에 선 `편집` 단추(이름 고치기)와 같은 낱말이라 상태를
  말하는 자리인지 할 일을 말하는 자리인지 헷갈렸습니다. 말을 바꿀 때는
  **두 화면을 함께** — 한쪽만 고치면 같은 상태가 두 이름을 갖습니다.)
- 두 아이콘 다 **밝은 바탕에 맞춘 두 색**(먹빛 `#3A312E`, 무늬 `#8A6258`,
  몸통 `#FFF7ED`)입니다. 어두운 패널 위에서는 `mono` 옵션으로 두 색을
  글자색으로 갈음하고 몸통을 비웁니다(`.study-activity-lock` 한 곳).
- 지금 붙어 있는 자리: 공부방 '활동 열기' 칩 · 곁텍스트 '단계 열기' 칩
  (`.section-gate-lock`) · 수업 모드 내보내기 칩 안의 자물쇠 · 활동 설정의 잠금 토글 ·
  진행 대시보드의 `잠김/열림` 알약 · 책방 활동 카드의 상태 배지 ·
  동료 평가 여닫기 · '보기 전용' 배지 · 활동 칸의 '잠김' · 실명 공개 배지.
- `StudyProgressBoard`의 잔디 범례만 자물쇠가 아니라 **대각선 무늬**입니다 —
  거기서 갈라야 하는 것은 상태가 아니라 칸 색이고, 이미 모양으로 구분됩니다.

### 저장된 글의 서식은 `.study-card-content`에도 걸어야 합니다

- 코드 블록(어두운 `pre`)과 글머리 기호 들여쓰기(`ul`/`ol`)는 한 규칙에
  **선택자를 늘어놓아** 정합니다(globals.css 4410줄 언저리). 여기에
  `.rte-area`(에디터) · `.qa-content` · `.chat-bubble` · `.notice-item`만
  들어 있고 **공부방 카드가 빠져 있었습니다** — 에디터에서는 검은 코드
  블록이던 것이 저장하고 보면 밋밋한 글이 되고 목록도 들여쓰기가 풀려,
  '활동 칸에는 서식이 안 먹는다'로 보였습니다(실제 신고).
- 지금은 `.study-card-content`(활동 칸·미리보기·모아보기·카드 모달)와
  `.study-card-body`(발표 모드·질문 엿보기)도 함께 있습니다. 카드 본문에만
  따로 있던 옅은 회색 `pre`는 걷어 냈습니다 — 같은 글이 화면마다 다른
  코드 블록으로 보이면 안 됩니다.
- **글이 뿌려지는 자리를 새로 만들면 이 두 규칙에 선택자를 더하세요.**
  안 그러면 그 화면에서만 서식이 풀립니다.

### 스크롤 칸 안의 `.sr-only`는 위치 기준을 함께 줘야 합니다

- `.sr-only`는 `position: absolute`입니다. **조상 중에 위치 기준(`relative`)이
  하나도 없으면 기준이 문서 전체가 되어**, 그 요소를 품은 칸의 `overflow`에
  잘리지 않고 문서 바깥으로 삐져나갑니다. 눈에는 안 보이는데 **문서 높이만
  늘어나** 페이지에 세로 스크롤바가 하나 더 생깁니다.
- 실제로 겪은 것: `/admin`의 과일 지급 격자(`.crtrend-scroll`) 안
  `<caption class="sr-only">`이 학생 26명짜리 표에서 **851px**을 넘겨,
  화면 높이에 못 박아 둔 대시보드(`.admin-shell`, 100vh)에 페이지 스크롤바가
  생겼습니다. 교사 화면에 막대가 셋(카드 안 · 본문 · 페이지)으로 보였습니다.
- **찾는 법**은 '가장 아래까지 뻗은 요소'를 짚는 것입니다. 껍데기 높이가
  화면과 같은데 문서가 넘치면, 넘치는 것은 껍데기 **바깥**에 있습니다.

  ```js
  let m=0,w=null;for(const e of document.querySelectorAll('body *')){
    const b=e.getBoundingClientRect().bottom+scrollY; if(b>m){m=b;w=e} }
  console.log(Math.round(m), w.tagName, w.className)
  ```
- 고치는 법은 **감싼 칸에 `position: relative`** 한 줄입니다. 껍데기에
  `overflow: hidden`을 둘러 덮지 마세요 — 증상만 가리고 진짜 넘치는 것은
  그대로 남습니다.

### 안내·예시 글자는 `--text-faint`

글자 짙기는 세 단계입니다 — 본문 `--text` · 딸림글 `--text-sub` · **안내·예시
`--text-faint`(#a09f96)**. 안내는 읽을 것이 아니라 참고할 것이라 살짝 보이면
됩니다. `--text-faint`가 바닥입니다(`--bg-deep` 위에서 대비 2.4:1).

- **입력칸의 예시 글(placeholder)은 `globals.css` 맨 위 한 곳**에서 정합니다
  (`input::placeholder, textarea::placeholder`). 정해 두지 않으면 브라우저
  기본색(크롬 #757575쯤)이라 본문만큼 짙어, 학생이 '이미 쓰여 있는 글'로
  읽습니다. `opacity: 1`을 함께 두세요 — 파이어폭스는 기본 opacity가 0.54라
  색만 정하면 한 번 더 옅어집니다.
  어두운 패널의 입력칸(`.kw-edit-input` · `.study-activity-row-input` ·
  `.kwl-textarea` · `.mm-node-input`)은 제 규칙이 아래에 있어 그것이 이깁니다 —
  **이 규칙을 그 아래로 옮기지 마세요.**
- **쓰기 시작하면 안내가 사라져야 합니다.** placeholder는 저절로 그렇고,
  칸마다 붙는 힌트(`.paratext-hint`)는 CSS로 그렇게 했습니다
  (`.raft-col.filled` · `.kwls-col.filled`에서 `display: none`). 자기 글과
  예시가 나란히 서면 어느 것이 자기 글인지 헷갈립니다.
- **RAFT 안내 문장**(`.raft-sentence`)은 빈칸이 남은 동안은 '채워 갈 틀'이라
  가장 옅고, 네 칸을 다 정하면 그때 비로소 **학생이 만든 문장**이 되어
  한 단계 또렷해집니다(굵게까지는 안 합니다 — 제목처럼 읽힙니다).
  교사 화면(`RaftBoard`)이 늘 `done`으로 그려, 한 칸도 안 쓴 카드에도
  '다 정했다'는 짙은 알림줄이 떴습니다. **판정을 학생 화면과 같게 두세요.**

### 개별 / 모둠도 한 쌍

- `IconIndividual`(한 사람 + 이름표) · `IconGroup`(나란히 선 두 사람) —
  **세는 것은 사람 수 하나뿐입니다.** 한때 모둠이 '가운데 한 사람 + 좌우로
  반쯤 걸친 두 사람'이었는데, 배지에 붙는 15px로 줄면 옆의 둘이 가운데
  몸통에 붙어 덩어리 하나로 뭉쳤습니다. 지금은 둘이 떨어져 섭니다.
  오른쪽 사람만 **초록(`#3F704B`)** 인데, 이 앱에서 이 색은 여기뿐입니다 —
  둘 다 먹빛이면 작은 크기에서 한 사람처럼 읽힙니다. 이모지(🧑‍🎓/👥)를
  쓰던 자리인데 기기마다 사람·피부색·머리 모양이 제각각이었고, 무엇보다
  프로젝트 만들기 창에서 두 선택 단추가 나란히 서서 **하나는 선 그림,
  하나는 컬러 이모지**로 갈렸습니다.
- 붙어 있는 자리: 프로젝트 배지(대시보드·상세 둘) · 프로젝트 만들기의 활동
  유형 두 단추 · 모둠 줄(`GroupMatesRow`·`GroupJoinRow`) · 모둠 짜기 창 제목 ·
  **모둠 카드의 아바타**(학생 카드·카드 모달·발표 모드·중계 화면 — 교사 카드가
  `IconTeacher`를 쓰는 그 자리입니다).
- `KwlPanel`의 '👥 학생 W 모음'만 이모지 그대로입니다 — 거기서 가리키는 것은
  모둠이 아니라 **반 전체 학생**이라 뜻이 다릅니다.

### 상태 알약의 크기는 한 곳에서

- 프로젝트 배지(`.study-project-badge` — 개별·모둠·활동 n개·함께 보기·보기
  전용) · 책방 활동 카드의 상태 배지(`.book-activity-state`) · 진행 대시보드의
  잠김/열림(`.progress-act-state`) 셋은 **같은 것을 말하는 알약**이라,
  글자 크기·여백·모서리를 `globals.css` 위쪽 한 규칙에서 함께 정합니다
  (색만 저마다의 규칙에 남습니다). 한쪽만 키우면 같은 뜻이 화면마다 다른
  크기로 보입니다.
- **13px / padding 4px 11px / 높이 28px.** 11.5px였는데, 카드에서 가장 먼저
  훑는 줄인데도 카드 안에서 제일 작은 글자였습니다. 글자만 키우면 알약이
  글자에 끼어 답답해지니 여백도 함께 키우세요. 안에 드는 아이콘은 15px.

### 모달 아래 두 단추는 늘 균등 두 칸

- `.modal-actions`는 `grid-template-columns: 1fr 1fr` — 창 폭이 얼마든 취소와
  실행이 **같은 크기로 아래를 채웁니다**. `flex: 1 1 0`으로 두면 테두리가
  있는 `.btn-ghost`가 없는 `.btn-primary`보다 2px 넓어집니다(flex-basis를
  content box로 재기 때문 — 실측 190 : 188). 격자는 정확히 반씩 나눕니다.
- **900px짜리 '독서 활동 만들기' 창도 그대로 균등입니다.** 취소가 417px라
  두 글자에 비해 넓어 보여 '긴 쪽 글자에 맞춰 오른쪽에 붙이자'고 제안했는데,
  선생님이 균등을 고르셨습니다 — 좌우 대칭이 안정적이고, 오른쪽 단추 문구가
  모둠 설정에 따라 길어지므로(`기본 모둠 5개로 만들기`) 글자가 잘릴 걱정이
  없습니다. **이 창만 예외를 두지 마세요.**

### 과일 주기 — 주는 단추의 과일은 '다음에 붙을 과일'

- 나열은 `RewardFruits`의 **`FRUITS` 열 가지 고정 순서**입니다(🍎🍊🍋🍇🍓🍑🍈🍉🍒🥝).
- 주는 단추가 늘 🍎였더니, 화면에는 열 가지가 줄지어 있는데 누르는 자리만
  사과라 '사과를 준다'로 읽혔습니다. 지금은 **`nextFruit(count)`**(다음에
  붙을 것) · **`lastFruit(count)`**(빼면 없어질 것)로 그립니다.
- **셈은 `RewardFruits.jsx` 한 곳에서만.** 나열이 i번째를 `FRUITS[i % 10]`으로
  그리므로 count개를 가진 사람의 다음은 `FRUITS[count % 10]`이고, 별로 접히는
  20개 주기와도 어긋나지 않습니다(`(count % 20) % 10 === count % 10`).
  다른 데서 따로 세면 단추와 줄이 다른 과일을 가리킵니다.
- **이름표 자리의 과일은 🍊입니다** — `누적 🍊 12` · `🍊 멋진 순간` ·
  `🍊 확인` · 축포의 `🍊 +1`처럼 특정 과일이 아니라 '과일'이라는 말을
  대표하는 자리입니다. **`FRUITS`의 차례(🍎부터)는 건드리지 마세요** —
  거기를 돌리면 이미 과일을 받은 학생의 뱃지 줄이 통째로 한 칸씩 밀려,
  어제까지 사과였던 것이 오늘 오렌지로 보입니다. 그래서 대표 그림만
  오렌지로 바꾸고 나열은 그대로 두었습니다. `RewardFruits.jsx` 말고 다른
  곳에 🍎를 새로 적지 마세요(이름표는 🍊, 주는 단추는 `nextFruit`).
- `RewardTally`도 그대로입니다. 이 패널은 **두 곳에 뜨고 이름이 다릅니다** —
  모둠 현황 패널의 접이 제목은 `🍎 궁금한 순간`, 수업 중 자리표의 탭
  이름표(`LessonSeatPanel`의 `TABS`)는 이모지 없는 `궁금한 순간`입니다
  (`embedded`일 때는 제목 줄을 안 그리고 탭이 그 자리를 대신합니다).
  한 이름으로 맞추자고 제안했지만 **지금대로 두기로 했습니다** — 둘이
  나란히 보이는 화면이 없고(하나는 공부방 사이드, 하나는 수업 중 오버레이),
  아래 막대가 세는 것은 실제로 `rewards` 누적 총계라 🍎가 내용과 어긋나지도
  않습니다. 한쪽만 고치지 마세요. 바꾼다면 두 곳을 함께.
### 팝오버 — 모달과 가려 쓰세요

- **모달**은 뒤를 덮고 다른 조작을 막습니다(`.modal-backdrop`). 닫기 전에는
  못 지나가므로 화면 한가운데 섭니다. **팝오버**는 누른 것 **옆에 붙어** 뜨고
  뒤가 그대로 살아 있어, 바깥을 누르거나 Esc·×로 닫힙니다. 툴팁은 올려놓기만
  해도 뜨는 글자뿐인 것, 드롭다운은 그중 '고르는 목록'입니다.
- 지금 팝오버인 것: 손들기 목록(`.question-signal-dropdown`) · 프로필 메뉴
  (`.profile-menu`) · **궁금한 순간의 이력**(`.tally-pop`) ·
  **자리표의 과일 주기·누가기록**(`.attend-tools-pop`).
- **자리 잡기와 닫기는 `lib/popover.js` 한 곳**에 있습니다
  (`usePopoverAnchor` · `usePopoverDismiss`). 화면마다 따로 적으면 어떤
  팝오버는 화면 밖으로 나가고 어떤 것은 Esc가 안 듣습니다.
- **구르는 목록 안의 팝오버는 `position: fixed`로 띄우세요.** 줄 안에 절대
  배치로 넣으면 목록의 `overflow`에 잘립니다(`.reward-tally-list`는
  `max-height: 320px`). 자리는 누른 것의 `getBoundingClientRect()`로 재서
  인라인으로 줍니다 — 오른쪽 → 왼쪽 → (좌우가 다 좁으면) 아래 → 위, 어느
  쪽이든 화면 안으로 끌어당깁니다.
- **누른 것을 따라다닙니다**(스크롤·창 크기). 열 때 한 번만 재면 뒤가
  구르는 동안 창만 제자리에 남아 엉뚱한 것을 가리킵니다. **구를 때 닫는
  방법을 쓰지 마세요** — 화면 가장자리의 것을 누르면 브라우저가 그것을
  화면 안으로 밀어 넣느라 클릭 **직후에** 스크롤이 한 번 일어나(실측:
  pointerdown 30ms 뒤) 누르자마자 닫히는 것처럼 보입니다.
- 바깥 클릭으로 닫을 때는 **`if (!e.target.isConnected) return;`**을 함께
  두세요(아래 '바깥을 눌러 닫기' 절). 그리고 **같은 목록의 다른 줄, 자리표의
  다른 자리를 누른 것은 '바깥'이 아닙니다** — 그리로 옮겨 가는 중이라,
  걸러 두지 않으면 닫혔다 다시 열리며 한 번 깜빡입니다(`keep` 인자).
- **자리표의 과일 창**(`StudentToolsPopover`)은 네 화면이 함께 씁니다 —
  참여 전광판 · 손들기 자리 확인 · 수업 중 자리표 · '멋진 순간'. 모달이던
  것을 옮긴 까닭은, 과일은 여러 학생에게 잇달아 주는 것인데 그때마다
  자리표가 통째로 가려져 **다음에 누를 자리가 화면에서 사라졌기** 때문입니다.
  - 자리 칸은 누른 요소를 함께 넘깁니다(`onPick(student, e.currentTarget)`ㆍ
    `onOpenTools(d, e.currentTarget)`) — 창이 어디에 설지 정하는 값입니다.
    **자리표를 새로 만들면 이것을 빠뜨리지 마세요**(안 넘기면 화면
    한가운데 섭니다).
  - '선생님 보기'로 자리표가 180도 돌아 있어도 그대로 맞습니다 —
    `getBoundingClientRect()`는 변형이 적용된 뒤의 자리를 돌려줍니다.
  - z-index는 `320`입니다 — 참여 전광판·손들기 자리 확인이 **모달 안**이라
    그 위로 올려야 합니다.

- 붙어 있는 자리: 자리표 과일 창의 ±1 · 카드 위 과일 단추 · 발표 모드의
  '과일 주기' · **활동 칸 머리의 과일 단추**(`StudyMyActivityCard`) ·
  **수업 노트 피드백 줄**(`CornellNoteReadModal`).
- **수업 노트 피드백 줄의 과일 단추**는 '선생님 한 마디' 이름표 오른쪽 끝
  입니다. 잘 쓴 노트를 보고 격려하려면 창을 닫고 자리표나 카드 격자로
  건너가야 했습니다. 읽는 문서는 **한 건**입니다
  (`subscribeMyClassRewardCount` — 이름이 `My…`지만 uid를 받는 함수라 교사가
  학생 것을 보는 데도 그대로 씁니다). 반 전체 `rewards`를 구독하면 노트 하나
  열 때마다 스물몇 건이 따라옵니다.
  - 이름표 줄은 `align-items: flex-start`입니다 — 왼쪽이 두 줄(제목 +
    딸림글)이라 가운데로 맞추면 과일이 두 줄 한가운데에 떠 제목과 어긋납니다.
  - **그 노트에 한 번 줬으면 단추가 사라집니다**(`awarded` →
    `.cornell-read-given`, 초록 알약 `🍊 줬어요`). 이 창은 한 학생의 노트를
    날짜로 넘겨 가며 스물몇 장을 읽는 자리라, 단추가 늘 같은 모습으로 서
    있으면 **'이 노트에 줬던가?'를 교사가 기억해야 했습니다**(실제 신고).
    - **판정은 `isCornellRewarded` — 카드 색과 같은 함수 하나**입니다.
      노트 문서에 `rewardedAt` 도장이 있나, 그것뿐이고 **날짜는 안 봅니다.**
      - **이 단추가 막는 것은 *그 노트에 대한* 중복 지급**이지 그 학생에
        대한 지급이 아닙니다. 자리표·발표 모드·손들기의 '🍎 확인'·
        '🍊 다 함께'는 그대로 열려 있고, 그쪽으로 준 과일은 **도장을 안
        남깁니다** — 노트와 무관한 지급이라서요.
      - **한때 '오늘 줬나'로 갈라 뒀습니다**(`isCornellRewardedToday`).
        그러면 카드 색과 단추가 서로 다른 물음에 답해 화면을 설명하는
        문장이 늘 둘이었고, **자정을 넘기는 순간 어제 준 노트의 단추가
        되살아났습니다**(창을 열어 둔 채로도). 지금은 함수가 아예 없습니다 —
        되살리지 마세요.
      - **되돌릴 길이 없습니다.** 도장을 지우는 함수가 앱에 없어(규칙도 교사가
        쓰는 것만 열려 있습니다), 잘못 누르면 그 노트에는 이 창에서 다시 못
        줍니다 — 과일은 자리표에서 −1로 뺄 수 있지만 도장은 남아 **카드는
        초록인데 실제 과일은 안 준** 상태가 됩니다. 그래서 단추 툴팁이
        누르기 전에 **'이 노트에는 한 번만 줄 수 있어요'**를 적습니다.
    - **방금 준 노트는 따로 듭니다**(`justAwarded` — 노트 id 집합).
      `rewardedAt`은 `serverTimestamp()`라 서버가 답하기 전까지 화면에 null로
      와서(휴지통 `deletedAt`의 그 함정), 그것만 보면 누른 뒤에도 단추가 한
      박자 더 남아 **한 번 더 눌립니다.** 도장 쓰기가 실패해도 여기에는
      담습니다 — 과일은 이미 나갔으므로.
    - **비워 두지 않고 표시를 남깁니다.** 단추만 걷으면 '줬다'와 '여기엔
      원래 단추가 없다'가 같은 모양이라, 헷갈리던 것이 그대로 남습니다.
      색은 기록 관리 수업 노트 탭의 '과일 준 카드'(`.notes-mgr-card.awarded`)와
      **같은 값**(#e8f7ed / #a6d8b6) — 같은 사실을 말하는 두 화면입니다.
    - 같은 노트에 한 번 더 주려면 자리표에서 그 자리를 누릅니다(낱개
      지급은 출석·노트와 무관하게 늘 열려 있습니다).
    - 실측: 안 준 노트에 단추 있음 · **사흘 전에 준 노트는 단추 없음** ·
      오늘 이미 준 노트도 처음부터 표시 → 누르면 **서버 시각이 오기 전에**
      표시로 바뀌고 시각이 와도 그대로 · 날짜를 오갔다 돌아와도 그대로 ·
      지급 호출 1회 · 가로 넘침 0.
- **활동 칸 머리의 과일 단추**는 활동 번호 바로 오른쪽입니다(교사가 남의
  카드를 볼 때만). 활동마다 서지만 **주는 대상은 카드 주인 한 사람**이라
  어느 칸에서 눌러도 같은 일입니다 — 활동 2의 답을 읽다가 그 자리에서 바로
  줄 수 있게 하려는 것입니다. 값도 길도 카드 격자의 단추와 같습니다
  (`classRoster`의 누적 개수 → `onAward(uid, 개수, +1)` 델타).
  - 번호와 단추는 **한 덩이로 묶습니다**(`.study-mycard-col-no`). 그 줄이
    `space-between`이라 그냥 나란히 두면 넷이 고르게 흩어져 '0/10자'가
    가운데를 벗어나고 '발표 모드'까지 자리가 밀립니다.
- **자리 칸의 🍎 뱃지(오늘치)와 창의 '누적'(총계)은 출처가 다릅니다** —
  뱃지는 `rewardEvents`(`useTodayRewardCounts`), 누적은 `rewards`
  (`roster[].count`). 자리표를 띄우는 페이지가 **`rewards`도 함께 구독해
  `roster`에 `count`를 실어 줘야** 합니다. 책방 페이지가 이것을 안 받고
  있어서, 뱃지는 4인데 창은 '누적 0'으로 보이고 **'−1'이 영영 꺼져
  있었습니다**(0개면 뺄 것이 없다고 판단하므로). '가득 찼어요' 판정도 같이
  어긋납니다. 자리표를 새 화면에 달 때는 이 한 가지를 꼭 확인하세요.
- **축포(`RewardCelebration`)는 `TopNav`에 한 번만 답니다.** 한때 공부방·책방
  페이지가 저마다 달고 있었는데, 과일을 주는 자리는 그 두 화면뿐이 아닙니다 —
  상단바 손바닥의 '🍎 확인'은 어느 화면에서든 눌리고 학생도 질문방·리포트에
  있을 수 있어, 받고도 아무 일 없는 일이 잦았습니다(손들기에 태그·메모가
  붙으면서 이 길로 주는 일이 늘어 드러났습니다). 상단바는 다섯 화면에 모두
  떠 있어 여기 하나면 어디서 받아도 터집니다. **페이지에 다시 달지 마세요** —
  구독이 둘이 되어 축포가 겹칩니다. 반은 `activeClassId`(옆 과일 뱃지와 같은
  값)를 봅니다.
- **연달아 주면 폭죽이 이어서 터집니다.** 교사는 한 학생에게 ＋1을 서너 번
  누르는데, 예전에는 **첫 지급에만** 폭죽이 돌고 그 뒤로는 뱃지 숫자만
  올라갔습니다 — 캔버스 effect가 `[active]`(개수가 0인가 아닌가) 하나만 보고
  있어 처음 한 번만 실행됐고, 프레임 루프도 `start + LIFE_MS`에 스스로
  멈췄습니다. 2초 간격으로 다섯 번 주면 3.2초 뒤부터 **밤하늘도 불티도 없이
  뱃지만 떠 있는** 화면을 7초 더 봤습니다(실제 신고).
  - 지금은 루프를 계속 돌려 두고 지급할 때마다 **발사만 덧붙입니다**
    (`launch`). 끝나는 시각도 `endAt`(마지막 발사 + `LIFE_MS`)로 밀려,
    캔버스·밤하늘·뱃지·사라지는 타이머가 한 시각에 맞습니다.
  - **'지급마다 처음부터 다시'로 짜지 마세요.** effect를 `[amount]`로 다시
    걸면 정리(cleanup)가 돌며 `resize()`가 `canvas.width`를 다시 넣어 **화면이
    통째로 지워지고**, 새 불씨가 바닥에서 올라오는 0.5초 동안 아무것도
    없습니다 — 빨리 누를수록 터지는 장면이 오히려 줄어듭니다.
  - 캔버스 effect가 **발사 effect보다 먼저 선언돼 있어야** 합니다. React는 같은
    렌더에서 선언 순서대로 실행하므로, 뒤에 두면 첫 지급 때 `launchRef`가 아직
    비어 있어 아무것도 안 터집니다.
  - **밤하늘은 key로 다시 마운트하지 마세요** — 들어오는 키프레임이 다시 돌아
    지급할 때마다 어둠이 0.26초씩 깜빡입니다. 들어오는 것만 키프레임이고 걷는
    것은 `.is-out` 클래스라, 늦게 온 지급에는 다시 짙어집니다. 그 0.64초는
    컴포넌트의 `FADE_MS`와 **같은 값이어야** 합니다.
  - 발수는 첫 발사만 예전 그대로(`9 + n*2`, 최대 16)이고 이어지는 발사는 작게
    잡습니다(`5 + n*2`, 최대 10). 한 발이 불티를 60~116개 만들어, 올라가는 중인
    발에 천장(`MAX_LIVE_SHELLS` 22)을 둡니다 — 천장에 닿아도 **한 발은 반드시**
    쏩니다(누른 만큼 무언가는 터져야 하므로).
  - 실측(1280×800, 1초 간격 다섯 번): 0.7초부터 6.9초까지 캔버스가 **한 번도
    비지 않고**(가장 긴 빈 구간 104ms, 그마저 다 사그라드는 끝자락), 밤하늘이
    6.9초에 걷히기 시작해 7.5초에 사라짐 — 마지막 지급(4.4초) + 3.2초와 맞음.

### '🍊 다 함께' — 출석한 학생 모두에게 한 개씩 (교사)

- '멋진 순간' 패널 자리표 머리줄, **'선생님 보기' 왼쪽**의 단추. 누르면 되묻지
  않고 바로 **오늘 출석한 학생 전원**에게 과일이 하나씩 가고(학생 화면에서는
  저마다 폭죽이 터집니다), 끝나면 **결과 창**이 뜹니다. 반이 통째로 잘한
  순간에 자리를 스물몇 번 누르던 것을 한 번으로 줄이는 자리입니다.
- **주는 대상은 '오늘 출석을 찍은 학생'입니다**(`awardTargets`). 보는 것은
  **'오늘 찍힌 기록이 있는가' 하나뿐** — 한 명이라도 있으면 그 사람들 모두에게
  하나씩 가고, 한 명도 없으면 단추가 꺼집니다.
  - **출석을 시작했나·끝냈나는 안 봅니다.** 받는 중에도 이미 온 학생들에게
    줄 수 있습니다(그때는 지금 찍힌 사람까지만 갑니다 — 툴팁의 인원수가
    그것을 말합니다).
  - **머리줄의 '출석 n/N'과는 기준이 다릅니다.** 그 라벨은 출석을 **끝낸
    뒤에만** 뜨는데(받는 중에 띄우면 오는 중인 학생이 결석으로 읽힙니다),
    단추는 받는 중에도 삽니다. 한때 둘을 같은 배열에서 뽑았고, 그 탓에 단추가
    라벨의 사정을 물려받아 죽었습니다 — **다시 묶지 마세요.**
  - **반 전체로 넘어가지 않습니다.** 출석 기록이 없는 날 명단 전원에게 주는
    길을 한 번 뒀다가 걷었습니다 — 결석한 학생까지 받게 되고, 같은 단추가
    날에 따라 다른 사람들에게 가서 '누구에게 주는 단추인가'가 흐려집니다.
  - **꺼져 있을 때는 무엇을 하면 켜지는지까지 적습니다**(`awardAllBlocked` →
    툴팁). `오늘 출석한 학생이 아직 없어요 — 학생이 '출석하기'를 누르면
    켜집니다`. 회색 알약만 놓아 두면 고장으로 보입니다: **'출석 종료'와
    '출석 기록 있음'은 다른 것**이라, 출석을 시작했다 아무도 안 찍은 채 끝낸
    날에도 꺼지는데 그때 '종료했는데 왜 꺼져 있냐'는 신고를 받았습니다.
  - 켜져 있을 때의 툴팁은 `awardWho`가 짓습니다(`오늘 출석한 21명에게 과일을
    하나씩 줍니다`) — 되묻는 창이 없으므로 **누르기 전에 인원을 말해 주는
    것은 이 한 줄뿐**입니다.
- **반드시 델타로 보냅니다** — `onAward(uid, 개수, +1)`. 화면에 보이는 개수로
  절대값을 만들면 구독으로 돌아오기 전의 옛 값이라 방금 다른 데서 받은 과일이
  묻히는데(위 '＋1·−1 단추' 절), 여기는 한 번에 스물몇 명이라 그 위험이 훨씬
  큽니다. 낱개 주기와 **같은 함수**를 그대로 되풀이해 부릅니다.
- **여섯 줄로 나눠 보냅니다**(`AWARD_ALL_LANES`). 학생마다 다른 문서라
  트랜잭션끼리 부딪히지는 않지만 스물몇 건을 한 순간에 던지지는 않고, 한 건씩
  차례로 가기에는 느립니다(한 반이 4초쯤). 여섯이면 네 번에 끝나 학생 화면의
  폭죽이 거의 동시에 터집니다. `queue.shift()`가 `await` **앞에서** 끝나므로
  같은 학생을 두 번 집는 일이 없습니다(실측: 26·60명에서 호출 수 = 인원 수,
  빠진 사람 0, 동시 실행 최대 6).
- **되묻는 창이 없습니다 — 누르면 바로 나갑니다.** 수업 중에 한 단계가
  거추장스럽다는 판단입니다(선생님 요청). 그 대신 둘을 함께 두었습니다.
  - **더블클릭은 ref로 막습니다**(`awardBusyRef`). 한 번의 실수가 곧
    스물몇 건의 쓰기인데, `awardingAll`은 상태라 같은 프레임의 두 번째
    클릭이 아직 옛 값(0)을 보고 지나갑니다.
  - 되돌리는 길은 자리를 하나씩 눌러 −1 하는 것뿐입니다. 창을 되살리려면
    `ConfirmModal`을 다시 부르면 되는데, 그때는 붉은 동그라미(`#fef2f2`)가
    '되돌릴 수 없다'는 뜻으로 읽히므로 **살구빛으로 바꿀 길을 함께** 내야
    합니다(한때 `iconTone` 프롭을 뒀다가 창과 함께 걷었습니다).

**끝나면 결과 창이 뜹니다**(`components/AwardAllResultModal.jsx`).
되묻는 창을 걷어 내 **무슨 일이 있었는지 말해 주는 자리가 여기 하나뿐**이라,
'몇 명에게 갔다'만이 아니라 **안 간 사람까지** 적습니다.

- **아무 일 없이 끝났을 때도 엽니다.** 폭죽은 학생 화면에서만 터지므로,
  확인할 자리가 없으면 교사는 눌린 것인지조차 알 수 없습니다.
- **안 간 까닭이 둘이라 칸도 둘입니다.**
  - **주지 못함** — 쓰기가 실패한 경우(연결 끊김 등). **이름이 곧
    단추**여서, 누르면 그 학생에게만 다시 보냅니다(`onRetry` →
    `onAward(uid, 개수, +1)` — 낱개 주기와 **같은 델타 경로**). 성공하면
    그 자리에서 초록 체크로 바뀌고 **위 인원수와 머리의 '남은 n명'도 함께**
    움직입니다 — 창을 닫았다 다시 열 필요가 없습니다.
    - 토스트였을 때는 이름만 읽고 자리표에서 그 자리를 **다시 찾아야**
      했고, 글자는 몇 초 뒤 사라졌습니다. 그래서 창으로 옮겼습니다.
    - 이름을 늘어놓는 한도(한때의 `FAILED_NAMES_MAX`)가 **없어졌습니다** —
      창은 길어져도 스크롤될 뿐이고, 스물몇 명이 실패한 날에도 하나씩
      짚어 다시 줄 수 있어야 합니다.
  - **이미 가득 참** — 누적이 천장(`REWARD_MAX` 100)에 닿아 더 안 올라가는
    학생. **글자로만 적고 누를 수 없게 둡니다** — 다시 보내도 값이 그대로라,
    눌리는데 아무 일도 안 일어나면 고장으로 보입니다.
    - **보내기 전에 미리 갈라냅니다.** `addStudentReward`는 천장에서 값도
      안 올리고 이력도 안 남기는데 **오류를 던지지도 않아**, 그냥 보내면
      '줬다'로 세어집니다. 누적은 명단이 이미 들고 있어(`roster[].count`)
      읽기가 늘지 않습니다.
- `lane()`의 `catch`는 **이름이 아니라 학생을 통째로** 담습니다 — 그 이름을
  눌러 다시 보내려면 uid가 있어야 합니다.
- **폭은 `width`와 `max-width`를 함께** 줍니다(`.award-result-modal`, 460px).
  `.modal`이 `width: 100%; max-width: 640px`이라 한쪽만 내리면 안 좁아집니다.
- 실측(1280×800): 창 460px · 가로 넘침 0 · 다시 주기 한 번에 `21명 → 22명`과
  `주지 못한 학생 2명 · 다시 준 1명`이 함께 바뀜 · 실패한 이름을 한 번 더
  눌러 성공 · 이미 다시 준 이름은 눌러도 호출이 안 늚(disabled).
- **`onToast`는 더 이상 안 받습니다**(패널에서 걷었고 `app/study/page.js`에서도
  뺐습니다). 덤으로 **책방(`/books`)에서도 결과가 보입니다** — 그 페이지는
  애초에 이 prop을 안 넘겨, 토스트 시절에는 결과가 아무 데도 안 떴습니다.
- **규칙을 한 줄도 안 건드립니다** — 낱개 주기가 이미 쓰는 그 길입니다
  (`rewards` 총계 + `rewardEvents` 한 건, 담당 교사에게 열려 있음).
- 머리줄의 단추 묶음은 **모자랄 때만 접힙니다**(`.reward-seat-head-btns`의
  `flex-wrap: wrap` + `justify-content: flex-end`). 패널 안쪽은 312px로 못
  박혀 있는데 이 단추가 들어오며 넷이 됐고, 넷이 한 줄에 서느냐는 **기기의
  한글 폰트에 달렸습니다.** Pretendard가 뜨는 보통 화면에서는 한 줄에 섭니다
  (실제 배포 화면에서 확인). 그 폰트가 없는 기기(학교 크롬북 등)는 넓은
  시스템 폰트로 떨어져 안 서는데, 그때 **패널 밖으로 삐져나가는 대신** 둘째
  줄로 내려갑니다 — 차례가 `출석 n/N · 🍊 다 함께`(누가 왔나와 그들에게 주는
  단추) / `선생님 보기 · ⤢ 확대`(보는 방법)라 접혀도 뜻으로 갈립니다.
  - **이 저장소의 컨테이너에서 잰 px을 그대로 믿지 마세요.** 한글 폰트가
    `WenQuanYi`(중국어 폰트) 하나뿐이라 Pretendard보다 글자가 훨씬 넓게
    측정됩니다 — 그 대체 폰트에서 잰 값은 `다 함께` 84px · `선생님 보기`
    90px · `⤢ 확대` 65px · 손바닥 50px이고 합이 312px을 1px 넘겨 접힙니다.
    한때 이 값을 근거로 '머리줄이 늘 두 줄'이라고 적어 뒀는데 **실제 화면은
    한 줄이었습니다.** `getComputedStyle`의 `fontFamily`도 선언한 목록의 첫
    이름(`Pretendard`)을 그대로 돌려주므로 대체 여부를 알려 주지 않습니다.
    폰트 폭이 걸린 자리는 여기서 재지 말고 실제 화면으로 확인하세요.
- **이 단추만 색이 있습니다**(`.reward-seat-all`, `--primary-light`).
  `.reward-seat-head-btns` 주석의 '하나만 달라 보이면 나머지가 눌리는 줄
  모른다'는 셋이 **같은 종류**일 때의 이야기입니다 — 옆의 둘은 보는 방법을
  바꿀 뿐이고 이것은 스물몇 명의 기록을 건드리므로, 눈에 띄는 편이 맞습니다.
  크기는 `.reward-seat-flip` 규칙을 함께 받아 저절로 맞습니다(따로 적으면
  글자 크기·여백이 어긋나 줄이 들쭉날쭉해집니다).
- **이름은 패널 제목과 갈라 둡니다.** 처음에는 단추도 '🍊 멋진 순간'이었는데,
  패널 제목이 이미 그 말이라 한 화면에 같은 글자가 60px 간격으로 두 번 서서
  무엇을 누르는 자리인지 흐렸습니다. 지금은 **`🍊 다 함께`** — 하는 일(반
  전체에게 한 개씩)을 그대로 말하고 제목과도 안 겹칩니다.
  - 이름을 또 바꾸게 되면 **단추 글자만** 고치세요. 패널 제목
    ('멋진 순간')은 접힌 슬림 바·`aria-label`까지 다섯 군데가 함께 쓰는 값이라
    따로 둡니다.

### PythonRunner 머리말 단추 — 지우개 · 복사
- `py-head-actions` 안, '프로젝트 연계' 버튼 왼쪽에 **지우개 → 복사** 차례.
  둘 다 '지금 쓴 코드'에 대한 일이라 나란히 두고, 화면을 여닫는 단추
  (프로젝트 연계·닫기)와는 갈라 둡니다.
- 복사(`.py-copy-btn`)는 누른 뒤 2초간 초록 체크로 바뀝니다(`copied`).
- 지우개(`.py-clear-btn`)는 되묻지 않습니다 — 실행기의 코드는 저장되는 글이
  아니고, 실수로 눌러도 Ctrl+Z(CodeMirror의 되돌리기)로 곧바로 돌아옵니다.
- **예시 코드는 진짜 내용이 아니라 placeholder입니다.** 그래서 학생이 한
  글자만 쳐도 저절로 사라지고, 지우개로 비우면 다시 옅게 떠 안내가
  돌아옵니다. 예전에는 진짜 코드로 채워져 있어 자기 코드를 적으면 예시
  줄이 아래에 남아 뒤섞였습니다(지우려면 여섯 줄을 손으로 지워야 했습니다).
  CodeMirror의 placeholder는 문자열의 줄바꿈을 공백으로 만들어 버리므로
  줄마다 `<div>`로 쌓은 조각을 넘깁니다(`samplePlaceholder`).

### PythonRunner 2단 서랍 — 프로젝트 연계 (교사)

- 실행기는 **2단**입니다(교사만 — 아래). 1단은 지금까지의 실행기이고, 머리말의
  **`⛶ 프로젝트 연계`**(예전 '전체 화면' 자리)를 누르면 서랍이 **프로젝트
  칸만큼만 더 벌어지고**(`LINK_W` = 300 + gap 16) 오른쪽에 `PyProjectPanel`이
  한 칸 더 섭니다.
  - **화면을 덮지 않습니다.** 2단은 서랍이 한 칸 더 열리는 것이지 전체 화면이
    아닙니다 — 뒤의 공부방 화면이 그대로 보여야 '옆에 두고 쓰는' 것이 됩니다
    (실측 1920px 화면에서 440 → 756px). 한때 여기서 `width: 100vw`로 벌렸는데
    그건 없앤 '전체 화면'이 하던 일이라 2단이 물려받을 이유가 없습니다.
  - **본문 배치는 두 단이 같습니다** — 에디터 위, 입출력 아래. 좌우로 가르던
    것도 전체 화면이 하던 일이고, 서랍 폭에서는 가를 자리가 없습니다.
  - 폭은 컴포넌트가 **인라인으로** 줍니다(1단 폭 + `LINK_W`). CSS에 못 박으면
    끌어서 정한 폭을 잃습니다. `.py-project`의 flex-basis나 `.py-main`의 gap을
    고치면 `LINK_W`도 함께 고치세요.
  - **여닫힘은 처음 열릴 때와 같은 전환을 씁니다** — `.py-panel`의
    `transition: transform .25s, width .25s`. 한 서랍이 두 속도로 움직이면
    2단만 따로 노는 것처럼 보입니다. `SLIDE_MS`(250)는 이 값과 **같아야**
    합니다 — 닫을 때 프로젝트 칸을 그만큼 더 그려 두는 데 씁니다.
    곧바로 걷어 내면 내용이 먼저 사라지고 폭이 뒤늦게 줄어 두 동작으로
    보입니다. **`.linked` 클래스도 `linkRendered`를 따릅니다**(`linkedOpen`이
    아니라) — 클래스가 먼저 빠지면 아래 `min-width`가 함께 풀려 닫히는 내내
    코드 칸이 403 → 90px로 짜부라집니다(실측).
  - 전환 중에 에디터 칸이 짜부라지지 않게 `--py-body-w`(= 폭 − 37)를 인라인
    으로 주고 `.py-panel.linked .py-body`의 `min-width`로 붙듭니다. 37은
    좌우 여백 18+18 + 왼쪽 테두리 1입니다(`.py-panel`은 border-box — 실측
    440 → 403). 넘치는 만큼은 `.py-main`의 `overflow: hidden`이 가려, 여는
    동안 프로젝트 칸이 **드러나고** 닫는 동안 **가려집니다**.
  - **`.py-body`에 `min-width: 0`이 있어야 합니다.** flex 항목의 기본값
    `min-width: auto`는 내용보다 좁아지기를 거부하는데, 세로로 쌓기만 하던
    때는 부모가 열이라 티가 안 났습니다. `.py-main`으로 감싸 가로줄이 되자
    이 칸이 부모보다 13px 넓어져 잘렸습니다(실측 403 대 416).
  - **크기 조절 손잡이는 2단에서도 그대로**입니다. 끄는 것은 '패널 전체 폭'인데
    state에 담는 것은 에디터·입출력 칸의 폭이라, 2단이면 `LINK_W`만큼 빼고
    담습니다 — 안 그러면 2단에서 끌 때마다 폭이 그만큼 튑니다.
  - **넓히기만 하던 예전 '전체 화면'은 없앴습니다.** 그래서 **연계할 것이 없는
    화면에는 단추가 아예 없습니다**(`canLink` — 교사이고 반·프로젝트 목록·
    사용자가 다 있어야 섭니다). 질문방(`/board`)과 학생 화면이 그렇습니다.
  - 상태 이름도 `linked`, CSS도 `.py-panel.linked`입니다 — `full`로 두면
    '넓히기'로 읽혀 없앤 개념이 이름에 남습니다.
- **2단은 교사에게만 있습니다**(`canLink`에 `isTeacher`). 학생 실행기는 1단
  연습장입니다 — 학생이 활동 칸에 코드를 쓰는 곳은 수업 노트 서랍의 '프로젝트
  활동'입니다(위 '수업 밖에서 — 프로젝트 활동' 절).
  - **한때 학생도 2단이 있었고, 거기서 '활동으로 보내기'를 눌렀습니다.**
    교사가 고른 곳(`pyTarget`)이 기본 목적지였고 학생은 제 화면에서만 바꿀 수
    있었습니다(`myPick`). 그 보내기가 탈의 원천이었습니다 — 제목으로 짝지어
    엉뚱한 칸에 들어가기(1번에 보냈는데 2번 글이 1번에), 카드 화면이 보낸 것을
    늦게 들여오거나 자동 저장으로 덮어쓰기, 고쳐 놓고 다시 안 돌린 코드에 지난
    결과가 붙기, 잠긴 활동으로 보내기. 학생이 **그 칸에서 곧바로 쓰면** 보낼 일
    자체가 없어 전부 걷었습니다(`pyShareHtml` · `appendToActivity`도 함께).
    **학생에게 보내기를 되살리지 마세요** — 되살리려면 위 탈을 전부 다시 막아야
    합니다.
- **교사 2단이 하는 일은 둘**입니다.
  - **고르면 그 활동을 엽니다**(`openIfLocked` — 위 studyTemplates 절). 고른
    곳은 반 문서의 `pyTarget`에 적어 다음에 열어도 그 자리입니다(학생 화면은
    이 값을 안 읽습니다).
  - **그 자리에서 프로젝트·활동을 만듭니다.** 수업 중에 공부방으로 건너가
    만들고 돌아오면 짜던 코드가 없습니다(화면을 옮기면 실행기가 닫힙니다).
    만든 것은 곧바로 골라 둡니다. 여기서 더한 활동만 **열린 채로
    시작합니다**(`nextActivityLocks`의 기본은 잠김인데, 지금 쓰게 하려고
    만드는 활동이라서).
  - 고르개 둘(프로젝트 ｜ 활동)은 **맨 위 한 줄에 나란히**(`.py-project-picks`,
    `1fr 1fr`). 칸이 300px이라 한 칸이 138px이고 긴 이름은 고르개가 알아서
    줄여 적으므로, **바로 아래 `.py-project-where`가 고른 곳을 온전히 되풀이**
    합니다(잠긴 활동이면 그것도 여기서).
  - 모둠 프로젝트를 고르면 '학생이 서랍에서 쓸 수 없다'는 한 줄을 답니다
    (규칙이 학생의 카드 생성을 막습니다).
- **`pyTarget`을 방송 문서에 두지 마세요.** `LessonMode`가 **일시정지에도**
  방송 문서를 지우므로, 잠깐 멈출 때마다 고른 곳이 사라집니다. 반 문서라야
  수업 내내 남습니다.
- **규칙을 한 줄도 안 건드립니다.** `classes/{cId}`는 읽기가 이미
  `signedIn()`이고 교사 수정에 필드 화이트리스트가 없습니다.
- **읽는 문서도 안 늡니다.** 프로젝트 목록(`subscribeStudyBoards`)도 반 문서
  (`subscribeClasses`)도 공부방이 **이미 구독해 둔 것**을 prop으로 받습니다.
- **밖에서 바뀐 카드는 칸 단위로 들여옵니다**(`StudyMyActivityCard`). 보내기가
  있던 시절에 생긴 것인데, 지금은 수업 노트 서랍이 같은 카드를 쓰므로 그대로
  필요합니다. 카드 내용을 마운트 때 한 번만 읽어 두면 서랍에서 쓴 것이 안 보이고,
  그 상태로 자동 저장이 돌면 **서랍의 글을 덮어쓰기까지** 합니다.
  - 들여오지 않는 경우, 다 **학생이 쓰던 글을 지키려고**입니다: 우리가 방금
    저장한 것과 같음 · 지금 저장하는 중(`savingRef`) · **'크게 쓰기' 창이 떠
    있음**(그 에디터는 비제어라 마운트 때 한 번만 읽어, 밑에서 값을 갈면
    화면과 상태가 어긋납니다). 창을 닫으면 그때 들여옵니다. 연계 프로젝트의
    '크게 보기' 창은 보기만 하므로 떠 있어도 들입니다.
  - **비켜 주는 단위는 카드가 아니라 칸입니다.** 칸마다 '마지막으로 저장한
    값 그대로인가'를 보고(`lastSavedPartsRef`), 그대로인 칸만 들여옵니다 —
    통째로 비켜 주면 그 뒤 자동 저장이 옛 상태로 들어온 글을 지웁니다.
  - 들여온 뒤 `baselineSigRef`를 **`sigOf()`와 같은 모양으로** 옮기세요 —
    안 그러면 방금 받아들인 값을 '내가 고친 것'으로 보고 되저장합니다. 기준은
    **들어온 값**입니다(섞인 결과가 아니라).
- **카드의 칸을 짚는 것은 자리(index)입니다 — 쓰는 곳에서
  `matchActivitySections`를 쓰지 마세요.** 학생 카드는 섹션을 자리로 읽고
  자리로 씁니다(`secs[i]` ↔ `activities[i]`). 옛 보내기가 제목으로 짝지었다가
  **1번에 보냈는데 2번 글이 1번에 들어왔습니다**(실제 신고) — 학생이 카드의
  활동 제목을 고칠 수 있고 교사가 활동 이름을 바꾸기도 해 제목은 자주 어긋납니다.
  `matchActivitySections`는 **읽기만 하는 화면**(카드 미리보기·대시보드·
  활동보기)에 둡니다.
- **카드 HTML을 짜는 자리는 `buildActivityHtml` 한 곳**입니다. 학생 카드
  (`StudyMyActivityCard`)와 서랍의 활동 칸(`LessonTaskPanel`)이 함께 씁니다 —
  두 곳이 다른 모양을 만들면 `parseActivitySections`가 되읽을 때 어긋나, 낸
  답이 '작성 전'으로 보입니다.
- 프로젝트 칸은 **폭 고정 300px**(`flex: 0 0 300px`, `.py-main`이 감쌉니다).
  `flex: 1`로 두면 서랍을 넓힐수록 고르개 두 개가 덩그러니 늘어나고 정작
  넓어야 할 에디터가 줍니다. 1024px 밑에서는 `.py-main`이 세로로 풀려
  프로젝트 칸이 아래로 갑니다.

### 모달은 Esc로 닫힙니다 — `backdropClose` 한 줄이 맡습니다

- 모달 마흔여 개 가운데 Esc로 닫히는 것이 열댓 개뿐이었습니다(출석 관리 ·
  질문 상세 · 공부 기록도 안 닫혔습니다). 저마다 keydown을 걸게 하지 않고,
  배경에 이미 붙어 있던 `{...backdropClose(onClose)}`가 **배경을 등록**하고
  창에 건 처리기 **하나**가 맨 위 모달만 닫습니다(`lib/modal.js`).
- **맨 위**는 안에 든 것 → z-index → 문서에서 뒤의 것 순으로 가립니다. 맨 위가
  등록 안 한 `.modal-backdrop`이면(제 Esc를 따로 다루는 창) 손대지 않습니다.
- **Esc를 스스로 다루는 곳은 `e.preventDefault()`를 부르세요.** 공통 처리가
  그것을 보고 비켜 갑니다 — 팝오버만 닫기(`lib/popover.js`) · 달력만 닫기 ·
  고치던 칸 취소 · 확대 이미지 닫기. 안 부르면 한 번 눌러 둘이 함께 닫힙니다.
  공통 처리는 **다른 처리기가 모두 돈 뒤에**(setTimeout 0) 판정하므로 등록
  차례와 상관없습니다. 지금 Esc를 다루는 스무여 곳은 모두 부르게 맞춰 두었습니다.
- **창 안에 글이 든 칸이 하나라도 있으면 닫지 않습니다**(초점 칸만이 아니라
  창 전체 — 질문 쓰기는 제목이 비어도 본문에 글이 있습니다). 그래서 값이 이미
  채워진 편집 창(프로필 · 원본 편집)은 Esc로 안 닫히는데, 그것이 안전한
  쪽입니다. 한글 조합 중의 Esc도 그대로 둡니다.
- 칸이 아닌 곳에 쓰는 창은 `backdropClose(onClose, { esc: false })`로 뺍니다 —
  지금은 그림판(`DrawingCanvas`) 하나입니다.
- 이 한 줄이 `ref`를 함께 돌려줍니다. **배경 요소에 `ref`를 따로 달지 마세요** —
  뒤에 적은 쪽이 이겨 한쪽이 조용히 빠집니다(지금은 그런 곳이 없습니다).
- 실측: 질문 상세에 답을 적는 중 Esc → 그대로, 비우고 Esc → 닫힘 · 출석 관리 ·
  공부 기록 닫힘 · 편집 창 위 확인 창에서 Esc → 확인 창만 · '멋진 순간' 확대
  창의 과일 팝오버 → 팝오버만, 한 번 더 → 창 · KWLS 크게 보기 한 번에 하나.
- 공통 `<Modal>` 껍데기는 아직 없습니다. 창마다 머리줄·폭·포털 여부가 달라
  한 번에 옮기면 모양이 흔들리므로, 동작(Esc)만 먼저 모았습니다.
- **Ctrl(⌘)+A도 같은 자리가 맡습니다** — 맨 위 모달이 있으면 그 창 본체
  (`.modal`)의 내용만 고릅니다(`installSelectAll`). 브라우저 기본은 모달이
  떠 있어도 뒤의 페이지·서랍까지 통째로 골라, 창 안의 코드와 결과만 복사하려던
  것이 한꺼번에 딸려 왔습니다(실제 신고). 판정은 Esc와 같은 `topBackdrop`.
  - **글자 칸 안에서는 손대지 않습니다**(입력칸 · 여러 줄 칸 · 서식 에디터 ·
    코드 칸) — 거기서는 '그 칸의 글 모두'가 맞고 브라우저가 이미 그렇게 합니다.
  - 한글 입력 상태에서는 `e.key`가 'ㅁ'으로 올 수 있어 `e.code`('KeyA')도 봅니다.
  - 실측: 모달 없음 → 페이지 전체(그대로) · 모달 빈 곳 → 창 글만(상단바 글
    섞임 없음) · 창 안 입력칸 → 그 칸의 글만.

### 바깥을 눌러 닫기 — 사라진 것은 '바깥'이 아닙니다

- 실행기는 `document`의 `mousedown`을 듣다가 `panelRef`에 안 담긴 곳을
  누르면 닫습니다. 여기에 **누른 것이 이미 사라진 경우**가 걸립니다.
- 자동 완성 목록이 그랬습니다. 항목을 마우스로 누르면 CodeMirror가 **그
  mousedown이 `document`까지 올라오기 전에** 완성을 넣고 목록을 통째로 DOM에서
  걷어 냅니다. 우리 차례가 왔을 때 `e.target`(그 `<li>`)은 문서에서 떨어져
  나간 뒤이고, 떨어진 노드는 어느 것에도 안 담기므로 `contains`가 false —
  '바깥을 눌렀다'로 읽혀 **실행기가 통째로 닫혔습니다**. 키보드 Enter로 고를
  때는 mousedown이 없어 멀쩡했고, 그래서 마우스로만 나는 증상이었습니다.
- 막는 것은 `if (!e.target.isConnected) return;` 한 줄입니다. 누르는 순간
  사라진 것은 '바깥'이 아니라 **그것을 없앤 쪽의 일**이라, 어디였는지 따질
  것 없이 지나갑니다. 자리를 옮겨 다니는 팝업을 선택자로 하나씩 적어 두는
  것(`.cm-tooltip` 따위)보다 넓게 막힙니다.
- **바깥 클릭으로 닫는 것을 새로 만들 때는 이 한 줄을 함께 두세요.** 누르면
  스스로 사라지는 것(드롭다운·툴팁·메뉴)이 그 안에 하나라도 있으면 같은 일이
  납니다.

## 주의 사항

- `lib/data/*`의 Mock 구현과 Firebase 구현을 **항상 동기화**할 것
  (함수 추가 시 두 분기 모두 작성)
- **반 명단은 `useClassRoster`로 받으세요**(`lib/useClassRoster.js`, 셈은
  `lib/roster.js`의 `buildClassRoster`). 같은 셈이 공부방·책방·손든 학생 자리
  확인 창에 따로 있었는데 **자리 확인 창만 학번순 정렬이 빠져**, 자리표에 없는
  학생을 빈자리에 채울 때(`normalizeSeats`는 명단 차례를 씁니다) 그 창에서만
  다른 자리에 앉았습니다. 소속 uid만 필요하면 `useClassMembers`.
  - 두 곳은 일부러 안 옮겼습니다 — `/admin`의 반 분석(`null` = 아직 모름 ·
    `Set`)과 기록 관리의 옆 반 명단(`null` = 아직 모름). 이 훅은 첫 답 전에도
    빈 배열이라 '없다'와 '아직 모른다'를 못 가릅니다(위 '첫 화면' 절).
  - 디렉터리·과일은 **받아서** 씁니다. 페이지가 이미 다른 데 쓰려고 구독해
    두어, 훅이 또 구독하면 같은 컬렉션에 리스너가 둘입니다.
- **데이터 함수는 영역 파일(`lib/data/*.js`)에 두고, 화면에 보일 것은
  `lib/store.js`의 목록에 한 줄 더합니다.** `store.js`는 7천 줄이 넘던
  한 파일을 영역별로 나눈 뒤 남은 입구라 다시 내보내기만 합니다 — 화면은
  지금까지처럼 `@/lib/store`에서 가져가고, 목록에 안 올리면 화면에서
  안 보입니다. 영역끼리 나눠 쓰는 비공개 도우미(`mock` · `notify…` ·
  `nextMockSeq`)는 일부러 목록에 올리지 않습니다.
  - 데모 문서 id의 일련번호는 `nextMockSeq()`로 올립니다. `mockSeq`는 `let`이라
    다른 파일에서 `++` 할 수 없습니다(ES 모듈의 import는 읽기 전용).
  - **영역 파일끼리 서로 가져올 때 최상위에서 곧바로 쓰지 마세요**(함수 안에서만).
    서로 가져오는 고리가 생기면 먼저 읽힌 쪽에서 아직 정해지지 않은 값을 봅니다.
    `shared.js`는 어느 영역 파일도 가져오지 않습니다 — 그래서 모두가 기댈 수 있습니다.
- **mock 문서는 `replaceDoc(목록, 문서, 고칠 값…)`으로만 고칩니다**
  (`lib/mockDocs.js`). `Object.assign(문서, …)`이나 `문서.필드 = 값`처럼
  제자리에서 고치면 구독하는 쪽이 같은 참조를 받아 React가 다시 그리지
  않습니다 — 반 문서·독서 활동·수업 노트·답변에서 따로 네 번 겪은 그 함정입니다
  (위 절들의 '갈아 끼웁니다'가 모두 이것). 한때 이 모양이 store.js에 34곳
  남아 있어 한꺼번에 옮겼습니다. 인자가 Object.assign과 같아 앞에 목록만
  붙이면 되고, 고칠 값 자리에 함수를 주면 지금 문서를 보고 셉니다
  (`(q) => ({ answerCount: q.answerCount + 1 })`). 새 객체는 **목록의 같은
  자리**에 들어갑니다 — 빼고 뒤에 다시 넣으면 화면 차례가 바뀝니다.
- `saveKwl` (upsert)은 제거됨 — `addKwl` (append)만 사용
- `subscribeMyKwl` (단일 반환)은 제거됨 — `subscribeMyTodayKwl` (배열 반환)만 사용
- CSS `@media (max-width: 760px)` 블록이 별도 존재함 — 768px 블록에서 필요 시 덮어쓸 것
- 채팅 입력: Enter 단독은 줄바꿈, **Ctrl/⌘+Enter는 전송** (전송 버튼도 유지)
- **pdf.js는 반드시 `legacy` 빌드**를 쓸 것 (`pdfjs-dist/legacy/build/…`).
  기본 빌드는 `Map.prototype.getOrInsertComputed` 등 최신 문법을 써서
  Chromium 141에서도 `render()`가 실패함(실측). `lib/pdfSlides.js`의 import와
  `scripts/copy-pdf-worker.mjs`가 복사하는 워커는 **항상 같은 빌드로** 맞출 것
- pdf.js 워커는 CSP(`worker-src 'self'`) 때문에 CDN 불가 — `public/`에 복사해
  같은 출처에서 서빙 (prebuild·predev에서 자동 실행, 생성물이라 git 제외)

## 질문 읽기 범위 — 진행 중인 정리

`questions`는 학교 전체가 함께 쓰는 컬렉션이라 2월까지 4~5천 건이 예상됩니다
(두 반 · 학생 50명). 화면마다 전체를 받으면 그 수에 비례해 느려지고,
Firestore 무료 할당(하루 읽기 5만 건)도 질문 300~400개 언저리에서 넘습니다.
그래서 **화면이 필요한 만큼만 받도록** 옮기는 중입니다.

**끝난 것** — 공부방·학습 리포트

| 화면 | 지금 읽는 범위 |
|------|----------------|
| `/report` | `subscribeMyQuestions` + `subscribeMyAnswerEvents` (내 것만, 리스너 2개) |
| 공부방 관련 질문 | `subscribeQuestionsByKeywords` (그 프로젝트 키워드) |
| 참여 전광판 질문 수 | `subscribeQuestionsByAuthors` (반 명단, 열었을 때만) |

`/report`는 예전에 **질문마다 답변 리스너를 하나씩** 걸었습니다(질문 5,000개면
리스너 5,000개). 지금은 방향을 뒤집어 내 답변을 먼저 찾고 그 답변이 달린 질문만
읽습니다 — 질문만 내 것으로 좁히면 **남의 질문에 단 내 답변이 통계에서
빠지므로**, 이 순서를 바꾸지 마세요.

**남은 것**

- **`/board` 페이지네이션** — 질문 200개쯤 쌓이면 착수. 목록을 자르는 것 자체는
  한 줄이지만, 전체 배열에 기대던 네 가지를 각각 옮겨야 합니다: 키워드 개수
  배지(`getCountFromServer`), 고정 글(`pinned` 별도 구독 후 앞에 붙이기),
  미해결·내 글 필터(서버 쿼리 — 여기만 **복합 색인 필요**, 코드보다 먼저 배포),
  `?open=<id>` 링크(목록에 없으면 `getDoc` 단건).
  200개 전에는 착수하지 말 것 — '고정 글이 첫 페이지 밖으로 밀려난 경우'를
  재현할 수 없어 맞게 짜도 확인이 안 됩니다.
- **`/admin`** — 아직 전체를 받고, 질문마다 답변 리스너를 겁니다(`/report`가
  갖고 있던 그 구조). 반 전체 활동 요약(누가 몇 개 묻고 답했나)을 만드는
  화면이라 '선택한 학생만'으로는 좁힐 수 없어 집계 방식을 다시 설계해야
  합니다. 교사만 가끔 여는 화면이라 `/board` 다음 순서.

**효과 확인** — Firebase 콘솔 → 사용량 → 읽기 수. 단계마다 전후를 비교하세요.

**'14칸을 다 채운 학생 n/N'을 활동 목록 카드에 두지 마세요.** 이 값을 내려면
그 활동의 낱말을 전부 읽어야 하는데(활동 하나에 400건 안팎), 목록은 카드가
계속 쌓이는 곳이라 카드마다 그 계산을 돌리면 활동 수에 정비례합니다 — 활동
20개면 목록을 한 번 열 때 7천여 건에 동시 조회 500여 개입니다. 한때 카드에
뒀다가 이 이유로 뺐습니다.

지금은 **작업 화면과 전체 보기**에만 있습니다. 두 화면은 이미 그 활동의 낱말을
구독하고 있어 읽기가 1건도 안 늡니다. 세는 범위가 화면마다 다른 점에 주의:
전체 보기는 모든 판을 받으므로 반 전체, 작업 화면은 개별 활동이면 반 전체이고
**모둠 활동이면 고른 모둠 하나뿐**입니다(그 화면이 그 모둠만 구독). 모둠
활동에서 반 전체를 보려면 '전체 보기'로 갑니다.

세는 단위는 낱말이 아니라 **칸**입니다(13칸에 낱말 14개면 13칸). 표시를
평균에서 완료 인원으로 바꿔도 읽는 양은 같습니다 — '그 칸을 채웠나'는 낱말
문서에만 있어서, 무엇을 세든 전부 읽어야 합니다. 목록에서도 싸게 보려면
낱말을 넣을 때 집계 문서를 함께 갱신하는 수밖에 없는데(Cloud Function),
활동이 스무 개를 넘겨 목록이 느려질 때까지는 하지 마세요 — 얻는 것 없이
어긋날 수 있는 실패 지점만 늡니다.
그래서 `fetchConsonantProgress`는 **한 번 읽고 세션 동안 캐시**하고, 교사가
그 활동을 열 때만 그 활동의 캐시를 버립니다(`invalidateConsonantProgress`).
활동이 스무 개를 넘어가면 이 방식도 무거워지므로, 그때는 '진행률 보기'를
눌러야 읽도록 바꾸거나 활동 문서에 집계를 적어 두는 쪽을 검토하세요.

새 색인을 늘리지 않으려고 `where`만 쓰고 정렬은 화면에서 합니다(등호 필터와
다른 필드 정렬을 함께 걸면 복합 색인이 필요해집니다). 결과가 수백 건 이하인
질의에서만 통하는 방법이니, 범위가 커지는 곳에는 그대로 쓰지 마세요.
